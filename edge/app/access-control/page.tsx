import { EdgeClient } from '../../src/components/edge-client';

/** Enterprise view: role-based access control (Phase 4). */
export default function AccessControlPage() {
  return <EdgeClient view="access-control" />;
}
