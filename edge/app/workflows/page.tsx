import { EdgeClient } from '../../src/components/edge-client';

/** Enterprise view: multi-step approval workflows (Phase 4). */
export default function WorkflowsPage() {
  return <EdgeClient view="workflows" />;
}
