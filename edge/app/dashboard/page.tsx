import { EdgeClient } from '../../src/components/edge-client';

/** Enterprise pilot: the redesigned dashboard. */
export default function DashboardPage() {
  return <EdgeClient view="dashboard" />;
}
