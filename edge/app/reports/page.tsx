import { EdgeClient } from '../../src/components/edge-client';

/** Enterprise view: reporting and report generation (Phase 2). */
export default function ReportsPage() {
  return <EdgeClient view="reports" />;
}
