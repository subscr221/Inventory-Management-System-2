---
topic: IMS inventory management platform UX (fresh create, post-pilot build)
updated: 2026-09-26T03:23
---

- (decision) Ergonomic leads over modern when the two conflict (user, party round 1).
- (decision) Role priority for UX work: gate first, then stores, then approvers, then finance.
- (decision) Gate officer devices: phone plus rugged scanner; some have a rugged tablet. Gate UI must work across phone, scanner (hardware trigger/keyboard-wedge) and tablet.
- (decision) Gate capture supports BOTH scan-to-fill (challan QR / number-plate capture) AND manual entry with large targets; manual is the always-available fallback.
- (decision) Two density modes under one token set: touch-roomy for gate and stores, compact for approvers and finance.
- (assumption) Frontline touch targets at least 56px with primary action full-width in bottom third; desk surfaces keep 44px floor (Sally proposal, Murat testable; user not yet confirmed numbers).
- (event) Amelia grep found no dedicated gate-entry screen in edge/src/components; backend has weighbridge.ts and gate_dwell_metric; gate passes are Epic 20 Phase 2. Unverified against dashboard screens.
- (decision) Supplier paperwork mix at gate: about 50% carry QR/barcode, about 50% handwritten. Both paths are first-class, neither is an edge case.
- (decision) Gate entry core fields (priority): challan photo, challan reference, vehicle number, supplier. Driver, weight etc. are secondary/optional.
- (decision) Rugged scanner is Android-based (model and screen size unknown). Design small-first so the scanner screen is the tightest canvas; tablet may go two-column.
- (assumption) Plate OCR is best-effort on-device with mandatory officer confirmation; never auto-submit. Scan-to-fill resolves challan QR against locally synced PO/ASN rows offline.
- (decision) Gate entry step order: CHALLAN FIRST (scan QR/barcode or type the reference), THEN photo. User overruled both photo-first (Victor/Ravi) and scan-only (Winston).
- (decision) Unlisted supplier allowed at gate: fuzzy pick from synced vendor master, with an Unlisted supplier option that flags the entry for stores to match later. The truck is never blocked at the gate for an unknown supplier.
- (event) Rendered .working/key-gate-entry.html: clickable gate-entry mock, scanner 360x640 + phone 412x860 + tablet two-column, existing app tokens only (colors undecided). Observed: scan toast overlaps stepper labels; on scanner width a QR match pushes Type challan number below the fold; Next is enabled before match is accepted.
- (decision) User direction: base the visual language on Zoho design (Zoho apps look and feel). Scope (which Zoho product, how literal) to be resolved; brand marks and logos are not copied.
- (event) Zoho research (ctx KB labels zoho-*): no public Zoho design system; Puvi typeface has no published license (do not bundle); Books nav = module sidebar (dark or light pane), search, org switcher, + quick create; Books accent default blue, user-selectable; product UI hex values unverified; Inventory VPAT partially supports contrast, does not support reflow/resize text; Inventory mobile does camera + external (Zebra) scanning with quick create for receives/picklists.
- (assumption) Emulate Zoho layout and interaction patterns only; never Zoho name, logos, icons, screenshots or Puvi font. Open font substitute (Inter or Lato) pending user pick.
- (decision) Zoho is the primary reference because it is familiar to users and has a nice interface; other modern ERPs are acceptable secondary references. Emulate structure and patterns, not brand.
- (decision) Navigation sidebar is DARK, like classic Zoho Books (desk/compact mode). Frontline touch mode collapses nav to a bottom bar.
- (decision) Typeface: Lato (open license), replacing Zoho Puvi which cannot be bundled.
- (decision) Accent colours are configurable (per site, Zoho-style user-selectable accent); exact default accent and per-site palette still to be picked.
- (decision) Gate mock fixes approved: (1) drop the scan toast, the match card is the feedback; (2) Next stays disabled until the challan match is accepted, rejected, or typed manually; (3) Type challan number stays reachable (no scroll) after a scan.
- (decision) Accent: render 4 candidate accents on a colour-direction board (dark Zoho-style desk shell + gate scanner screen), each AA contrast-checked with white text; user picks from the 4.
- (change) key-gate-entry.html updated: toast removed (aria-live kept), Next gated on challan resolution, Scan again + Type challan number side by side (>=48px) visible without scroll at 360x640, Lato-first font stack. Verified in Chrome by renderer.
- (event) Rendered .working/color-themes-1.html: 4 accents all pass 4.5:1 with white button text: A Ledger Blue #1E6BC6, B Plant Teal #0A7667, C Board Indigo #4F46C8, D Kiln Terracotta #B4461B. Dark sidebar #1B2230. Finding: base accents fail 3:1 on dark sidebar, so a 5th token --accent-on-dark (lighter tint) is needed for active nav marker; passes for all four.
- (decision) Default accent for CMF-ALIGARH: A Ledger Blue #1E6BC6. Teal #0A7667, Indigo #4F46C8, Terracotta #B4461B stay as preset per-site options. Each accent is a five-token family incl. --accent-on-dark for the dark sidebar.
- (decision) Storekeeper job (user): accepts material into the store, takes photographs, assigns a rack, ensures traceability, issues items, and puts returned items back on the shelf. Stores is the second priority surface after gate.
- (decision) Storekeeper devices: rugged scanner, tablet and desktop, depending on where they are. Stores surfaces must work across all three.
- (decision) Racks/bins carry barcode or QR labels today; stores has a label printer to tag received material.
- (decision) Stores home = task inbox (arrived trucks awaiting acceptance, pending issues, returns) with an always-on-top Scan anything bar (scan rack, item or challan and the app offers the matching action).
- (decision) Rack assignment: system suggests the rack (directed putaway).
- (assumption) Storekeeper can override the suggested rack in one tap (existing directed-putaway override that teaches the system); user said system suggests, override not explicitly confirmed.
- (assumption) Gate entry hands off to stores inbox as an arrived-awaiting-acceptance item carrying challan, supplier and PO lines (no retyping).
- (override) Density follows device input, not role: coarse pointer or small viewport = touch-roomy (>=56px targets), fine pointer = compact. Supersedes the earlier role-based density split (gate/stores roomy, approvers/finance compact).
- (assumption) Gate surface is always touch-roomy regardless of device (Sally guard); user approved density-by-input but did not explicitly rule on this floor.
- (decision) Label printing is non-blocking: GRN/acceptance completes, label goes to a print queue with a Reprint action on the item. Every received lot gets a printed QR resolving to its GRN, PO line and rack.
- (decision) Excess qty: design supports both return-with-vehicle and hold-for-buyer. PILOT: UI caps accepted qty at PO + over_receipt_tolerance_pct and records the remainder as 'Excess - returned with vehicle' (reason code); only accepted qty is posted, so no backend change and no whole-line RECEIPT_TOLERANCE_EXCEEDED reject. POST-PILOT: 'Hold for buyer decision' routes excess to quarantine and creates an approval item.
- (decision) Receiving reason codes (fixed, coded, grouped so <=4 tiles per screen): SHORT = supplier short-shipped, part delivery - balance to follow, count mismatch with challan; DAMAGED = transit damage, packing damage, rust or corrosion; REJECTED = wrong item or spec (to QC); OTHER = mandatory photo plus one line. Labels site-configurable later.
- (decision) Per GRN line record Received = Good + Damaged + Rejected. Good goes to suggested rack; damaged and rejected go to a quarantine bay, never the shelf. Shared state pattern: problem stock is not on shelf until resolved.
- (assumption) Pilot feasibility of splitting one PO line across rack and quarantine locations in one GRN is unverified (Amelia to check); if unsupported, it becomes a small backend story.
- (event) Backend check (PARTIAL): split works today via two POST /api/v1/grn-lines calls on the same grn_id (good qty to rack target_location_id, damaged qty to quarantine location), optional own lot + PUT /lots/:id/quality-hold to lock it in quarantine. Missing: no damaged/rejected qty field and no reason code on a grn line; damaged line shows status posted with putaway ready, not held. Tolerance is CUMULATIVE per PO line (all non-rejected lines + legacy received vs ordered x (1+pct)), compliance/receiving.ts:629-645.
- (change) Pilot receiving pattern refined: UI computes remaining band = ordered x (1+tolerance) - already received, and caps posted qty (good + damaged kept) to it; the rest is excess returned with vehicle and not posted. Damaged kept for debit note posts as a second grn line to a quarantine location with quality hold; damaged sent back on the truck is not posted. Wrong item or spec is never posted against the PO line.
- (assumption) Backend story needed post-pilot (or pre-pilot if reason reporting is required): condition (good/damaged/rejected) and reason_code on grn lines so reasons are stored in the event, not only in the UI.
- (decision) Receiving reason reporting is REQUIRED AT PILOT. Pre-pilot backend story: add condition (good/damaged/rejected) and reason_code (fixed coded list) to grn lines and the goods.received event, with damaged/rejected lines routed to quarantine with quality hold. Needs a story via bmad-create-story or bmad-correct-course (adds pilot scope).
- (override) Damage is NOT decided by gate officer or storekeeper. Damage is usually found later when packaging is opened, not at the gate. Only QC and finance decide, after assessment and inspection. Supersedes the storekeeper-recorded Good/Damaged/Rejected split and the storekeeper-picked damage reasons.
- (decision) Storekeeper and gate only REPORT: at receipt they may note visible external damage (observation + photo, not a verdict); at any later time (e.g. unpacking) anyone handling stock can 'Report damage' by scanning the lot/item. Reporting places the stock on hold (not issuable) and routes it to QC for inspection; QC then finance decide the outcome.
- (event) Backend has a QC module: /api/v1/qc/tasks/:id/observations, /results, /inspection-completion, /split, /disposition (lot dispositions accept|reject, plus conditional_release, DOA-governed), lot quality_hold placed/cleared. No edge UI for QC found. Unverified: ad-hoc QC task creation from a damage report, and any finance commercial outcome (debit note / return to supplier / write-off) endpoint.
- (assumption) The pre-pilot backend story reshapes: 'Report damage' = lot quality hold + QC task carrying reason code and photo, reusing the QC module (observations, split, disposition); finance commercial decision surface still to be designed.
- (override) ALL received items land first in a separate INSPECTION BAY (not directly on the suggested rack). Rack assignment/putaway to the suggested rack happens after inspection clearance. Supersedes the stores mock's rack-at-acceptance step.
- (decision) At receipt, items with visible damage are marked 'Inspect on opening - damage suspected' or 'Inspect on opening - damage observed' (a flag for the inspector, not a verdict).
- (decision) Damage outcome requires CONCURRENCE of both QC and finance. They may decide together or one after the other depending on the situation; the outcome is final only when both have concurred.
- (decision) Anyone in the chain can report damage, including shop-floor machine operators (e.g. damaged component, or dead-on-arrival electronic part found after work starts). Report damage is a universal action, not a stores-only screen.
- (decision) If QC and finance disagree on a damage outcome, the case escalates to the CEO for the final decision.
- (decision) Hold scope: localised damage holds only the reported units, nothing elsewhere. If it is a quality issue, there is an option to hold ALL remaining stock of that lot everywhere (store, inspection bay, shop floor).
- (decision) Shop-floor machine operators report damage from a shared terminal at the machine (no personal device).
- (decision) Report damage + Request replacement is one flow, IN PILOT SCOPE (replacement becomes an indent/issue request so the line is not stopped).
- (decision) Devices: finance on desk; QC on both tablet (at the inspection bay) and desk.
- (event) Design preview published on staging: https://ims-staging.ancorlabs.org/ux-preview/ (index + gate entry + corrected stores inbox/acceptance with inspection bay first + colour board). Static nginx location /ux-preview/ (noindex) and read-only mount UX_PREVIEW_DIR in deploy/compose; public, no login. Only nginx recreated 2026-09-25; box backups /root/*.pre-ux-preview-202609251149.
- (decision) User asked to extend the design preview to ALL roles (2026-09-25). Pilot roles grouped into 8 surfaces: Gate (gate_officer, weighbridge_operator); Stores (store_assistant); Warehouse floor tasks (warehouse_operator picker, dispatch_clerk, unloading_supervisor exceptions); QC workbench (qc_inspector, qc_head); Approvals inbox (department_head, warehouse_manager, qc_head, maintenance_supervisor, finance_controller, cfo) incl. damage case with QC+finance concurrence and CEO escalation; Maintenance (maintenance_technician, maintenance_supervisor); Shop-floor terminal (machine operator report damage + request replacement, floor indent raiser); Desk overview (warehouse_manager/site head, inventory_controller, finance_controller valuation, internal_auditor read-only, migration_lead).
- (assumption) Shared machine terminal: per-report badge scan or PIN step-up sign-in, session returns to idle after submit (room recommendation, not yet ruled).
- (assumption) Lot-wide hold: the reporter can only REQUEST it ('Suspect whole lot'); QC head decides it; reporter's own hold is always local (room recommendation, not yet ruled).
- (assumption) Approver devices, volumes and per-role approval details not elicited; approvals inbox follows Zoho-style desk list/detail with a phone-width variant; DOA types and approver roles taken from world.json doa_entries.
- (assumption) Weighbridge weight capture, picking, packing/dispatch, maintenance and desk dashboard layouts not elicited with the user; drafted from existing edge components, epics and role jobs, tagged for review.
- (event) All-roles preview published 2026-09-25 at /ux-preview/: 6 new mocks (key-approvals, key-qc, key-floor, key-maintenance, key-shopfloor, key-desk) + role map index; real pilot names replaced with fictitious ones in color board, stores and gate mocks. Open: desk control height (mocks use 30-36px, earlier 44px desk floor was only an assumption); QC mock added unrequested inbox group 'Sent on - finance or CEO'; each mock legend lists its own [ASSUMPTION]s.
- (override) EVERY role can use the system on EVERY device type (rugged scanner, phone, tablet, shared terminal, desktop). Supersedes all per-role device assignments; earlier device answers now mean 'most common device', not 'only device'. Role decides WHAT a person sees; device decides HOW it is laid out and operated.
- (override) There are NO shared machine terminals. Machine operators and everyone else use their own devices (any class). Removes the shared-terminal surface and the badge/PIN step-up assumption; report damage and requisitions run on each person's own signed-in device.
- (decision) Gate screens on a desktop use compact density like any desktop (no gate-specific touch-roomy floor).
- (decision) Approvals require a live connection on every device (confirmed).
- (decision) Add a device switcher (handheld / tablet / desktop) to all 9 preview mocks and re-publish.
- (decision) Any employee can create a requisition (indent) and check material/stock availability at any time. Junior employees need approval for some items; some items (e.g. a janitor's cleaning gear) are issued without any approval.
- (event) Indent backend check: no indent_raiser role in code (planning doc only); raising needs procurement write (indents.ts:557); stock lookup needs inventory read (stock.ts:195); approval decided ONLY by indent total value vs indent_approval DOA bands, approver = department_head (not reporting manager), SOD-01 self-approval blocked; pilot has one unbounded band so every indent needs approval; no item-level free-issue flag, item_category is free text per line; no employee grade or reporting line. Possible bug: indent with no band match is stored without approver and approve refuses NOT_RESOLVED_APPROVER (compliance/indent.ts:599-603), i.e. stuck not auto-approved. Epic 4 Story 4.3 promises rules by amount band, item category and department; only amount band built.
- (assumption) Requisition approval model for mocks (pending user ruling): each item carries an approval class - FREE ISSUE (no approval for anyone, e.g. cleaning gear, gloves, stationery; optional per-person monthly cap), CONTROLLED (junior employees need reporting-manager approval; senior employees self-approved within their value band), HIGH VALUE (DOA value band, department head and above). Every signed-in employee gets a base 'employee' hat: raise requisition, check stock availability, report damage, see own requests.
- (event) Preview re-published 2026-09-25: all 8 surface mocks have a 4-class device switcher (Handheld/Tablet/Desktop/Desktop touch) with state kept across switches; key-shopfloor.html retired (removed from server) and replaced by key-requisitions.html (any employee: check stock, requisition with approval classes, report damage from own device); desk mock renamed Manager Overview; gendered pronouns in mock copy replaced with they/their. Open: SOD-01 conflict with senior self-approval assumption; stores handheld pins primary only on acceptance, not on putaway/scan-result screens.
- (decision) Senior staff may self-approve requisitions within a limit. This overrides backend SOD-01 (no self-approval) for requisitions within the limit; needs a backend/spec change.
- (override) 'Junior' will NOT be defined as a grade or designation. Instead of item approval classes by seniority, approval exemption is granted per person: a 'No approval required' (NAR) grant links a specific user to a specific item or item group. Supersedes the free-issue/controlled-by-junior assumption.
- (decision) NAR grants are assigned by the site manager (site head) or the head of department. Each grant needs a one-time approval from the finance department head before it takes effect.
- (decision) NAR grants are revoked automatically when the employee exits the company, and can be revoked manually at any time by the assigner (as required).
- (decision) Assigners and approvers of NAR grants receive frequent status lists of NAR items and employees (who holds what, usage) to enable continuous pruning of the lists.
- (decision) Senior self-approval limit is set per person, using the same assign / finance-head approve / review cycle as item grants (no job-grade list).
- (decision) Requisitions without a grant are approved by the requester's head of department by value band (as today) until reporting lines exist.
- (decision) Grants may optionally carry a monthly quantity cap and an end date; usage over the cap falls back to normal approval.
- (decision) Pruning lists: frequency selectable by each assigner/approver, default every 30 days; in-app plus email; each grant with last-period usage; grants unused 90 days flagged 'consider revoking'.
- (decision) Exit trigger: disabling the person's sign-in account revokes all their grants the same day.
- (decision) At issue time the stores screen and issue slip must state the grant explicitly, e.g. '<grant term> for <user>. Storekeeper please issue.'
- (decision) User finds the term 'No approval' confusing; a new name for the grant is to be chosen.
- (decision) Grant name: STANDING APPROVAL (ref SA-YYYY-NNN). Issue slip text: 'Standing approval SA-2026-014 for <user> (<item group>). Storekeeper please issue.' Senior limit named 'Self-approval limit Rs <amount>'. Replaces all 'No approval required' wording.
- (event) Standing approval published 2026-09-25 in requisitions (chips, My standing approvals, self-approval limit), approvals (finance head one-time approval SA-2026-021 grant, SA-2026-022 limit), manager overview (Standing approvals module: list, statuses, assign, revoke, auto-revoke on exit, review-list frequency default 30 days, pruning preview), stores (issue screen with 'Standing approval SA-2026-014 for Ramu (Cleaning gear). Storekeeper please issue.' banner + slip). Cap counts quantity issued. Assumptions open: value bands (HOD up to Rs 1,00,000, finance above), grant send/revoke need live connection, revoke needs no finance approval.
- (assumption) Device class breakpoints (proposed 2026-09-25, user did not object): Handheld under 600px wide + coarse pointer; Tablet 600-1100px + coarse; Desktop over 1100px + fine pointer (compact); Desktop touch over 1100px + coarse (touch-roomy). Class chosen by width + pointer media queries, never by role or device name.
- (event) Spines drafted 2026-09-25: DESIGN.md (Google spec, tokens incl. accent family + presets, density by class) and EXPERIENCE.md (14 sections, 5 named flows, 14 open questions, backend dependencies). Status draft; Finalize steps pending: reviewer gate, open-item triage, promote mocks to mockups/, editorial passes, status final.
- (decision) Q1 confirmed: breakpoints under 600 / 600-1100 / over 1100px; pointer type decides compact vs touch density.
- (decision) Q2 confirmed: touch primary 56px, secondary 48px; desktop controls 34px (Zoho density).
- (decision) Q3 confirmed: rack overrides teach the putaway suggestion (Story 3.5 reslot).
- (decision) Q4 confirmed: lot-wide hold - reporter requests, QC head decides.
- (decision) Q11 confirmed: keep QC inbox group 'Sent on - finance or CEO'.
- (decision) Q12 and Q14 parked post-pilot: hold-for-buyer excess route; site-configurable receiving reason labels.
- (decision) Q5: finance damage outcomes = debit note to supplier, return to supplier for replacement, write-off, accept as-is with price reduction. Decision recorded in IMS, executed in ERP. Physical return handled on paper until Epic 20 gate passes.
- (decision) Q6: two requisition value bands at pilot: head of department up to Rs 1,00,000; finance controller above.
- (decision) Q7: assigner may revoke a standing approval alone and instantly (no finance approval); granting and revoking require a live connection.
- (decision) Q8: a concurrence can be withdrawn with a reason until both keys have turned; after both, the decision is locked and any correction is a new case linked to the old one (append-only).
- (decision) Q13: plate reading best effort and always human-confirmed; challan QR resolves offline against synced POs. Scanner make/model still unknown; one real device needed for testing before pilot.
- (decision) Q10: module visibility and menus derive from the access matrix permissions; handheld bottom bar shows 4 items per role plus More; no separate menu list.
- (decision) Q9: screens drawn without user input (picking, packing/dispatch, maintenance, manager overview) are validated by real users through the staging preview. Weighbridge weight capture is missing from the gate screen and must be added.
- (decision) All 14 EXPERIENCE.md open questions resolved 2026-09-25 (room recommendations accepted). Remaining actions: scanner make/model, weighbridge capture in gate mock.
- (event) Weighbridge added to gate mock and published 2026-09-25: Trucks on site list, weigh-in gross / weigh-out tare, AUTO or MANUAL kg reading, net vs PO line ordered qty with under/over tolerance, breach exception, overdue legal-metrology stamp blocks weighing, offline pending sync. Backend finding: weighbridge tolerance breaches target role 'receiving_supervisor' (src/compliance/weighbridge.ts:11, src/api/v1/weighbridge.ts:34) but the pilot roles file only has 'unloading_supervisor' (Mahesh Tyagi), so breach tasks would reach nobody at pilot. Gross and tare travel in ONE weighbridge.recorded event, so the gross reading waits on the device until tare.
- (event) Session paused at Finalize 2026-09-25 by user. Next: reviewer gate question (unanswered), fold weighbridge into spines, promote mocks, editorial passes, status final.
- (change) Weighbridge capture folded into EXPERIENCE.md (component pattern, stamp-overdue state, receiving_supervisor backend gap, remaining-action closed). Finalize resumed 2026-09-26; reviewer gate answered: rubric walker only.
- (decision) All 13 remaining [ASSUMPTION] tags accepted as decisions 2026-09-26 (user bulk-accept): sync-badge spec, chip colour pairs, concurrence pill colours; stepper lock after submit, maintenance triage offline-disabled, lot-wide hold blocks Accept on good part, IRN missing blocks dispatch (park + billing notified), gate desktop photo upload/webcam, pack station USB scanner, maintenance handheld grouped list, manager overview under More + bell in user menu, approve-above-band forwards, mixed requisition splits per route with Ready-to-collect counter stock. High findings 1-2 fixed: weighbridge Key Flow added, match-card visual spec + token added; component names aligned (Scan bar, Inbox card, Standing approval chips, match-card token ref).
- (event) spines finalized 2026-09-26: rubric review (3 high fixed, 6 medium fixed, rest logged as accepted lows), all ASSUMPTION tags ruled, 6 numbered Key Flows with UJ ids (incl. new UJ-WEIGH-01 Devi flow, extended UJ-IND-01 Ramu flow, Meera named in flow 6), match-card/weighbridge/composed visual specs added, mocks promoted .working to mockups (key-shopfloor stays retired in .working), links rewritten, DESIGN tables captioned Table 1-3, editorial passes done, status final. Reports: review-rubric.md, validation-report.md/.html. Open outside spine: scanner make/model, 6 backend stories, uncommitted work.
