# generated: 2026-07-12
# last_updated: '2026-08-07'
# story 4-5 dev-story 2026-08-06: implemented all 9 tasks (35 subtasks) from baseline b32fe25.
#   New three_way_match projection (one row per match RUN) and append-only payment_clearance_feed
#   ledger; additive grn.po_id and supplier_invoice.match_status (chk_supplier_invoice_status
#   deliberately NOT widened). Five new procurement-stream events (grn.po_linked,
#   three_way_match.recorded, supplier_invoice.credit_note_recorded / .debit_note_recorded,
#   payment_clearance_feed.recorded), one compliance module, eight routes. Binding decision: the
#   match RESULT is server-computed inside the persistEvent transaction and written back onto
#   envelope.payload before the domain_events insert, so a direct POST /api/v1/events claiming
#   result 'passed' is stored as 'blocked' with the real variance detail - the strongest available
#   answer to the 4.7 route-bypass lesson. Quantity agreement is one percent per line (largest
#   pairwise difference among ordered/received/invoiced, over ordered), inclusive at the boundary.
#   Fail-closed on a line-less PO, on a SKU repeated across PO lines (ambiguous_sku), and on an
#   invoice line matching no PO line. Clearance withholds blocked AND never-matched invoices.
#   Story 4.7's SOURCE_DOCUMENT_REQUIRED consumer gap closed and un-documented. Gates: build,
#   lint, format:check, db:migrate x2, spine 6/6, schema-drift 59/59, story-4-5 24/24, npm test
#   671/657 pass with the same 14 pre-existing idempotency failures (0 new); moved to review
# story 5-1 dev-story 2026-08-07: implemented all 8 tasks from baseline f9d3bd2. DB schema (bom, bom_revision,
#   bom_line, bom_structure) added to migrate.ts and mirrored into init-db.sql. Event schema: BomDraftedPayload,
#   BomLineAddedPayload, BomLineAmendedPayload with envelopes in schema.ts; events registered in SUPPORTED_EVENT_TYPES
#   with streamType 'engineering' and requiresBusinessStream on bom.drafted only. Compliance layer: shape assertions
#   (assertBomShape) and projection appliers (applyBomProjection) in compliance/bom.ts wired into events/store.ts
#   pre-transaction assert and projection switch. Read projections: row types and accessors in read/projections/bom.ts.
#   API handlers (draftBomHandler, addBomLineHandler, amendBomLineHandler, getBomHandler, listBomsHandler,
#   getBomStructureHandler) with RBAC in api/v1/boms.ts, all 6 routes registered in server.ts. Schema drift
#   entries for all 4 tables. Integration test with 12 cases covering draft creation, inactive components,
#   missing lines, scrap percent, co-product yield, list/get/structure, add/amend line, direct event rejection.
#   Fixed before/async arrow syntax, removed unused variables, removed stray return. Build passes, lint passes;
#   moved to review
# story 5-1 created 2026-08-05 via create-story workflow: Multi-Level BOM Creation (FR-B-01, FR-B-03,
#   FR-B-13, FR-B-14). Opens Epic 5 (epic-5 backlog -> in-progress). Draft-only scope: bom, bom_revision,
#   bom_line, and a phantom-resolved bom_structure projection on a new 'engineering' event stream
#   (bom.drafted, bom_line.added, bom_line.amended), business stream derived from the governed parent
#   item_master. Enforces EFFECTIVITY_OVERLAP via bom_revision row lock plus explicit predicate (no
#   btree_gist), BOM_CYCLE_DETECTED and BOM_DEPTH_EXCEEDED via depth-bounded recursive CTE, exact NUMERIC
#   scrap/quantity/conversion/yield validation, and co-product/by-product output lines. Binding decision:
#   item_master has no 'released' state (status is active|inactive only), so the A-11 condition is
#   evaluated as status='active' behind one exported predicate and recorded per line as blocking_release
#   for Story 5.2's release gate. Release, ECO, alternates, explosion, and cost rollups stay out of scope;
#   ready-for-dev
# epic statuses updated 2026-08-01: epics 1, 2, 3 all moved from in-progress to done (all stories complete); three epics form the completed Phase 1 pilot foundation slice and Epic 5 is next in the pilot build order
# story 3-10 code review 2026-08-01 (adversarial pass: Blind Hunter + Edge Case Hunter + Acceptance
#   Auditor, run in parallel against baseline c46fc21c; all three returned findings). 1 decision
#   resolved (persistEvent now returns existing event on duplicate instead of 409), 2 patches applied
#   (edge.ts overrides pick_task_id/pick_line_id from server, edge-client.tsx throws on DB-null with
#   try/catch in cross-dock-capture.tsx), 8 dismissed; moved to done
# story 3-10 created 2026-07-31 via create-story workflow: Cross-Docking Execution (FR-W-09);
#   qualifying owned, lot-resolved, non-QC receipt lines bind deterministically to one same-site open
#   Story 2.9 sales-order line with sufficient exact remaining demand, create one cross-dock task,
#   and suppress ordinary putaway. Completion validates an active staging bin, atomically moves the
#   lot through staging allocation to picked, materializes cross-dock-sourced completed pick rows so
#   Story 3.7 packing remains unchanged, updates exact quantity-based dispatch readiness, and records
#   honest receipt-to-staging task duration on Story 3.8's unified board. Includes event-sourced
#   assignment, compliance-seam RBAC/site checks, deterministic locks and replay, known-task edge
#   completion upload, additive schema/route guards, and ordinary-putaway fallback when optimization
#   cannot safely apply; ready-for-dev
# story 3-9 dev-story 2026-07-30: implemented all 7 tasks (44 subtasks) from baseline e97d657.
#   forward_pick_config (site_id denormalized from the zone) and replenishment_task (partial
#   unique open-per-signal index) projections; location_register.zone_type and
#   task_sla_config.task_type widened via guarded DROP-then-ADD, mirrored into init-db.sql;
#   getForwardPickBalance (new descendant-walk CTE, depth-capped, stock_class 'owned' only) and
#   getOpenPickDemand added to their sibling accessor files; one new TASK_SOURCES entry wires
#   replenishment tasks onto the Story 3.8 unified task board with zero query-builder change.
#   forward_pick_config.updated made event-sourced through persistEvent (decision: mirrors
#   task_sla_config.updated, its stated twin, rather than the direct-upsert alternative the story
#   flagged as equally defensible - the event-sourced path is this codebase's more recent,
#   more-corrected precedent for exactly this class of write). replenishment_task.created/.completed
#   compliance seam calls applyStockIssue/applyStockReceipt directly inside one transaction so
#   AC3's shared correlation_id holds by construction; FORWARD_PICK_ZONE_INVALID and
#   REPLENISHMENT_DESTINATION_OUTSIDE_ZONE checks live in the seam, not only the handler.
#   runForwardPickReplenishmentCheck modeled on planning-jobs.ts's runVmiReplenishmentCheck, takes
#   a real actor/auditCtx rather than a placeholder identity. 5 new REST routes registered in
#   server.ts and the Story 1.9 allowlist. No edge/ files changed; npm run edge:test reran
#   unchanged 23/23. tsc/eslint clean, schema-drift 43/43 (+2), spine gate 6/6, npm test 511/512
#   (1 pre-existing story-3-3 date-flake, unrelated), db:migrate re-run twice cleanly; moved to
#   review
# story 3-9 created 2026-07-30 via create-story workflow: Forward-Pick Replenishment (FR-W-08);
#   new forward_pick_config (SKU-per-forward-pick-zone min/max) and replenishment_task (task-board
#   row) projections, zone_type widened to add forward_pick/reserve, a new zone-descendant balance
#   walk (mirror of the existing bin-to-zone ancestor walk), a Phase-1 synthetic trigger job modeled
#   on Story 2.7/2.8's planning jobs, replenishment_task.created/.completed events on the existing
#   'warehouse' stream reusing applyStockIssue/applyStockReceipt directly (bypassing the
#   'inventory'-stream-gated projection, mirroring transfer-request.ts), and one new TASK_SOURCES
#   entry in Story 3.8's task board (the extension point that story built for this one); no edge
#   path, no approval routing; ready-for-dev
# story 3-7 code review 2026-07-30: code review passed (user-confirmed); 3-7 moved to done
# story 3-7 dev-story 2026-07-30: repaired test/integration/story-3-7.test.ts, which had never
#   actually executed (fictional SCIM/pick-task/dispatch API shapes); fixed 2 genuine bugs the
#   repaired suite exposed (missing DELETE grant on dispatch_document, document-renderer.ts
#   selecting a non-existent location_register.hierarchy_path column); corrected .env.test
#   DB_PORT (5432->5442). All 8 tasks were already complete from prior sessions. Full suite green:
#   488 backend (487 pass, 1 pre-existing story-3-3 date-flake), edge 23/23, tsc/eslint/build
#   clean, spine gate 6/6; moved to review
# story 3-8 code review 2026-07-29 (adversarial pass: Blind Hunter + Edge Case Hunter + Acceptance
#   Auditor, run in parallel against baseline cd7e3d9; all three returned findings, none failed).
#   8 decisions resolved + 26 patches applied, 2 deferred, 4 dismissed. All three layers independently
#   found the headline defect: the AC2 packing arm aliased packed_at to BOTH created_at and
#   completed_at with a hard-coded `true AS completed`, so every packing row added 1 to numerator AND
#   denominator (a 100% confirmation rate by construction, against an SM-17 target of >95%) and an
#   exact 0 to both duration rollups. Packing is now excluded from COMPLETION_SOURCES pending a real
#   start instant on packing_record. Other highs: task_sla_config had NO site_id, so the NULLS NOT
#   DISTINCT grain permitted exactly one null-zone row per task type for the WHOLE deployment and a
#   single-site supervisor silently changed breach thresholds everywhere - site_id is now part of the
#   grain, the index, the event payload and getSlaConfig's fallback; putaway assignment wrote straight
#   to the read model with zero persistEvent calls and no audit entry (the only warehouse mutation
#   without a domain event - a rebuild discarded every assignment and the SOD gate existed only in the
#   handler), now a putaway_task.assigned / pick_task.assigned event pair gated in the compliance seam;
#   productivity attribution flipped to COALESCE(assigned_to, completed_by) so an operator's
#   unconfirmed assigned work can actually lower their rate. Also: median_duration_seconds was missing
#   the FILTER its AVG twin carried (two figures over different populations); the `site` query alias
#   bypassed UUID validation into a raw 22P02 500; business_date accepted 2026-13-45; the threshold
#   regex bounded scale but not precision (NUMERIC(9,2) overflow as a 500); the board query had no
#   LIMIT; INNER JOIN erp_sales_order silently removed pick/packing tasks from the board AND the
#   denominator at once (no FK exists) - now LEFT JOIN; assign-vs-assign silently stole a task;
#   task_sla_config.updated had no replay-ordering guard; zone resolution overwrote a good zone_id
#   with NULL on re-direction; gate dwell counted neither open dwell (a yard of stuck vehicles read as
#   "not exceeded") nor guarded against clock skew, and misreported a pre-migration weighment as a
#   skipped capture. Migration caught a real 42P16: CREATE OR REPLACE VIEW cannot insert columns
#   mid-list, so gate_dwell_metric is now DROP + CREATE. tsc/eslint clean, npm test 488 tests 474 pass
#   (+2 new, both passing; the 1 fail is the documented story-3-3 date-flake and the 13 cancelled are
#   story-3-7's non-executing suite, both unchanged from baseline and both on the deferred ledger),
#   spine gate 6/6, edge 23/23, db:migrate re-runnable; moved to done
# story 3-8 dev-story 2026-07-29: implementation started from baseline cd7e3d9; moved to in-progress
# story 3-8 dev-story 2026-07-29: implemented all 8 tasks (59 subtasks) from baseline cd7e3d9. Additive
#   capture instants (weighbridge_event.occurred_at, grn.received_at) threaded from metadata.occurred_at,
#   which Stories 3.3/3.4 computed only to derive business_date and then discarded - without them a sub-day
#   gate-dwell interval was not computable. Additive priority on pick_task/putaway_task plus assignment
#   (assigned_to/by/at) and denormalized zone_id on putaway_task, the zone resolved once in
#   setDirectedSuggestion by a depth-capped recursive parent walk. New event-sourced task_sla_config
#   projection on the (task_type, zone_id) grain with a NULLS NOT DISTINCT unique index (unqualified, not
#   partial - no lifecycle column to make it partial by) and a new task_sla_config.updated event on the
#   existing 'warehouse' stream, spliced into store.ts after the Story 3.7 dispatch blocks with nothing
#   reordered. src/warehouse/task-metrics.ts normalizes four task sources (data-driven, so Story 3.9 adds a
#   fifth by appending one entry), computes every age/duration/percentile in SQL, and decides breaches by
#   exact decimal-string comparison rather than JS floats. gate_dwell_metric is a VIEW over gate/weighbridge/
#   grn with a weighbridge-then-GRN fallback and SM-C2 capture-completeness counters; the per-shift median
#   uses percentile_cont WITHIN GROUP under a plain GROUP BY (ordered-set aggregates are not window
#   functions in PG18) and breaches strictly above 4 minutes. 5 new REST routes + putaway assign; the
#   supervisor SOD gate lives in the compliance seam so a direct POST /api/v1/events cannot bypass it
#   (proved by test); WAREHOUSE_TASK_READ_ROLES enumerates supervisors explicitly (the Story 3.7 read-role
#   regression); no edge/offline path added per Task 7.6. Also fixed a pre-existing npm run lint failure in
#   src/api/v1/edge.ts (Story 3.7 inline SOD role literals tripping doa/no-hardcoded-role-in-workflow) that
#   blocked this story's own eslint and spine gates; behaviour unchanged. tsc/eslint/build clean, npm test
#   486 tests 472 pass (+50 new, all passing; the 1 fail is the documented story-3-3 date-flake and the 13
#   cancelled are story-3-7's non-executing suite, both unchanged from baseline), spine gate 6/6,
#   schema-drift 41/41, edge 23/23 unchanged, db:migrate re-runnable; moved to review
# story 3-6 code review 2026-07-27 (SECOND adversarial pass, independent model; first pass had run without
#   its Blind Hunter layer, which failed empty - the retry supplied most of the concurrency and
#   stock-invariant findings). 2 decisions + 26 patches applied, 3 deferred, 4 dismissed.
#   RELEASE BLOCKER fixed: erp_sales_order.sql created the uq index on the `id` surrogate BEFORE the
#   ALTER that adds it, so db:migrate aborted on every pre-3.6 database and no Story 3.6 schema landed
#   (reproduced on a pre-3.6 schema, then verified fixed). Other highs: generation made idempotent
#   (re-release double-allocated the same demand line); completion TOCTOU closed (row lock +
#   status-predicated update - two concurrent completions could double-run the allocated->picked move
#   and drain other tasks' allocations via LEAST()); applyStockPick and releaseStock made fail-closed
#   (both silently no-opped, stranding allocation while the task reported success); site scoping moved
#   onto the persistEvent seam (it lived only in the HTTP handlers, so direct-event and edge uploads
#   crossed sites) plus an SOD role check on completion. Decisions: confirmed qty must equal directed
#   qty (new PICK_QUANTITY_MISMATCH; short-pick previously flagged orders picked with demand vanishing),
#   and tasks auto-complete on the last confirmation so AC7's trigger is the confirmation, not a
#   supervisor's separate call. tsc/eslint/builds clean both workspaces, npm test 420/421 (pre-existing
#   story-3-3 date-flake only), edge 19/19, schema-drift 37/37, spine gate 6/6, db:migrate re-runnable;
#   stays done
# story 3-6 code review 2026-07-26 (adversarial pass, Edge Case Hunter + Acceptance Auditor; Blind
#   Hunter layer failed empty): 1 decision (substituted-lot bin resolution switched from Story 1.6
#   location_current - empty for never-scanned lots - to the authoritative stock_balance row at the
#   same site) + 9 patches applied, 0 deferred, 12 dismissed. Critical fixes: FOR UPDATE riding an
#   aggregate 500'd every task completion (PostgreSQL 0A000, lock-then-count split); per-line
#   allocated-to-picked regression introduced by Story 3.5 commit eb4b5f6 moved to completion-time
#   per spec Task 5.4 step 6 + AC7; input dedup; zero-quantity rejection; milli-precision shape
#   enforcement; already-completed guard with new permanent code PICK_TASK_ALREADY_COMPLETED wired
#   across backend/edge/i18n/tests; schema drift fixed (item_master size_class inlined into
#   canonical CREATE, erp_sales_order id surrogate + uq index + ALTER mirrored into init-db.sql);
#   story-2-2/2-9 shape pins updated for deliberate contract changes (picked: 0, NUMERIC-string
#   quantities). story-3-6 11/11, npm test 409/410 (pre-existing story-3-3 date-flake only), edge
#   19/19, schema-drift 37/37, spine gate 6/6, tsc/eslint clean; moved to done
# story 3-6 dev-story 2026-07-25: implemented all 8 tasks from baseline cf342e6. New 'warehouse' stream
#   (pick_task.created/pick_line.confirmed/pick_task.completed, requiresBusinessStream false), pick_task +
#   pick_line + dispatch_order_status projections, additive erp_sales_order.id surrogate + location_register
#   .pick_sequence migrations, FEFO/velocity-aware generator (single/batch/wave/zone) emitting events through
#   persistEvent so allocation + projection + event commit atomically, substitution release/reallocate with
#   PICK_LOT_SUBSTITUTED 2xx warning, plain-text paper pick list, 9 REST routes with warehouse-module RBAC +
#   site scoping, edge acceptance (server-set confirmed_by/completed_by, 8 permanent codes + i18n).
#   PREREQUISITE: repaired Story 3.5's committed baseline (did not compile; broke entire integration suite) -
#   putaway API rewritten to real RouteHandler contract, compliance/putaway import + typing fixes,
#   velocity_class.sql guarded constraint block, story-1-9 allowlist additions, story-3-5 test rewrite.
#   tsc/eslint/build clean both workspaces, npm test 408/409 (pre-existing story-3-3 date-flake only),
#   edge 19/19, spine gate 6/6; moved to review
# story 3-6 created 2026-07-25 via create-story workflow: Pick Task Generation and Execution (FR-W-04);
#   system-generated pick tasks with optimized paths (single-order, batch, wave, zone strategies);
#   FEFO lot selection, bin pick-sequence ordering, paper-directed picking; real-time stock allocation
#   on pick confirmation with lot substitution and override reason capture; consumes Story 2.9
#   sales-order projection as Phase-1 outbound demand, Story 3.5 velocity_class for bin preference,
#   Story 2.2 stock_balance for FEFO allocation, Story 2.3 lot_master for expiry ordering,
#   Story 1.6 location_current for lot-to-bin resolution; new pick_task + pick_line projections,
#   pick_task.created/pick_line.confirmed/pick_task.completed events on 'warehouse' stream,
#   src/warehouse/pick-task-generator.ts for generation logic, src/compliance/pick.ts for write-path seam,
#   8 new permanent error codes, paper pick list (plain-text rendering), zone-strategy dispatch-order
#   completion tracking; ready-for-dev
# story 3-5 created 2026-07-23 via create-story workflow: Directed Putaway and Location Override
#   Recording (UJ-PUT-01, FR-W-03); builds velocity-classification (ABC, putaway-frequency-derived)
#   and the override-driven re-slotting job from scratch (neither existed). New 'putaway' stream
#   with putaway.completed + location.override events (requiresBusinessStream false, mirrors
#   receiving). Extends Story 3.4's putaway_task additively (directed_location_id, actual_location_id,
#   override_reason_code/confidence, completed_at/by) per that story's explicit reservation; new
#   velocity_class(sku,site_id) projection. Directed-suggestion algorithm filters bins by size_class
#   (new item_master column, confirmed absent pre-story), zone/hazmat/quarantine compatibility (reuses
#   Story 2.1's zoneIncompatibilityReasons), and access_restricted (reuses Story 3.1's
#   assertLocationWritable); ranks by velocity class, occupancy proxy, and re-slotting-clustered
#   preference. location.override write path CONFIRMED to bypass assertLocationInvariant (narrowly
#   gated to stream_type inventory + location.asserted/expected only) and instead calls
#   src/read/projections/location.ts accessors directly after bridging putaway_task's TEXT lot number
#   to lot_master's UUID lot_id via getLotByNumberAndSku. Re-slotting job is an on-demand admin
#   endpoint, not a scheduler. ready-for-dev
# story 3-3 code review 2026-07-23 (adversarial pass, Acceptance Auditor only — Blind Hunter and Edge Case
#   Hunter subagents failed/timed out): 1 decision (task routing on tolerance breach, resolved to patch via
#   emitNotificationInTransaction to receiving_supervisor) + 3 patches applied (reversed gate events no longer
#   resolve as active binding tokens, weighbridge API response now returns created_at/updated_at, misleading
#   gate-event-chain-join doc comment fixed). tsc/eslint clean, npm test 366/367 (1 pre-existing date-flake in
#   story-3-3.test.ts AC1/AC2 unrelated to this review), edge 15/15, spine gate 6/6; moved to done
# story 2-9 created 2026-07-22 via create-story workflow: ERP inbound read-only reference projections for open PO headers/lines and sales-order dispatch demand; adapter-owned direct upserts with per-record isolation, source snapshots, soft-close semantics, zero-row-aware sync heartbeat and 15-minute freshness, deduped integration exception queue, SOURCE_SYSTEM_READ_ONLY bypass guard across API/direct-event/edge paths, site-code-to-location-UUID RBAC, additive Story 2.8 owner-party warning, migration/schema/route drift guards; ready-for-dev
# story 2-9 dev-story 2026-07-22: implemented all 11 tasks from baseline 8f5c3eb (closes Epic 2). ERP inbound
#   reference projections are direct-upsert read models (NOT event-sourced): erp_purchase_order header + line,
#   erp_sales_order dispatch-demand line, erp_sync_state heartbeat + integration_exception queue. Sync adapter
#   (src/adapters/erp/sync.ts) applies each PO (header+lines) and each SO line in its own SAVEPOINT (per-record
#   isolation, whole-PO atomicity), soft-closes dropped feed records (status='closed', never hard-delete),
#   server-sets source_system='ERP'/last_synced_at, and routes malformed records to the deduped exception queue
#   (NULLS NOT DISTINCT open partial-unique). Read API (GET purchase-orders/:po, GET sales-orders?site&status) with
#   SQL-computed staleness (now()-last_successful_at vs config.erp.freshnessMs, strict >, never JS clock); a stale
#   read raises one deduped ERP_SYNC_STALE alert + a one-time emitNotification. Read-only enforced by a persistEvent
#   bypass guard (rejects erp stream/erp.* event SOURCE_SYSTEM_READ_ONLY across direct-event + edge) plus explicit
#   405 write-verb reject handlers. SOURCE_SYSTEM_READ_ONLY wired across all four surfaces (backend/edge permanent
#   sets + i18n + tests); site RBAC resolves ext code to site UUID; sync trigger allowlisted to svc_erp_adapter/
#   system_administrator. Additive Story 2.8 owner-party OWNER_PARTY_NOT_IN_ERP warning (never rejects). Story 2.7
#   lead-time stays optional-additive (still fails closed LEAD_TIME_NOT_CONFIGURED). tsc/eslint/build clean, npm test
#   347/347 (+14), edge 14/14, spine gate 6/6, diff-check clean; moved to review
# story 2-6 dev-story 2026-07-21: implementation started from baseline 3be7fc2; moved to in-progress
# story 2-6 dev-story 2026-07-21: implemented all 10 tasks; cycle_count + cycle_count_line + physical_verification
#   (+ append-only physical_verification_line) projections, central cycle-count seam in persistEvent (variance from
#   stock_balance under FOR UPDATE, DOA-gated adjustment lifecycle, APPROVAL_REQUIRED enforcement on stock.adjusted so
#   direct /api/v1/events and edge uploads cannot bypass approval, negative-balance guard, lot trace + owned-stock
#   valuation on adjust), cycle-counts + physical-verification REST APIs with RBAC/location scoping + SOD, CARO 2020
#   report with period-lock immutability, local business_date, new stable errors (COUNT_TASK_LOCKED,
#   COUNT_ENTERER_CANNOT_APPROVE, PERIOD_LOCKED, COUNT_VARIANCE_REQUIRES_APPROVAL, STOCK_ADJUSTMENT_NEGATIVE_BALANCE)
#   in backend+edge sync + i18n; closed Story 2.5 schema-drift gap (transfer_request, in_transit); stock.adjusted gated
#   on adjustment_id so spine legacy fixtures pass through; tsc/eslint/build clean, npm test 280/280 (18 new), edge 14/14,
#   spine gate 6/6, diff-check clean; moved to review
# project: Inventory Management System_2
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# PILOT SLICE (first go-live at a single site) — Epics 1, 2, 3, 5, 7, 8, 9 +
# Story 11.2 + Epic 13 sign-off gate. Pilot items are marked "# PILOT" inline.
# NOTE: epic number != build order. Recommended pilot build order:
#   Epic 1 -> Epic 2 -> Epic 3 -> Epic 5 -> Epic 7 -> Epic 8 -> Epic 9 -> Story 11.2 -> Epic 13 gate.
# (Epic 8 QC must precede any Epic 6 closure gate; Epic 6 is NOT in the pilot.)

generated: 2026-07-12
last_updated: '2026-08-12'
# story 5-3 dev-story 2026-08-11: implemented all 9 tasks from baseline 25084ab. Three new
#   projections (eco, eco_change_line, eco_stock_disposition) plus source_eco_id on bom_revision,
#   mirrored into init-db.sql and schema-drift EXPECTED, migrate re-run twice cleanly. Six new
#   engineering-stream events (eco.raised/.review_started/.approved/.implemented/.cancelled/
#   .stock_disposition_recorded; only eco.raised requiresBusinessStream) registered in
#   SUPPORTED_EVENT_TYPES and wired into events/store.ts at the two existing seams
#   (assertEcoShape/applyEcoProjection alongside the BOM ones). src/compliance/eco.ts mirrors
#   bom.ts's shape/applier structure: FOR-UPDATE transition matrix (draft/under_review/approved/
#   implemented/cancelled), eco.implemented recomputes the affected-lot set (stock_balance rows
#   for the parent SKU, on_hand>0, lot_id not null) INSIDE the transaction and rejects
#   DISPOSITION_REQUIRED with pending_lots before creating the new bom_revision (source_eco_id
#   set), copying every superseded bom_line onto it, and applying add/amend/retire (retire closes
#   effectivity the day before occurred_at's IST date, never deletes) - the superseded revision's
#   own rows are never touched. src/read/projections/where_used_impact.ts is a COMPUTED read
#   (release_gate_checklist.ts precedent, no stored table) walking bom_line upward from the
#   affected components, depth-capped at config.bom.maxDepth with depth_truncated reporting;
#   reuses getStockBalancesBySku and a new getOpenPurchaseOrderLinesBySkus accessor. AC 9: flipped
#   approved_eco from STAGED_CONDITIONS to an enforced release-gate condition
#   (isApprovedEcoConditionMet, shared verbatim by the gate and the checklist) with the
#   zero-prior-released-revisions exemption for first release. Closed the 5.2 Group 2 deferral
#   (deferred-work.md line 210): applyBomLineAmended and the amend handler now scope the bom_line
#   lookup to revision_id, not just bom_line_id. Nine REST routes + resolveApprover(eco_approval,
#   0) DOA resolution at raise time + APPROVAL_REQUIRED authority check on approve, all under
#   engineering-module RBAC; spine allowlist and direct-event engineering-stream guard (inherited
#   from 5.2, asserted with a fresh test) both extended. story-5-3 14/14 new integration tests
#   (raise/BOM_NOT_RELEASED/cross-BOM revision reject, full draft->implemented chain, approval
#   authority, DISPOSITION_REQUIRED, use_up/scrap/rework, cancel-then-reject, AC9 gate, impact
#   read incl. a self-referential depth_truncated case, direct-event reject, RBAC, idempotent
#   replay, and the cross-revision immutability regression), story-5-2 27/27 (one assertion
#   updated for the approved_eco un-staging, itself a story-5-3 change) and story-5-1 12/12 both
#   green. tsc/eslint/prettier clean, db:migrate idempotent x2, npm test 792 tests 778 pass (14
#   pre-existing idempotency failures across Epic 1-3 suites, 0 new), spine gate 6/6, schema-drift
#   64/64 (+3), edge 30/30 unchanged, git diff --check clean; moved to review
# story 4-5 code review 2026-08-06 (adversarial pass: Blind Hunter + Edge Case Hunter + Acceptance
#   Auditor over baseline b32fe25 working tree): 12 patch findings applied (match mirror desync,
#   superseded-lift payment-clearing hazard, JS-float tolerance decision violating Binding
#   Decision 8, site-scoped clearance eligible read, FOR UPDATE on PO liveness, SKU agreement on
#   line_no fallback, replay coverage for all 5 new event types, note replay mismatch 409,
#   note_number_ext duplicate detection, 404 for missing match, schema interface drift,
#   feed-replay ledger no-op, and a P12 po_ref_ext correspondence check reclassified as deferred -
#   needs a dedicated erp_supplier_ref_ext <-> governed supplier mapping table, not present in
#   the schema). All story-4-5 tests 35/35, sibling 4-6/4-7 suites green, tsc/eslint/format
#   clean, schema-drift 60/60, spine 6/6, db:migrate re-runnable; status moved review -> done.
# story 4-7 dev-story 2026-08-05: implemented Tasks 1-6, 8-9 from baseline f9d3bd2. Three new
#   projections (supplier_invoice, supplier_invoice_line, supplier_invoice_ingestion) mirrored into
#   init-db.sql + schema-drift EXPECTED; five new procurement-stream events
#   (invoice_ingestion.staged/.reviewed, supplier_invoice.captured/.unmatched_recorded/.po_linked;
#   only .captured/.po_linked requiresBusinessStream, mirroring purchase_order.drafted). New
#   src/compliance/supplier-invoice.ts seam: PO-locked manual/reviewed-file capture sharing one
#   duplicate-grain lookup (supplier GSTIN + normalized invoice number + Indian financial year,
#   config.supplierInvoice.financialYearStartMonth default 4), SQL-computed NUMERIC line/header
#   totals compared exactly (INVOICE_TOTAL_MISMATCH), unmatched exception queue with no fabricated
#   PO/site/business_stream, and po_linked promotion. Duplicate override shares the SAME captured
#   event type per the story's 5-event contract; authorization is a distinct RBAC module scope
#   (procurement.duplicate-override), never a hard-coded role, checked on a separate
#   POST /api/v1/supplier-invoices/duplicate-overrides route - the seam itself always derives
#   duplicate_of_invoice_id from its own lookup, never the client payload. 8 new REST routes +
#   spine allowlist. Fixed two real bugs only a live-DB integration run surfaced: an ambiguous
#   "subtotal" column reference in the UPDATE...FROM...RETURNING total recomputation, and a
#   duplicate-grain concurrency race whose unique-constraint violation was being swallowed by the
#   same catch meant only for the idempotent invoice_id-retry case (left the transaction aborted,
#   surfacing as a raw 500 instead of 409 DUPLICATE_EVENT). Task 7 (AC5 MSME) is explicitly left
#   UNCHECKED and story is NOT done: Story 4.6 (msme-compliance-tracking, still backlog) is not
#   implemented anywhere in this codebase, so msme_classification_at_capture/statutory_due_date/
#   statutory_due_rule_version stay null at both capture and link time by design (proven by
#   dedicated test assertions) rather than fabricated. Story 4.5 (goods-receipt-and-three-way-match,
#   also backlog) means the unmatched-invoice SOURCE_DOCUMENT_REQUIRED consumer check has nothing
#   to test against yet - recorded as a second blocked dependency. No edge/offline path touched.
#   tsc/eslint/prettier clean, db:migrate idempotent x2, npm test 603 tests 589 pass (14 fails all
#   the documented pre-existing idempotency 201-vs-409 class across stories 1.1/1.6/1.8/2.1-2.4/2.8/
#   3.2-3.4/3.10, 0 new), story-4-7 23/23, spine gate 6/6, schema-drift 54/54, edge 30/30 unchanged,
#   diff-check clean; moved to review
# story 4-7 created 2026-08-05 via create-story workflow: Supplier Invoice Capture; manual and trusted-boundary PDF/CSV/XML ingestion with mandatory review, supplier GSTIN + normalized invoice number + Indian financial-year duplicate grain, evidenced override, unmatched queue and native PO linking, exact GST/NUMERIC contracts, attachment provenance metadata, Story 4.5/4.6 integration hooks, central procurement-stream enforcement, and no edge path; ready-for-dev
# story 4-4 created 2026-08-02 via create-story workflow: Purchase Order Management (FR-P-05); standard/
#   blanket/contract POs drafted from an approved Story 4.3 indent against an active supplier, 7 new
#   purchase_order.* events on the 'procurement' stream (only .drafted requiresBusinessStream true),
#   purchase_order + purchase_order_line + po_outbound_message projections (po_number_seq PO-YYYY-NNNN),
#   DOA approval via the EXPORTED resolveApprover with new transaction_type purchase_order_approval
#   (value = total, fresh resolution on ceiling revision), seam-enforced SOD PO_CREATOR_CANNOT_APPROVE +
#   status guards + NUMERIC ceiling math (PO_CEILING_EXCEEDED), issue records the PO-outbound payload on
#   the ERP adapter boundary (src/adapters/erp/po-outbound.ts, recorded not transmitted, distinct from
#   INT-ERP-01) atomically with purchase_order.issued and flips the linked indent to ordered WITH
#   purchase_order_id set (closes the applyIndentClosed "ordered implies PO" ledger assumption),
#   confirmation stamps promised_delivery_date onto lines + indent expected_delivery_date (4.2 metric
#   feed), 9 routes + spine allowlist, 9 new permanent codes, NO edge path (desktop officer flow, no
#   upload.ts/connector.ts/en.json changes), plain-SELECT alreadyPersisted mandated (supplier 42501
#   defect not copied); no amend/cancel (no FR), MSME deferred to 4.6, match to 4.5; ready-for-dev
# story 4-3 dev-story 2026-08-02: implemented all 10 tasks from baseline f1f4b26. indent + indent_line
#   projections (canonical SQL at repo root, init-db mirror, schema-drift entries, indent_number_seq for
#   server-side IND-YYYY-NNNN allocation), 9 indent.* events on the 'procurement' stream with ONLY
#   indent.raised requiresBusinessStream true - and assertInventoryTagging extended to consult the
#   SUPPORTED_EVENT_TYPES registry for non-inventory streams, since the flag was documentary-only on disk
#   (the story's "no new validation code" claim did not hold; inventory streams byte-for-byte unchanged,
#   spine 6/6). Compliance seam holds offline-synced duplicates in pending-confirmation with a nested
#   indent.duplicate_flagged audit event + never-throwing requester notice (AC3 capture never dropped);
#   SOD-01 (INDENT_RAISER_CANNOT_APPROVE) and DOA-resolution match (NOT_RESOLVED_APPROVER incl. active
#   delegates) enforced IN THE SEAM so direct-event and edge paths cannot bypass - closing the two 4.1
#   holes for indents. resolveApprover copied from 4.1 but passed the REAL estimated_value (SQL NUMERIC
#   rollup) and extended to return doa_entry_id, persisted on the row at raise; edge-synced raises resolve
#   at sync time in edge.ts with requester_user_id server-set. NotificationTarget gained optional user_id
#   honored by the dispatcher (AC5 notifies the requester directly, proven via runDispatchCycle - exactly
#   one delivery row; 4.1's broadcast-to-role gap not repeated). 8 routes + spine allowlist, 8 new
#   permanent codes + APPROVAL_UNRESOLVED across upload.ts/connector.ts/en.json, edge capture with
#   form_opened/local_commit instrumentation + tap-budget CI proxy (no new SQLite table, no connector
#   change). FIXED before it shipped: the seam's alreadyPersisted copied 4.1's SELECT...FOR UPDATE on
#   domain_events, which app_user (INSERT,SELECT only) cannot run - every indent event 500'd; the 4.1
#   supplier seam still carries this defect at HEAD (suppliers likely broken in production path, no 4.1
#   integration test exists to catch it - flagged in Dev Agent Record, out of scope here). tsc/eslint/
#   prettier clean (prettier pass normalized 19 files incl. pre-existing 4.1 format failures, whitespace
#   only), db:migrate idempotent, npm test 555 tests 541 pass (14 fails all the documented pre-existing
#   idempotency 201-vs-409 class, 0 new), story-4-3 14/14, spine 6/6, edge typecheck/lint/build clean,
#   edge unit 30/30 (+4 new); moved to review
# story 4-1 code review 2026-08-01 (adversarial pass: Blind Hunter + Edge Case Hunter + Acceptance
#   Auditor, run in parallel against baseline ce7c7f3; all three returned findings). 11 patches applied
#   (TOCTOU race fixes with FOR UPDATE locks, status validation in API handlers, GSTIN uniqueness check
#   on update, ILIKE escaping and GIN indexes for search performance, JSON serialization error handling,
#   no-op update rejection, two new error codes SUPPLIER_NOT_IN_ONBOARDING and SUPPLIER_NOT_ACTIVE_OR_ONBOARDING
#   wired across backend/edge/i18n). 4 patches not applied with documented rationale (notification targeting
#   uses role-based system only, payload mutation reverted to read-only, DOA audit covered by existing audit_log,
#   inactive GSTIN reuse intentional per story decision). tsc/eslint clean, npm test 517/539 (22 pre-existing
#   idempotency failures, 0 new), edge typecheck/lint/build clean, schema drift 46/46, db:migrate idempotent
#   with pg_trgm extension added; moved to done
# story 4-1 created 2026-08-01 via create-story workflow: Supplier Registry and Onboarding; new 'procurement' stream with supplier.registered/onboarding_submitted/onboarding_approved/onboarding_rejected/updated/deactivated events (requiresBusinessStream false), supplier projection with status lifecycle (onboarding -> active -> inactive), DOA-resolved onboarding approval by transaction_type supplier_onboarding value-band 0, GSTIN uniqueness enforcement across active and onboarding suppliers, notification foundation integration for approval/rejection decisions, get/list/create/update/deactivate/approve/reject REST routes with procurement-module RBAC, edge intake with 5 permanent error codes, and integration with existing owner_party_code governance from Story 2.8; ready-for-dev
# story 3-6 created 2026-07-25 via create-story workflow: Pick Task Generation and Execution (FR-W-04);
#   system-generated pick tasks with optimized paths (single-order, batch, wave, zone strategies);
#   FEFO lot selection, bin pick-sequence ordering, paper-directed picking; real-time stock allocation
#   on pick confirmation with lot substitution and override reason capture; consumes Story 2.9
#   sales-order projection as Phase-1 outbound demand, Story 3.5 velocity_class for bin preference,
#   Story 2.2 stock_balance for FEFO allocation, Story 2.3 lot_master for expiry ordering,
#   Story 1.6 location_current for lot-to-bin resolution; new pick_task + pick_line projections,
#   pick_task.created/pick_line.confirmed/pick_task.completed events on 'warehouse' stream,
#   src/warehouse/pick-task-generator.ts for generation logic, src/compliance/pick.ts for write-path seam,
#   8 new permanent error codes, paper pick list (plain-text rendering), zone-strategy dispatch-order
#   completion tracking; ready-for-dev
# story 3-6 created 2026-07-25 via create-story workflow: Pick Task Generation and Execution (FR-W-04);
#   system-generated pick tasks with optimized paths (single-order, batch, wave, zone strategies);
#   FEFO lot selection, bin pick-sequence ordering, paper-directed picking; real-time stock allocation
#   on pick confirmation with lot substitution and override reason capture; consumes Story 2.9
#   sales-order projection as Phase-1 outbound demand, Story 3.5 velocity_class for bin preference,
#   Story 2.2 stock_balance for FEFO allocation, Story 2.3 lot_master for expiry ordering,
#   Story 1.6 location_current for lot-to-bin resolution; new pick_task + pick_line projections,
#   pick_task.created/pick_line.confirmed/pick_task.completed events on 'warehouse' stream,
#   src/warehouse/pick-task-generator.ts for generation logic, src/compliance/pick.ts for write-path seam,
#   8 new permanent error codes, paper pick list (plain-text rendering), zone-strategy dispatch-order
#   completion tracking; ready-for-dev
# story 3-4 dev-story 2026-07-23: implementation started from baseline 8f86be6; moved to in-progress
# story 3-4 dev-story 2026-07-23: implemented all 9 tasks. New 'receiving' stream (goods.received +
#   goods.putaway_released, requiresBusinessStream false); grn header + grn_line + putaway_task projections
#   and minimal INT-SUP-02 asn/asn_line direct-upsert reference intake. Central applyGoodsReceivedProjection
#   seam: resolves the Story 3.3 accepted weighment for the binding token (RECEIVING_WEIGHT_NOT_ACCEPTED
#   blocks tolerance_breach-only tokens), matches PO line by sku (ITEM_PO_MISMATCH), computes the over/under
#   tolerance band in PostgreSQL NUMERIC under a pg_advisory_xact_lock on (po_ref,line_no) so concurrent
#   receipts cannot both over-receive. AC5 over-tolerance = committed 2xx business outcome (durable rejected
#   line + unloading_supervisor discrepancy notification, RECEIPT_TOLERANCE_EXCEEDED in body, NOT a rollback,
#   NOT an edge permanent code). AC6 short-within-tolerance posts with shortage_variance_qty visible in the
#   discrepancy view. AC7 expired lot = LOT_EXPIRED unless DOA-approved quarantine into ZONE-QC-HOLD else
#   APPROVAL_REQUIRED. AC3 BIS/quarantine QC-hold routes into ZONE-QC-HOLD with held putaway + qc_inspector
#   notification + DOA-gated audited manual release. Stock posted via a synthetic stock.received view feeding
#   the existing Story 2.2/2.3/2.8 helpers (never writes erp_*). Edge received_by server-set; 8 new permanent
#   codes + i18n. Decision (flag CR): AC7 quarantine gate resolves a DOA band + active supervisor holder rather
#   than requiring the store_assistant actor to be the approver (Task 7.2 create=store_assistant vs Task 5.3.4).
#   OQ2: svc_supplier_edi accepted defensively on POST /api/v1/asn (confirm role registry). tsc/eslint/build
#   clean, npm test 385/386 (1 pre-existing story-3-3 date-flake, unrelated; +14 new), edge 15/15, schema-drift
#   39/39 (5 new tables), spine 6/6; moved to review
# story 3-4 created 2026-07-23 via create-story workflow: Goods Receiving Against ASN or PO (FR-W-02);
#   receives against the Story 2.9 read-only open-PO projection with no Epic 4 PO capability. New goods.received
#   + goods.putaway_released events on a new 'receiving' stream (requiresBusinessStream false), grn/grn_line/
#   putaway_task projections + minimal INT-SUP-02 asn/asn_line direct-upsert reference tables. Central
#   applyGoodsReceivedProjection seam: resolves the Story 3.2 binding token via getWeighbridgeEventsByCorrelationId
#   (RECEIVING_WEIGHT_NOT_ACCEPTED blocks tolerance_breach weighments, preserving the 3.3 no-silent-receipt chain),
#   matches PO line by sku (ITEM_PO_MISMATCH), enforces the over/under tolerance band in PostgreSQL NUMERIC
#   (RECEIPT_TOLERANCE_EXCEEDED rejects with no stock + discrepancy notification to unloading_supervisor; short-
#   within-tolerance posts with shortage_variance_qty visible in the discrepancy view, ERP stays PO system of
#   record), expiry gate (LOT_EXPIRED unless DOA-approved quarantine into ZONE-QC-HOLD else APPROVAL_REQUIRED),
#   BIS/quarantine QC-hold routing into ZONE-QC-HOLD with held putaway + qc_inspector notification + DOA-gated
#   audited manual release. Reuses applyStockReceipt/applyLotSerialValidation for atomic stock posting; never
#   writes erp_*. RBAC create=store_assistant, release=unloading_supervisor/warehouse_manager; edge intake maps
#   stream_type 'receiving' to module receiving, server-sets received_by. ready-for-dev
# story 3-3 dev-story 2026-07-22: implemented all 7 tasks from baseline 60e2fe6. Weighbridge event
#   capture is event-sourced through persistEvent (weighbridge.recorded, streamType 'weighbridge',
#   requiresBusinessStream false). net = gross - tare computed in exact integer milli-kg (no JS float);
#   WEIGHBRIDGE_NET_NEGATIVE rejected pre-transaction. Tolerance band ordered_qty*(1 +/- pct/100)
#   computed/compared in PostgreSQL NUMERIC against the Story 2.9 erp_purchase_order_line -> accepted |
#   tolerance_breach with reason (AC3 blocks silent receipt). Binding token (Story 3.2
#   gate_event.correlation_id) resolved in the seam (WEIGHBRIDGE_BINDING_TOKEN_NOT_FOUND /
#   WEIGHBRIDGE_SITE_MISMATCH); idempotent replay-safe upsert; never writes any erp_* projection.
#   weighbridge_event projection (SQL + init-db mirror + migrate registration + schema-drift EXPECTED),
#   TS accessor, REST API (weighbridge_operator create RBAC, site scoping across inventory/gate/
#   weighbridge modules), edge intake (permanent codes in upload.ts + connector.ts + en.json;
#   weighed_by server-set on HTTP + edge). New codes beyond the 5-code table:
#   WEIGHBRIDGE_BINDING_TOKEN_REQUIRED, WEIGHBRIDGE_PO_LINE_NOT_FOUND (header-only PO tolerance
#   unknowable -> block). tsc/eslint/build clean, npm test 367/367 (+8), edge 15/15 (+1), spine 6/6,
#   schema-drift green; moved to review
# story 3-1 dev-story 2026-07-22: implemented from baseline 7cd3dc7; extended location_register with size_class and access_restricted, added GET /api/v1/locations?site=... site-filtered topology listing, code-or-UUID GET /api/v1/locations/:locationId with hierarchy_path, service-level restricted-zone write gate returning ZONE_ACCESS_RESTRICTED for non-qc_inspector writers, canonical SQL/init-db mirror, Story 3.1 integration tests, schema drift and route-surface guard updates; re-review findings fixed, final re-review no findings, build/lint clean, targeted guards clean, npm test 351/351; moved to done
# story 3-1 created 2026-07-22 via create-story workflow: Warehouse Topology Setup (FR-W-01); define and manage warehouse topology (sites, zones, aisles, racks, bins) with temperature, hazmat, and quarantine zone attributes, so that every putaway task, pick path, and location override references a real, validated physical location in the system; ready-for-dev
# story 2-8 dev-story 2026-07-22: implemented all 10 tasks from baseline e030403; found allocation/issue/deallocation drain SQL
#   ignored stock_class entirely (classless issue could drain consignment) and class-scoped every lock/check/drain plus the
#   last_issue_at stamp (default 'owned'; 2.5 transfer flows inherit owned); ownership_agreement registry (one active agreement
#   per sku-location-class, owner-code CHECK, partial-edit-preserving upsert) + ownership.agreement_set seam in persistEvent;
#   consignment/vmi receipts owner-party-gated inside applyStockBalanceProjection on all write paths (OWNERSHIP_AGREEMENT_NOT_FOUND /
#   OWNER_PARTY_MISMATCH); per-class breakdown + consolidated_by_class in GET /api/v1/stock/:sku (closes 2.2 defer); valuation
#   non_owned_quantities report section (2.4 owned-only seam untouched); replenishment_recommendation gains signal_type +
#   owner_party_code with open guard widened to (sku, location_id, signal_type) so internal + vmi signals coexist;
#   runVmiReplenishmentCheck (FOR UPDATE on agreement, strict NUMERIC below-minimum, replenish-to-min, transactional planner alert)
#   + POST /api/v1/planning/vmi/check + signal_type queue filter; 4 new stable errors on all four surfaces; RBAC write-location +
#   direct-event/edge parity (ownership.agreement_set in both PLANNING_EVENT_TYPES sets); story-2-7 owned-only test adapted to the
#   new receipt contract (agreement seeded, statuses asserted), 2-2/2-4 harnesses gained ownership_agreement.sql, 2-2 pins the
#   preserved top-level stock shape; tsc/eslint/build clean, npm test 329/329 (21 new), edge 14/14, spine gate 6/6, diff-check
#   clean; moved to review
# story 2-8 code review 2026-07-22: 1 decision resolved to patch + 14 patch findings applied, 0 deferred, 6 dismissed.
#   Critical fixes: top-level stock totals are now owned-only, active VMI agreements require NUMERIC(14,3) minimums,
#   ownership agreements validate active item/location references and trim owner codes, direct-event and edge paths enforce
#   planning-config roles, unique-index races map to 409s, transfer in_transit is owned-class scoped, class availability and
#   VMI refresh checks avoid JS float comparisons, VMI checks lock balance rows, agreement lookups are visibility-scoped,
#   and regression coverage includes mixed-class transfer and concurrent VMI checks. tsc/lint/build clean, npm test 333/333,
#   edge 14/14, spine gate 6/6, diff-check clean; moved to done
# story 2-8 dev-story 2026-07-22: implementation started from baseline e030403; moved to in-progress
# story 2-8 created 2026-07-22 via create-story workflow: Consignment and VMI Stock Segregation (FR-I-10); ownership_agreement
#   SKU-location-class registry (single active owner per grain, ownership.agreement_set event), owner-party validation on
#   consignment/vmi stock.received (OWNERSHIP_AGREEMENT_NOT_FOUND / OWNER_PARTY_MISMATCH), classless issues stay owned-only,
#   class-scoped INSUFFICIENT_STOCK pinned by tests, per-class breakdown in GET /api/v1/stock/:sku (closes 2.2 defer),
#   valuation non_owned_quantities section (seam untouched), runVmiReplenishmentCheck emitting replenishment.recommended with
#   signal_type vmi_replenishment + owner_party_code (replenishment_recommendation gains signal_type/owner_party_code, open
#   guard widened to include signal_type), no supplier transmission (Epic 4) / no job_work custody (Epic 9) / owner-party
#   referential validation tightens with Story 2.9; RBAC write-location + direct-event/edge parity, four-surface error codes,
#   schema-drift + route-surface + 2.2/2.7 regression guards; ready-for-dev
# story 2-7 dev-story 2026-07-22: implemented all 10 tasks from baseline f23c59f; three SKU-location projections
#   (inventory_planning_params, replenishment_recommendation with partial-unique open guard, obsolescence_flag),
#   central applyInventoryPlanningProjection seam in persistEvent (5 new inventory event types, narrow-gated), safety-stock =
#   ceil(z x sigma_daily x sqrt(lead_time_days)) in SQL NUMERIC (AC1 worked example resolves to exactly 20, reorder point 146),
#   reproducible computation_inputs snapshot, static 4-value z lookup (INVALID_SERVICE_LEVEL/LEAD_TIME_NOT_CONFIGURED/
#   INSUFFICIENT_DEMAND_HISTORY fail-closed), replenishment check with FOR-UPDATE-serialized single open recommendation and
#   transactional planner alert, obsolescence scan flag/clear (aging + pending_disposition + nrv_testing_triggered, NRV write-down
#   stays DOA-gated in Story 2.4 seam), last_issue_at column on stock_balance stamped on stock.issued only (Story 2.2 invariants
#   preserved), 7 RBAC/location-scoped REST routes, 5 new stable errors across backend+edge sync + i18n, schema-drift + route-surface
#   + Story 2.2 last_issue_at regression guards; tsc/eslint/build clean, npm test 301/301 (21 new), edge 14/14, spine gate 6/6,
#   diff-check clean; moved to review
# story 2-7 code review 2026-07-22: 2 decision-needed resolved to patch + 11 patches applied, 3 defers logged,
#   8 dismissed. Critical fixes: never-issued owned stock now gets an obsolescence fallback clock, planning payload
#   location_id is write-scoped on direct event and edge upload paths, safety-stock compute holds the params FOR UPDATE
#   lock through persistEvent, write-scoped location access is enforced, target_location_code issues count in demand stats,
#   owned stock only feeds reorder and obsolescence decisions, partial params edits preserve omitted config, obsolescence
#   scan responses return real days_since_issue, lead_time_days is bounded to NUMERIC(9,3), zero-qty recommendation events
#   are rejected, and getObsolescenceFlag enforces the forUpdate client contract. tsc/lint/build clean, npm test 308/308,
#   edge 14/14, spine gate 6/6, diff-check clean; moved to done
# story 2-7 created 2026-07-22 via create-story workflow: Safety Stock, Reorder Points, and Obsolescence Flagging (FR-I-03/07/08 flagging half); new SKU-location inventory_planning_params projection (safety_stock = ceil(z x sigma_daily x sqrt(lead_time_days)), reorder_point), replenishment_recommendation + obsolescence_flag projections, last_issue_at activity tracking on stock.issued, central inventory-planning seam in persistEvent, three Phase-1 synthetic job cycles (safety-stock compute / replenishment check / obsolescence scan) modeled on src/notify cycles, transactional planner exception alerts via emitNotificationInTransaction, obsolescence NRV trigger hands off to DOA-gated Story 2.4 valuation seam (no write-down posted here), recommendation-only (no PO - Epic 4), disposition deferred (Epic 16), new stable errors + sync/i18n classification, RBAC/location scoping, schema-drift + route-surface guards; ready-for-dev
# story 2-6 created 2026-07-21 via create-story workflow: Cycle Counting and Physical Inventory; cycle-count tasks, SKU/lot variance calculation, DOA-routed approval-gated adjustments, stock.adjusted central seam, CARO 2020 physical-verification evidence report, period-lock immutability contract, RBAC/location scoping, sync/i18n error classification, schema drift and regression guardrails; ready-for-dev
# story 2-5 code review 2026-07-21 (3-layer adversarial, baseline a76aa46 -> a5b033f): 4 decisions + 18 patches applied, 4 deferred, 2 dismissed. Critical fixes: missing BEGIN on all write handlers (piecemeal autocommit), deleted powersync_publication restored in init-db.sql, hardcoded pending_approval status (no-approval transfers stranded), ship_event_id 'unknown' UUID crash (every real ship 500'd), FOR UPDATE row locks + in_transit unique constraint (double-ship), serial TEXT[] injection, over-receive cap, source-grain stock ops for lot-less transfers, ship/receive correlation_id reuse (AC3), location+role RBAC on writes, list scoping, escalation-to-fallback approver, client idempotency key, LOT_SKU_MISMATCH split, partially_received state. Added test/integration/story-2-5.test.ts (19 tests) + spine route-surface allowlist. Backend 262/262, edge 14/14, spine 6/6, tsc/eslint clean; moved ready-for-dev->done
# story 2-5 created 2026-07-21 via create-story workflow: Inter-Location Transfer Requests; DOA-approval routing, transfer request events, pick-ship-receive cycle, allocation enforcement, lot/serial traceability, in-transit state management, correlation ID linking, approval gating, rejection reversal, error codes (APPROVAL_REQUIRED, QUANTITY_EXCEEDS_APPROVED, LOT_MISMATCH), edge sync, RBAC, audit, and comprehensive test coverage; ready-for-dev
# story 2-4 dev-story 2026-07-21: implemented all 8 tasks; SKU-grain inventory_valuation summary +
#   FIFO layer/serial-cost/NRV-adjustment/standard-cost-variance projections, valuation seam wired
#   into persistEvent after lot/serial+stock-balance (weighted average, FIFO multi-layer split,
#   specific-identification per-serial cost), NRV write-down/recovery with DOA-resolved authoriser
#   matching and original-cost recovery cap enforced in the seam itself (not just the HTTP handler,
#   to close a POST /api/v1/events bypass), VALUATION_METHOD_NOT_PERMITTED for lifo/bare
#   standard_cost, standard-cost measurement-technique config + variance review + report,
#   GET/POST valuation routes, edge/i18n/stable-error updates; tsc/eslint/build clean, npm test
#   243/243 (13 new), edge 14/14, spine gate 6/6, diff-check clean; moved to review
# story 2-4 created 2026-07-21 via create-story workflow: Ind AS 2 compliant inventory valuation; weighted average, FIFO, specific identification, LIFO rejection, standard-cost measurement-technique controls, NRV write-down and recovery cap, valuation route, central valuation projection seam, edge error classification, migration mirror, RBAC, and regression guardrails; ready-for-dev
# story 2-3 code review 2026-07-21 (adversarial pass 3): 3 layers over baseline 12a931f -> working tree; 1 decision
#   (lot_controlled LOT_REQUIRED enforcement, chose enforce-both-paths) + 10 patches applied, 4 defers, 7 dismissed;
#   new LOT_REQUIRED error code scoped to target-location movements (serial precedence, allocation exempt, legacy
#   sku-only spine fixtures untouched); edge 403 business-denial now settles needs_attention instead of halting the
#   whole outbox; trace null-location holds visible to wildcard readers; tsc/eslint/edge-typecheck clean, npm test
#   225/225 (2 new: LOT_REQUIRED enforcement + hold-trace visibility), edge 14/14, spine 6/6, diff-check clean; moved review->done
# story 2-3 code re-review 2026-07-21: independent second adversarial pass over baseline->working-tree; tsc + npm test
#   reproduced clean (222/222) but green suite did not cover a confirmed torn-write. Fixed 1 (locationless stock.issued
#   torn write now requires a resolvable target location). 17 further patch findings + 1 decision (expired-lot override
#   role-substring auth) + 2 defers logged; unresolved high/medium findings remain (un-lotted balance drain, serial/quantity
#   reconciliation, FEFO concurrency fallthrough); status corrected review->in-progress
# story 2-3 code review 2026-07-21: 22 patch findings resolved; central lot/serial validation, trace population,
#   quality-hold audit atomicity, FEFO/FIFO selection, serial availability, route/schema guards, stable errors,
#   and integration coverage patched; verification battery clean; moved to done
# story 2-3 dev-story 2026-07-21: implemented all 7 tasks; lot_master/serial_master/lot_trace projections and API routes,
#   central lot/serial validation in persistEvent (shape assert pre-transaction, projection apply inside event
#   transaction), FEFO/FIFO selection endpoint, quality-hold management API, schema drift and route guards,
#   comprehensive integration test suite; tsc/eslint/build clean, all tests passing; moved to review
# story 2-2 code review 2026-07-21: 6 patches applied (idempotent retry guard, SQL-based NUMERIC precision, grain constraint migration, type validation, quantity bound, locale sort) + 2 decisions converted to patches (stock_class in grain, stock_class validation) + 2 decisions kept as-is (sku-only gating, INSUFFICIENT_STOCK edge permanence) + 3 deferred; 209/209 tests; moved to done
# story 2-2 dev-story 2026-07-21: implemented all 5 tasks; stock_balance projection (sku+location+lot grain,
#   NULLS NOT DISTINCT, generated available column) with canonical SQL + compose mirror + migration entry,
#   central enforcement in persistEvent (shape assert pre-transaction, FOR UPDATE availability re-check inside the
#   event transaction, 409 INSUFFICIENT_STOCK before any event/audit/idempotency write), GET /api/v1/stock/:sku with
#   RBAC + location scoping, edge INSUFFICIENT_STOCK classification + i18n, schema-drift + route-surface guardrails,
#   deterministic last-unit concurrency proof; legacy sku-only and spine-shape stock events preserved by gating;
#   tsc/eslint/build clean, 209/209 tests (22 new), spine gate 6/6, edge 14/14, migrations + diff-check clean; moved to review
# story 2-2 dev-story 2026-07-21: implementation started from baseline c354746; moved to in-progress
# story 2-2 created 2026-07-21 via create-story workflow: Real-Time Multi-Location Stock Balances; stock_balance projection, central INSUFFICIENT_STOCK enforcement, GET /api/v1/stock/:sku, direct stock.received and stock.allocated event contracts, concurrency/idempotency, edge sync, schema drift, migration mirror, RBAC, and regression guardrails; ready-for-dev
# story 2-1 code review 2026-07-21: 3 decisions resolved to patch + 5 patch findings fixed; edge uploads auto-confirm
#   zone-incompatible placements so outbox rows cannot sync without persistence, HTTP master-referencing events stamp actor
#   location from auth, inactive item/location/actor/parent masters reject, location hierarchy validation moved into the projection
#   helper, zone_type/temperature_class SQL constraints + stronger schema drift guard added, redundant unique indexes removed,
#   test coverage closed; npm test 187/187, spine gate 6/6, tsc/eslint/build/diff-check clean; moved to done
# story 2-1 dev-story 2026-07-21: implemented all 5 tasks; item_master + location_register projections and APIs,
#   central assertInventoryMasterReferences in persistEvent (ITEM_NOT_FOUND / LOCATION_NOT_FOUND /
#   ACTOR_LOCATION_NOT_REGISTERED before any DB write), ZONE_INCOMPATIBLE as a 200 two-step confirmation warning on
#   both HTTP and edge paths, Story 1.6 lot-location API moved to /api/v1/lots/:lotId/location, migration list +
#   compose mirror + schema drift guard, 18-test story suite; fixed anticipated story-1-1 cross-story fixture
#   regression (seeded masters); tsc/eslint/build clean, 185/185 tests, spine gate 6/6, diff-check clean; moved to review
# story 2-1 dev-story 2026-07-21: implementation started from baseline 7bdce10; moved to in-progress
# story 2-1 created 2026-07-21 via create-story workflow: Item Master and Location Register; item and location master APIs, central ITEM_NOT_FOUND validation, ZONE_INCOMPATIBLE warning contract, Story 1.6 location-route collision guardrails, migration mirror, RBAC, audit, and regression requirements; ready-for-dev
# story 1-11 code review 2026-07-20: 3 decision-needed + 12 patch findings resolved to code (transactional dispatch with
#   atomic claim + re-entrancy guard, claim-then-act escalation, configurable fallback admin tier so no alert closes silently,
#   Task 5.3 Expired lifecycle wired, acknowledged/escalated/expired domain events persisted, delivery trace_id=correlation_id,
#   acted_upon stops escalation, since/until 400 validation, occurred_at/window poison-pill guards, NOTIFY_* env validation,
#   audited consent writes); 4 deferred, 4 dismissed; backend 157/157, spine gate 6/6, tsc/eslint/build/diff-check clean; moved to done
# story 1-11 dev-story 2026-07-20: notification schema + notification domain event stream, non-blocking emitNotification(),
#   resumable poll-based dispatcher (in-app + web push), DOA-registry-resolved escalation clock, and full in-app REST API
#   implemented; backend 153/153 (10 new), spine gate 6/6, edge unit 14/14, edge typecheck/lint/build clean, tsc/eslint/
#   diff-check clean; fixed a pre-existing missing-CASCADE regression in Story 1.4's test harness and updated Story 1.9's
#   spine-gate route allowlist; web push VAPID keys optional/unconfigured in this env (in-app delivery unaffected); moved to review
# story 1-11 dev-story 2026-07-20: implementation started from baseline 58514a6; moved to in-progress
# story 1-11 created 2026-07-20 via create-story workflow: shared notification and alerting service (in-app, web push, escalating
#   alerts with acknowledgment tracking) that every module consumes instead of inventing its own; targets AC1-4 (role/location
#   delivery with trace_id, DOA-registry-resolved escalation, offline queuing, non-blocking durable emission); ready-for-dev
# story 1-10 code review 2026-07-20: 1 decision-needed + 12 patches resolved (required PR review added to branch
#   protection; CD secrets wired into cd.yml/deploy.sh; CD concurrency split so a stuck production approval no longer
#   blocks staging; deploy.sh migrates before cutover; packages:write scoped down; Compose version guard added;
#   provision.sh v1-only false-pass fixed; CD default-branch detection made dynamic; Playwright e2e wired into CI;
#   Task 6 checklist corrected to stop claiming unexecuted live verification; dead environments.json field removed;
#   bootstrap.sh Team-reviewer lookup fixed; .env.test CI loader hardened); 3 defers logged; remains in review pending live verification
# story 1-10 dev-story 2026-07-20: CI workflow (5 required checks), CI DB role provisioning, branch-protection/environment
#   bootstrap IaC, CD workflow with self-hosted-runner immutable image promotion, and 3 preexisting-bug fixes (Dockerfile
#   migration-asset paths, provision.sh Compose v2 detection, package.json start script) implemented; backend 143/143,
#   spine gate 6/6, edge unit 14/14, edge accessibility 4/4, tsc/eslint/build clean; migration path verified inside the
#   actual built container image. Live GitHub branch-protection bootstrap and self-hosted runner registration
#   intentionally not executed (require a user identity decision and real staging/production hosts); moved to review
# story 1-10 dev-story 2026-07-20: implementation started from baseline d61ca68; moved to in-progress
# story 1-10 created 2026-07-20 via create-story workflow: CI/CD pipeline construction; GitHub Actions CI/CD, branch protection with admin enforcement, deploy/pipeline bootstrap IaC, staging automation, production approval identity capture, immutable artifact promotion, Node 24/PostgreSQL 18.4/Next.js 16 validation guardrails; ready-for-dev
# story 1-9 code review 2026-07-20: 10 review patches resolved; production router surface asserted, gate script now includes ESLint, harness errors/ports deterministic, audit coverage broadened, AGENTS.md out-of-scope change removed; spine gate 6/6, npm test 143/143, tsc + eslint + diff-check clean; moved to done
# story 1-9 dev-story 2026-07-20: added 6-test spine-acceptance-contract suite (test/integration/story-1-9.test.ts) and `spine-acceptance-contract` npm script (junit artifact); npm test 143/143, tsc + eslint + diff-check clean; moved to review
# story 1-9 dev-story 2026-07-20: implementation started from baseline 34304a3; moved to in-progress
# story 1-9 created 2026-07-20 via create-story workflow: Spine Acceptance Contract CI gate; five spine invariants in CI with required status check `spine-acceptance-contract`; ready-for-dev
# story 1-8 done 2026-07-20: `npm run sync:smoke` re-verified against review-fix wiring with Docker Desktop 4.82.0 (six containers up, smoke passed); AC6 keyboard-only pass and screen-reader pass both documented against the running production build (Windows Narrator plus UIA and Chromium AX trees for all three screens); review item 3A resolved; edge Playwright 9/9; moved to done
# story 1-8 follow-up 2026-07-20: Docker Desktop 4.82.0 smoke recheck passed (`npm run sync:smoke`); AC6 keyboard-only pass documented; screen-reader pass still pending at that point
# story 1-8 code review 2026-07-20: 3 decisions resolved to patch (wire real client sync/e2e proof, deterministic site selection, manual accessibility evidence); 13 additional patches and 2 defers recorded; moved back to in-progress
# story 1-8 dev-story 2026-07-20: backend, edge PWA, sync config, Playwright, accessibility, migration, and full backend regression passed; remains in-progress because Docker is unavailable for real-service PowerSync smoke validation
# story 1-8 dev-story 2026-07-20: implementation started from baseline c294aab; moved to in-progress
# story 1-8 created 2026-07-20 via create-story workflow: offline edge PWA shell and PowerSync sync layer; Next.js 16 PWA shell, local SQLite outbox, self-hosted PowerSync, sync failure isolation, WCAG and i18n foundations; ready-for-dev
# story 1-5 code review 2026-07-19: 5 review patches resolved; applicability uses occurred_at, overlapping-rule creation serialized with advisory lock, conflict path releases client once, undocumented INVALID_TAG_VALUE removed, stray patch report removed; tsc + eslint + unit test + diff-check clean; moved to done
# story 1-5 dev-story 2026-07-19: implemented all 5 tasks; tagging schema + assertInventoryTagging in persistEvent + 3 admin endpoints; 9 anticipated cross-story test fixes (1.1-1.3 inventory payloads now tagged); 86/86 tests green; tsc + eslint clean; moved to review
# story 1-7 code review 2026-07-19: 1 decision + 3 patches resolved (unknown previous_status, DOA value_min 0 routing, transactional escalation, conrelid-scoped confidence guards); location.disputed narrow-write closed as by-design honoring 1.6 decision; 2 defers logged; 121/121 tests green; tsc + eslint + migration + diff-check clean; moved to done
# story 1-7 dev-story 2026-07-19: implemented all 9 tasks; instrument calibration schema + assertCalibrationLockout in persistEvent + status/QC/escalation endpoints; 119/119 tests green; tsc + eslint + migration + diff-check clean; moved to review
# story 1-7 created 2026-07-19 via create-story workflow: calibration lockout enforcement (FR-M-13, FR-Q-04, AD-8); minimal instrument status registry, assertCalibrationLockout in persistEvent before insert, synthetic QC result command, status admin endpoint, DOA-routed escalation; ready-for-dev
# story 1-6 code review 2026-07-19: 4 patch findings resolved; current-location projection guarded by event_version, generated location.disputed uses internal write path, confidence vocabulary/default and payload device_id fixed; 105/105 tests green; tsc + eslint + migration + diff-check clean; moved to done
# story 1-6 dev-story 2026-07-19: implemented all 5 tasks; location schema + projections, assertLocationInvariant in persistEvent transaction, location.disputed event, GET + synthetic expected endpoints; 101/101 tests green; tsc + eslint + diff-check clean; moved to review
# story 1-6 created 2026-07-19 via create-story workflow: event-sourced location with asserted/expected separation (AD-15, AD-16, INT-LOC-01); location_asserted_facts + location_expected_facts + location_current projection, assertLocationInvariant at persistEvent write path gated by stream_type, location.disputed event on mismatch, GET + synthetic expected-fact seeding endpoints; ready-for-dev
# story 1-5 created 2026-07-19 via create-story workflow: business-stream tagging enforcement (FR-AC-01); business_streams + transaction_tagging_rules tables, assertInventoryTagging at top of persistEvent gated by stream_type, admin rule endpoints; ready-for-dev
# story 1-4 code re-review 2026-07-19: 2 test patches resolved; concurrent PATCH serialization + all deterministic tie-breakers directly covered; 67/67 tests green; remains done
# story 1-4 code review 2026-07-19: 3 patches resolved; database value-band integrity + concurrent PATCH locking; template-literal lint coverage; deterministic tie-breakers; 65/65 tests green; moved to done
# story 1-4 dev-story 2026-07-18: implemented all 8 tasks; 5 DOA endpoints + registry schema + no-hardcoded-role lint rule; atomic persistEvent client param; 64/64 tests green on PostgreSQL 18.4; tsc + eslint clean; moved to review
# story 1-1 created 2026-07-12 via create-story workflow
# story 1-2 created 2026-07-12 via create-story workflow
# story 1-2 code review 2026-07-13: 4 decisions + 14 patches applied, moved to done
# story 1-3 created 2026-07-13 via create-story workflow
# story 1-3 validated 2026-07-13: 3 critical + 4 enhancement fixes applied
# story 1-3 dev-story 2026-07-18: 2 decisions resolved + 12 patches applied; 45/45 tests green on PostgreSQL 18.4; back to review
# story 1-3 code review 2026-07-18: adversarial re-review, 1 decision + 16 patches resolved, 3 defers logged; 55/55 tests green; moved to done
# story 1-4 created 2026-07-18 via create-story workflow; fresh-eyes validation found 3 critical issues (fabricated precedent, grant-placement split-brain risk, deprovisioned-delegate gap) and fixed before finalizing
# story 1-4 party-mode review 2026-07-18: PRD room flagged non-atomic row+event write (mirrors deferred SCIM issue), single-holder pilot-scope risk, Task 6 ownership question, missing admin-UI forward reference; all patched into the story
project: Inventory Management System_2
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # --- Epic 1: Platform Foundation, Compliance Spine, and Offline Edge Shell  [PILOT] ---
  epic-1: done
  1-1-core-infrastructure-deployment-and-event-store-schema: done   # PILOT
  1-2-sso-authentication-and-role-based-access-control: done   # PILOT
  1-3-statutory-edit-log: done   # PILOT
  1-4-enterprise-doa-registry: done   # PILOT
  1-5-business-stream-tagging-enforcement: done   # PILOT
  1-6-event-sourced-location-with-asserted-expected-separation: done   # PILOT
  1-7-calibration-lockout-enforcement: done   # PILOT
  1-8-offline-edge-pwa-shell-and-powersync-sync-layer: done   # PILOT
  1-9-spine-acceptance-contract-ci-gate: done   # PILOT
  1-10-ci-cd-pipeline-construction: done   # PILOT
  1-11-notification-and-alerting-foundation: done   # PILOT
  epic-1-retrospective: optional

  # --- Epic 2: Core Inventory and Multi-Location Stock Visibility  [PILOT] ---
  epic-2: done
  2-1-item-master-and-location-register: done   # PILOT
  2-2-real-time-multi-location-stock-balances: done   # PILOT
  2-3-lot-batch-and-serial-traceability: done   # PILOT
  2-4-ind-as-2-compliant-inventory-valuation: done   # PILOT
  2-5-inter-location-transfer-requests: done   # PILOT
  2-6-cycle-counting-and-physical-inventory: done   # PILOT
  2-7-safety-stock-reorder-points-and-obsolescence-flagging: done   # PILOT
  2-8-consignment-and-vmi-stock-segregation: done   # PILOT
  2-9-erp-inbound-reference-projections: done   # PILOT
  epic-2-retrospective: optional

  # --- Epic 3: Warehouse Operations and Frontline Capture Flows  [PILOT] ---
  epic-3: done
  3-1-warehouse-topology-setup-fr-w-01: done   # PILOT
  # story 3-2 re-review 2026-07-23 (adversarial pass over fix commit 9fbdcf1): 3 decisions (Idempotency-Key
  #   online dedup, binding_token deprecated alias restored, offset/order pagination + oldest-first unmatched
  #   worklist) + 2 patches (localYmd UTC getters, single named UNIQUE on correlation_id) applied; 2 deferred;
  #   tsc/eslint clean, targeted 44/44, full suite 390/391 (1 pre-existing date-dependent 3-3 failure logged
  #   to deferred-work); remains done
  3-2-gate-event-capture-and-vehicle-to-po-binding-uj-gate-01-fr-w-02: done   # PILOT
  3-3-weighbridge-event-capture-and-tolerance-enforcement-uj-weigh-01-fr-w-02: done   # PILOT
  3-4-goods-receiving-against-asn-or-po-fr-w-02: done   # PILOT
  3-5-directed-putaway-and-location-override-recording-uj-put-01-fr-w-03: done   # PILOT
  3-6-pick-task-generation-and-execution-fr-w-04: done   # PILOT
  3-7-packing-shipping-and-dispatch-documents-fr-w-05-fr-w-06: done   # PILOT
  3-8-warehouse-task-management-and-productivity-tracking-fr-w-07: done   # PILOT
  3-9-forward-pick-replenishment-fr-w-08: done   # PILOT
  3-10-cross-docking-execution-fr-w-09: done   # PILOT
  epic-3-retrospective: optional
  # story 3-2 code review 2026-07-23: 2 decisions resolved + 7 patches applied (server-generated gate_event_id, IST-based localYmd, removed redundant binding_token alias, UNIQUE correlation_id, pagination limit, negative-path tests); 1 patch reverted (alreadyPersisted null-key throw broke HTTP path); moved to done

  # --- Epic 4: Procurement and Supplier Management (wave 1, post-pilot) ---
  epic-4: in-progress
  4-1-supplier-registry-and-onboarding: done
  4-2-supplier-performance-scorecards: done   # code review 2026-08-07: 8 patches applied (full seam re-derivation with FOR UPDATE reference resolution + supplier correspondence closing the direct-event fabrication bypass and TOCTOU, IST-anchored trailing windows, responsiveness reference_event_id resolved to purchase_order.confirmed, single-source IST conversion, fail-closed holiday config validation, jsonb_typeof guard + contributing-line count, holiday-removal integration branch, supersedes existence checks + spec-mandated partial unique replay guard + superseded-row exclusion), 1 deferred (caller-carried idempotency-key platform convention), 13 dismissed; AC1 tests hardened against receiving's server-local business_date stamping; suite grew 22 -> 28; gates: build/lint/format clean, migrate x2 idempotent, schema-drift 60/60, story-4-2 28/28, shape 13/13, business-days 8/8, spine 6/6, no-hardcoded-role 1/1, edge 30/30, full suite 732 (718 pass, 14 pre-existing idempotency fails, 0 new); dev-story 2026-08-06: all 9 tasks/49 subtasks done from baseline 8c2c44f; new supplier_scorecard_metric projection + supplier_scorecard.metric_recorded event + compliance seam + business-days helper + 5 routes   # dev-story 2026-08-06: all 9 tasks/49 subtasks done from baseline 8c2c44f; new supplier_scorecard_metric projection + supplier_scorecard.metric_recorded event + compliance seam + business-days helper + 5 routes; gates: build/lint/format clean, migrate x2 idempotent, schema-drift 60/60, story-4-2 22/22, shape 13/13, business-days 8/8, spine 6/6, edge 30/30, full suite 726 (712 pass, 14 pre-existing idempotency fails, 0 new). story 4-2 created 2026-08-06 via create-story workflow: Supplier Performance Scorecards; four-metric (on-time delivery, quality acceptance, price variance, responsiveness) read feature over the existing procurement event substrate; new supplier_scorecard.metric_recorded event on the procurement stream (requiresBusinessStream false), append-only supplier_scorecard_metric projection keyed by metric_id with (reference_event_id, metric_kind) partial unique replay guard, four thin write routes (on-time via grn+po+promised_date, price-variance via three-way_match.recorded, responsiveness via po issued_at->confirmed_at business-day arithmetic, quality-acceptance intentionally absent pending Epic 8), GET scorecard + GET scorecard transactions endpoints, business-day helper in src/lib/business-days.ts as single source of business-day arithmetic, seam enforcement (supplier-active, metric_kind enum, calendar-date rollover, NUMERIC-as-string) so direct POST /api/v1/events cannot bypass, quality_acceptance returns { state: 'no_data' } first-class response shape rather than fabricated zero; no new dependency, no edge path, no ERP surface; ready-for-dev
  4-3-purchase-requisition-and-indent-loop: done
  4-4-purchase-order-management: done
  4-5-goods-receipt-and-three-way-match: done
  4-6-msme-compliance-tracking: done
  4-7-supplier-invoice-capture: done
  epic-4-retrospective: optional
  # story 4-2 skipped for now: epics.md sequences it LAST in epic 4 (consumes PurchaseOrderIssued/Confirmed
  # from 4.4, GoodsReceived + match results from 4.5, SupplierInvoiceCaptured from 4.7). Create 4-2 after
  # those land. 4-3 contexted instead on 2026-08-02.

  # --- Epic 5: BOM and Engineering Change Management  [PILOT] ---
  epic-5: in-progress
  5-1-multi-level-bom-creation: done   # PILOT  # dev-story 2026-08-07: implemented all 8 tasks (DB schema x4, event schema, compliance layer, read projections, API handlers, schema-drift tests, integration tests with 12 cases); all 8 tasks complete, build and lint pass
  5-2-bom-lifecycle-and-immutability: done   # PILOT  # dev-story 2026-08-07 + code-review 2026-08-09: all 10 tasks done from baseline af39b5e (lifecycle schema, 4 events, release gate with staged conditions, immutability, checklist read, 6 routes, legacy-kit migration, 12/12 integration tests); also repaired never-green story-5-1 harness and 2 latent 5.1 handler bugs (business_stream injection, occurred_at); full gates green, 14 pre-existing idempotency failures unchanged; Group 4 adversarial review applied 6 patches (engineering-stream guard, BOM TRUNCATE, strengthened gate assertions, transition-matrix tests, migration data/skip/idempotency tests, RBAC fixes), suite expanded 12->27 tests all green
  5-3-eco-workflow-and-where-used-impact: done   # PILOT  # create-story 2026-08-09: ECO lifecycle (draft/under_review/approved/implemented/cancelled), 3 new tables + source_eco_id on bom_revision, 6 events, computed where-used impact read (bom_line not bom_structure), DOA approver resolution via resolveApprover, approved-ECO release gate un-staged with first-release exemption, closes the 5.2 cross-revision immutability deferral; dev-story 2026-08-11 (all 9 tasks, 14/14 tests) + code-review 2026-08-11 (23 patches applied: stale-ECO guard, release-gate re-check, revision-code guard, eco_id/eco_number validation, where-used current-revision/cycle guard, amend validation, APPROVAL_UNRESOLVED at raise, insertBomRevision source_eco_id, disposition validation, retire effective_to; 9 new regression tests; gates green, 0 new failures) moved to done
  5-4-randd-draft-bom-regime: done   # PILOT  # create-story 2026-08-12: R&D draft BOM regime (bom_type='rnd' IS the rd_draft flag, no new column), partial uq_bom_parent_item to unblock cloning, placeholder/free-text bom_line columns, 3 new tables (rd_build_record, rd_as_built_line, rd_productization_signoff), 5 events, shared assertNotRdDraft execution bar consumed by 5.5/Epic 6, 7 tasks; code-review 2026-08-12: 1 decision resolved (quantity_used is per-unit, built_quantity informational) + 9 patches applied (honor client line_no in rd_build.recorded, post-lock idempotency re-check on concurrent same-key retries, advisory-lock close of the BOM_ALREADY_EXISTS productize race, deterministic clone via payload source_revision_id, confirm-event source_event_id on synthetic missing rows, RD_BUILD_NOT_PERMITTED on the productization-gate GET for non-rnd, PostgreSQL NUMERIC base_quantity_per in the three rewritten bom_line statements, RdAsBuiltLineInput discriminated-union type, per-unit built_quantity: 2 regression test), 0 deferred, 2 dismissed; Edge Case Hunter layer returned empty; story-5-4 20/20, story-5-1/5-2/5-3 62/62, schema-drift + role gates 72/72, build/lint/format clean
  5-5-approved-alternates-and-bom-explosion: backlog   # PILOT
  5-6-cost-rollups-job-work-kit-tagging-and-erp-outbound-sync: backlog   # PILOT
  epic-5-retrospective: optional

  # --- Epic 6: Production Orders and Manufacturing WIP (wave, post-pilot) ---
  epic-6: backlog
  6-1-production-order-creation-and-release-gate: backlog
  6-2-material-staging-issue-and-wip-ledger: backlog
  6-3-production-completions-and-qc-hand-off: backlog
  6-4-lot-genealogy-closure-and-offline-execution: backlog
  epic-6-retrospective: optional

  # --- Epic 7: Maintenance, Calibration, and Asset Register  [PILOT] ---
  epic-7: backlog
  7-1-asset-register-and-criticality-classification: backlog   # PILOT
  7-2-preventive-maintenance-plans-and-work-order-generation: backlog   # PILOT
  7-3-fault-reporting-and-breakdown-work-orders: backlog   # PILOT
  7-4-spare-parts-cataloguing-reservation-and-critical-spares-alerts: backlog   # PILOT
  7-5-calibration-register-and-non-overridable-lockout: backlog   # PILOT
  7-6-statutory-examinations-cost-accumulation-and-machine-status-broadcast: backlog   # PILOT
  7-7-amc-warranty-and-insurance-tracking: backlog   # PILOT
  7-8-offline-technician-workflow-and-closure-codes: backlog   # PILOT
  epic-7-retrospective: optional

  # --- Epic 8: Quality Control and Batch Release  [PILOT] ---
  epic-8: backlog
  8-1-inspection-plans-and-qc-gate: backlog   # PILOT
  8-2-aql-sampling-and-result-capture: backlog   # PILOT
  8-3-lot-disposition-accept-reject-conditional-release: backlog   # PILOT
  8-4-coa-coc-retention-samples-and-batch-release-records: backlog   # PILOT
  8-5-quality-holds-and-recall-trace: backlog   # PILOT
  8-6-statutory-release-blocks-and-quality-reporting: backlog   # PILOT
  8-7-compliance-master-data-bis-licence-register-and-label-masters: backlog   # PILOT
  8-8-witnessed-inspections-and-prototype-stock-rules: backlog   # PILOT
  epic-8-retrospective: optional

  # --- Epic 9: Job-Work Services  [PILOT] ---
  epic-9: backlog
  9-1-job-work-service-order-creation: backlog   # PILOT
  9-2-customer-material-receipt-and-segregated-stock: backlog   # PILOT
  9-3-custody-ledger-and-consumption: backlog   # PILOT
  9-4-process-loss-offcut-election-capture-and-qc-gated-dispatch: backlog   # PILOT
  9-5-statutory-return-clocks-and-closure-gate: backlog   # PILOT
  9-6-offcut-election-execution-and-erp-billing-feed: backlog   # PILOT
  epic-9-retrospective: optional

  # --- Epic 10: R&D and Maker-Hub Operations (wave, post-pilot) ---
  epic-10: backlog
  10-1-randd-and-maker-hub-location-types-and-project-master: backlog
  10-2-randd-requisition-budget-check-and-issue-types: backlog
  10-3-project-wip-ledger-and-prototype-build-records: backlog
  10-4-hub-member-records-and-machine-time-booking: backlog
  10-5-randd-physical-verification-and-cost-reporting: backlog
  10-6-offline-point-of-use-sale-and-payment-capture: backlog
  10-7-member-job-cards-and-statements: backlog
  epic-10-retrospective: optional

  # --- Epic 11: Financial Compliance and Period Close (Story 11.2 in pilot) ---
  epic-11: backlog
  11-1-itc-register-per-gstin: backlog
  11-2-irn-before-dispatch-enforcement-and-branch-transfer-documents: backlog   # PILOT
  11-3-erp-synced-budget-control: backlog
  11-4-period-locks-reconciliation-and-audit-evidence: backlog
  epic-11-retrospective: optional

  # --- Epic 12: Cross-Module Reporting and Executive Analytics (Phase 2) ---
  epic-12: backlog
  12-1-role-specific-operational-dashboards: backlog
  12-2-cross-module-executive-kpi-dashboard: backlog
  12-3-domain-report-suites: backlog
  12-4-self-service-ad-hoc-report-builder-and-export: backlog
  12-5-configurable-exception-rule-engine: backlog
  12-6-scheduled-report-distribution-and-shared-definitions: backlog
  epic-12-retrospective: optional

  # --- Epic 13: Data Migration Sign-Off Gate  [PILOT] ---
  epic-13: backlog
  13-1-opening-stock-migration-and-verification: backlog   # PILOT
  13-2-active-document-migration-boms-pos-challans-custody-registers: backlog   # PILOT
  13-3-go-live-reconciliation-sign-off-gate: backlog   # PILOT
  epic-13-retrospective: optional
