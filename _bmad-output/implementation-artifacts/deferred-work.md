# Deferred Work

Triaged 2026-09-04 by a party-mode round table (Winston, Amelia, Murat, John, Grumbal, Ravi) over a nine-agent code survey of every entry. Line numbers in the Ref column point at the pre-triage ledger at commit 3868f81 (working tree of 2026-09-04) for traceability. The verdict counts in Table 1 cover all 484 entries.

Table 1: Triage totals

| Verdict | Count | Meaning |
| --- | --- | --- |
| Fix batch | 33 | Approved small fixes, applied to the working tree on 2026-09-04 (uncommitted) |
| Decided | 15 | Decision recorded, implementation is a story or a bundled commit |
| Open | 104 | Still deferred with a named trigger |
| Awaiting decision | 1 | Needs a human with information the room did not have |
| Resolved | 53 | Already closed in code, never struck from the ledger |
| Killed | 253 | Will not be done; reason recorded |
| Folded | 25 | Duplicate of another entry, closed with it |

## Fix batch

Table 2 lists the approved fixes. All were applied to the working tree on 2026-09-04 in one pass (uncommitted, alongside the 9-5 work); the 409 canonical status for APPROVAL_UNRESOLVED was propagated to every seam that still raised 404 (custody-ledger, maintenance-coverage, maintenance-sync-conflict, master-data, production-completion, quality).

Table 2: Approved fix batch

| Ref | Story | Item | Fix |
| --- | --- | --- | --- |
| 11 | 9-3 | `story-8-5.test.ts` asserts `not_yet_covered` count shrank but never asserts what the remaining item is - deferred, minor test-robustness gap | test/integration/story-8-5.test.ts:966 - add one line after it: `assert.ok((coverage['not_yet_covered'] as string[])[0]!.includes('production genealogy'));` (value from src/quality/recall-trace.ts:193). |
| 16 | 9-3 | `where_used` array not globally chronologically sorted across production and custody sources [src/quality/recall-trace.ts] - deferred, minor readability gap | src/quality/recall-trace.ts:95 (production, ORDER BY :92) then :125 `whereUsed.push(...)` (custody, ORDER BY :121) - two pre-sorted lists concatenated. One line before :196: `whereUsed.sort((a, b) => a.occurred_at.localeCompare(b.occurred_at));` (ISO strings sort lexically). |
| 39 | 8-8 | Round 2: `WITNESS_HOLD_POINT_NOT_FOUND` (a 404) sits in AUDITED_REJECTIONS while `LOT_NOT_FOUND` on the raise route does not - probing unknown hold-point ids... | Not-found probes are never audited, house-wide. Remove WITNESS_HOLD_POINT_NOT_FOUND from AUDITED_REJECTIONS (quality.ts:319). |
| 64 | 1-3 | Archival retention math uses calendar-year subtraction, not financial-year boundaries [src/cli/archive-audit-log.ts] - deferred, pre-existing open question i... | Retention cutoff is the Indian financial year (1 April), not calendar year. Fix src/config/audit.ts:40-45. |
| 94 | 1-10 | Runtime database role passwords (`app_user`, `readonly_user`, `replication_user`, `svc_powersync`) are created with committed default passwords rather than d... | Compose fails closed: drop every ${VAR:-default} password fallback in deploy/compose/docker-compose.yml; dev copies .env.example. |
| 104 | 1-11 | Notification schema is duplicated across `deploy/compose/init-db.sql` and `read/projections/notification.sql` with only a "change both together" comment and ... | Add three EXPECTED entries (`notifications`, `notification_deliveries`, `push_subscriptions`) to `test/unit/schema-drift.test.ts` in the shape of `:359-364`; constraint/index names are in `read/projections/notification.sql`, mirrors exist at `init-db.sql:587,623,707` |
| 142 | 3-2 | `entered_at` is client-supplied with no upper bound and is the gate-event list sort key; a future-dated or bad-clock row pins to the top of every list. Pre-e... | `src/compliance/gate.ts:78` after the ISO check add `if (Date.parse(entered_at) > Date.now() + 5*60_000) throw AppError(400,'INVALID_PARAMS','entered_at is in the future')` (4 lines) |
| 144 | 3-2 | Story 3-3 (surfaced by this review's full-suite run, pre-existing): `src/compliance/weighbridge.ts:30` has its own `localYmd` using pure local getters with n... | Test half done (`story-3-3.test.ts:321` derives via `localYmd()`); replace `src/compliance/weighbridge.ts:32-37` and `story-3-3.test.ts:27-32` bodies with the IST-shift from `gate.ts:29-34` (8 lines, 2 files) |
| 178 | 3-7 | Lot-lock ordering can deadlock two concurrent orders sharing lots - both LOT_ON_HOLD queries lock candidate lots via `FOR UPDATE OF lm` with no deterministic... | `src/compliance/dispatch.ts:451-456` add `ORDER BY lm.lot_id` before `FOR UPDATE OF lm`; the helper queries at `:40,:65` already order, this dispatch-time one does not |
| 237 | 4-5 | Client-controlled `occurred_at` reaches statutory timestamps [src/compliance/three-way-match.ts:344 and 400] - deferred, pre-existing platform-wide event-sou... | Server bounds occurred_at to now plus 5 minutes platform-wide at store.ts:442; no lower bound because offline uploads are legitimately old. |
| 256 | 5-2 | `correlation_id` passed unvalidated into a UUID column (PG 22P02 500 on non-UUID) [src/compliance/bom.ts:706] - deferred, pre-existing pattern at bom.ts:816 ... | bom.ts:988 and :1093 still write `p.correlation_id ?? null` with no isUuid check in the payload validators. Diff: in the bom.drafted and bom.migrated validators (style of bom.ts:186) add `if (p['correlation_id'] != null && !isUuid(p['correlation_id'])) reject('INVALID_PARAMS', 'correlation_id must be a UUID');` (2 lines, reuses existing isUuid). |
| 277 | 5-6 | `getLatestCompleteRollup` and `getLatestRollup` are dead code and diverge from the live gate predicate `evaluateCostRollupCondition`, which runs its own stal... | bom_cost_rollup.ts:183-211 exported, zero callers in src or test (grep). Diff: delete both functions. |
| 280 | 5-6 | `emitBomSyncConflictEvents` swallows every `persistEvent` rejection with a silent catch, hiding shape-assert defects in the emission path [src/api/v1/erp-pro... | erp-projections.ts:335 `.catch(() => undefined)`. Diff: `.catch((err) => console.warn('bom.sync_conflict_raised emit failed', err))` (console.warn is the existing api/v1 convention, e.g. compliance.ts, edge.ts). Same one-liner applies to the sibling swallows at 103, 186, 207 if desired. |
| 286 | 7-1 | NUL byte in text fields or the search parameter surfaces as a raw 500 (unmapped SQLSTATE) [src/events/store.ts:875-1101] - deferred, pre-existing platform ma... | Only 23505/23514/22003 are mapped (store.ts:1229, :2120, :2163); src/middleware/error.ts maps no pg codes. Add a sibling branch next to store.ts:2163: `if (err.code === '22021' \/\/ err.code === '22P05') throw new AppError(400, 'INVALID_PARAMS', 'text contains a NUL byte or invalid encoding')` (~6 lines, one file). |
| 372 | 7-6 | `findRoleHolder` selects only the earliest-assigned active holder of a role (ORDER BY created_at ASC LIMIT 1), so when a role has several active holders only... | Any active holder of the DOA role may sign. Replace the LIMIT 1 pick at doa_registry.ts:193 with a membership check. |
| 378 | 7-6 | `APPROVAL_UNRESOLVED` is raised with 404 by the Story 7.6 handler and seam (no DOA entry governs the transaction type, per the story's Table 6) but with 409 ... | APPROVAL_UNRESOLVED is 409 everywhere. |
| 388 | 7-6 | `OVERDUE_SCAN_DATE` is hardcoded to a date roughly ten months in the future, so re-stamp rows are written with `examined_on` in 2027 and `recorded_at` at wal... | Derive OVERDUE_SCAN_DATE from today in test/integration/story-7-6.test.ts:140; it is a June 2027 time bomb. |
| 417 | 6-1 | `APPROVAL_UNRESOLVED` is raised with 404 by the release handler when no DOA entry governs `production_order.release_override`, but with 409 by the shared `re... | 409 canonical; align production-orders.ts:653 with the line 378 fix. |
| 437 | 7-7 | `chk_asset_coverage_dates` is `expiry_date > start_date`, strictly greater, so a one-day insurance cover note with `start_date = expiry_date` cannot be recor... | Same-day cover is real (transit cover notes). Constraint becomes expiry_date >= start_date; seam at maintenance-coverage.ts:166 follows. |
| 448 | 7-7 | `business_date` is documented as a filter on the cross-asset coverage list but `coverageStatusFilter` returns early when `status` is absent and never reads i... | src/api/v1/maintenance.ts:4005-4028: move the `business_date` read + `isValidCalendarDate` 400 (lines 4022-4026) above the `if (status === null) return` at line 4011 so a malformed date is rejected regardless of `status`. One function, ~4 lines moved. |
| 485 | 6-2 | The staging `lines[]` array is unbounded, so one event can run hundreds of descendant walks plus inserts and allocations in a single transaction (request amp... | `src/compliance/production-material.ts:219`: after the empty check add `if (lines.length > 500) reject('INVALID_PAYLOAD', 'lines is capped at 500 entries')`, mirroring the 8.2 500-reading cap |
| 512 | 7-8 | Sync-conflict read surface (`GET /api/v1/maintenance/sync-conflicts`) is unscoped and returns `conflicting_payload` company-wide; whether the supervisor queu... | Scope the sync-conflict queue by permittedLocationsForModuleScope read at maintenance.ts:4477. |
| 544 | 8-2 | `format:check` reports four files untouched by this story (src/compliance/weighbridge.ts, src/read/projections/asset.ts, src/read/projections/instrument_cali... | Verified today: `npx prettier --check` still warns on all 4. Run `npx prettier --write src/compliance/weighbridge.ts src/read/projections/asset.ts src/read/projections/instrument_calibration_escalation.ts src/read/projections/instrument_register.ts` in a housekeeping commit |
| 628 | 8-4 | Node clamps a `setInterval` delay above 2^31-1 to 1 ms with only a `TimeoutOverflowWarning`, turning a mistyped interval env var into a tick storm; Story 8.4... | Helper already supports it: parsePositiveIntEnv(name, fallback, max) at src/config/index.ts:53, MAX_INTERVAL_MS at :82, already passed for retention (:624), BIS (:643), jobwork (:729). Add `, MAX_INTERVAL_MS` as 4th arg to the three notify timers at src/config/index.ts:324, :329, :332. |
| 705 | 8-7 | Two concurrent `bis_licence_recorded` events with different licence numbers but overlapping windows for the same sku/site can both pass `requireNoOverlap` an... | src/compliance/master-data.ts:402 and :452 - insert `await client.query('SELECT pg_advisory_xact_lock(hashtext($1))', [sku + '/' + (siteId ?? '')])` immediately before each requireNoOverlap call (appliers already run in-transaction). |
| 712 | 8-7 | GET-by-id handlers raise `BIS_LICENCE_NOT_FOUND` / `LABEL_MASTER_NOT_FOUND`, both members of `AUDITED_REJECTIONS`, but do not audit; PATCH audits the same co... | Same policy as line 39: drop NOT_FOUND codes from the audited list in compliance.ts:52-55. |
| 738 | 8-8 | The three pre-existing `schema-drift` failures in this working tree (`compliance_bis_licence`, `label_master`, `gate_dwell_metric`) are line-ending artifacts... | Add .gitattributes with *.sql text eol=lf and renormalize deploy/compose/init-db.sql in one commit. |
| 765 | 9-5 | The `custody.return_recorded` uom gate (src/compliance/custody-ledger.ts:1139) reads the earliest custody row for the sku with `ORDER BY occurred_at ASC LIMI... | src/compliance/custody-ledger.ts:941 and :1176 (loss gate and return gate share the query): append `AND ownership = 'customer'` to both WHERE clauses. Column vocab is customer/processor (custody_ledger_entry.sql:57). |
| 778 | 9-5 | The Story 9.5 suite still carries pre-existing absolute challan dates (`2026-02-15`, `2026-01-10`, `2026-03-10`) asserted against a live sweep, which become ... | test/integration/story-9-5.test.ts:946 `'2026-01-10'` to `shiftDays(TODAY, -120)`, :947 `'2026-03-10'` to `shiftDays(TODAY, -60)`, :1534 `'2026-02-15'` to `shiftDays(TODAY, -100)`. All stay outside the 90-day alert window; FIFO order preserved. The module-load TODAY midnight flake is a KILL. |
| 780 | 9-5 | The sweep alerts on clocks belonging to CLOSED orders (src/notify/jobwork-clock-sweep.ts): the closure gate checks the custody balance, consumption into WIP ... | Due query joins service_order and filters status open; closed orders never alert. |
| 785 | 9-5 | `reportSiteScope` returns the silent empty report its docstring says it prevents: a jobwork reader with no location assignments gets a 200 with zero rows and... | service-orders.ts:1000: before `return scope.wildcard ? null : [...scope.locations];` add `if (!scope.wildcard && scope.locations.size === 0) throw new AppError(403, 'LOCATION_ACCESS_DENIED', 'No read assignment grants access to any site');` (mirrors :991-996). |
| 786 | 9-5 | `totals.challans` on the ITC-04 report counts clock rows, not challans. One clock exists per receipt and one receipt per GRN line, while `challan_number_ext`... | service-orders.ts:1087 `challans: rows.length` to `challans: new Set(rows.map((r) => `${r.service_order_id}\/${r.challan_number_ext}`)).size`; challan_number_ext is NOT NULL (jobwork_material_receipt.sql:28) and on the row (:306,:320). |
| 793 | 9-5 | The sweep resolves `today` once per tick while `now` is taken per row, so rows processed after IST midnight are decided against the previous calendar date bu... | src/notify/jobwork-clock-sweep.ts: move `const now = new Date().toISOString();` from :150 (inside the row loop) to :127 beside `const today`. Uses at :153, :178, :212, :238 unchanged. |

## Decided, implementation pending

Table 3 records decisions the room took. Each becomes a backlog story or a bundled commit; none should be re-argued.

Table 3: Decisions taken

| Ref | Story | Item | Decision |
| --- | --- | --- | --- |
| 15 | 9-3 | `total_customer_balance` sums closing balances across all SKUs regardless of UOM into one number [src/compliance/custody-statement.ts:135] - deferred, matche... | Statement total becomes one line per UOM; the unit-less grand total is dropped. Ravi: kg plus pieces is not a number. |
| 138 | 2-9 | Dropped PO lines persist as phantoms: soft-close is header-only; erp_purchase_order_line has no status column and the app role has no DELETE grant on it (del... | Add a line status column (dropped) on erp_purchase_order lines; no DELETE grant. Small story alongside the next ERP inbound change. |
| 151 | 3-4 | Under-receipt below the lower tolerance bound is unenforced (`under_receipt_tolerance_pct` fetched but unused). A symmetric-reject patch was attempted and re... | Under-receipt check fires when the ERP PO status arrives as closed. Wire it when the 2-9 closure feed exists. |
| 208 | 4-4 | PO write routes (approve/reject/issue/confirm/releases/ceiling) perform no site-write authorization [src/api/v1/purchase-orders.ts:349-592] - deferred, pre-e... | All five procurement route files get assertSiteWriteAccess on writes. One platform story: procurement write-site guard. |
| 215 | 4-7 | DATE columns serialize as shifted timestamps on non-UTC servers [src/read/projections/supplier_invoice.ts:10] - deferred, pre-existing repo-wide: no pg.types... | One IST-pin commit: pg DATE parser in src/config/db.ts plus pool TimeZone, with its own full-suite run. Pairs with line 616. |
| 326 | 7-4 | Superseding a catalogue row's min-max levels is out of scope for Phase 1: a second POST on an occupied (sku, location_id) key returns 409 SPARE_ALREADY_CATAL... | Min-max edit route is required before go-live; stocking levels change monthly. Backlog story. |
| 327 | 7-4 | Partial issue against a reservation is out of scope: issue is all-or-nothing on the reserved quantity, and a declared quantity that diverges from the locked ... | Partial issue against a reservation is real floor behaviour. Backlog story. |
| 343 | 7-5 | Amending or voiding a recorded calibration certificate is out of scope for Phase 1: a certificate is immutable once recorded, a second POST on an occupied (i... | Certificate void or supersede route is required before go-live; a wrong validity date locks or unlocks a machine. Backlog story. |
| 364 | 7-6 | The statutory scan notification targets the location of the operator who ran the scan, not a physical asset location: the Story 7.1 asset register has no loc... | Statutory alerts target the role enterprise-wide in Phase 1; asset home location remains the AD-9 revisit. |
| 474 | 6-2 | Cross-stream valuation coherence is a platform decision: production-stream issues drain stock but do NOT call applyValuationIssue, so inventory_valuation.run... | FG costing is out of Phase 1; production and job-work drains do not relieve valuation. Platform backlog. |
| 499 | 7-8 | The REST-path replay contract (2xx via `replayIdOrReject`, and the remaining idempotency-replay test failures across the suite) remains the open platform dec... | REST replay returns 200 with the original result; edge keeps 409 DUPLICATE_EVENT. The idempotency test family is updated to assert 200. One platform story; this also drains most of the permanent noise floor. |
| 616 | 8-4 | Neither `TimeZone` nor `DateStyle` is pinned on the connection pool, so every `column::text` read of a DATE is format-ambient and every `CURRENT_DATE` compar... | Bundled with line 215 into the single IST-pin commit. |
| 637 | 6-3 | The rejected SOURCE lot is not consumable by its own rework order: `assertQcGateAllows` blocks it with `LOT_ON_HOLD` reason `rejected`, so a rework order can... | A rework order may draw its own rejected source lot. Gate exemption when order type is rework and the lot is its declared source. Backlog story. |
| 761 | 9-5 | The return-clock FIFO pool is keyed on (service_order_id, sku) and ignores `challan_class` (src/compliance/jobwork-return-clock.ts:212, `lockReturnClocksWith... | A return drains the matching challan_class first, then the oldest live clock, then breached clocks. Fix the lock query at jobwork_return_clock.ts:134-137. |
| 766 | 9-5 | A negative `count_adjustment` is written unconditionally (src/compliance/cycle-count.ts:1436) with no check that the variance is covered by the customer-owne... | Refuse a count variance that would take the customer-owned balance below zero at sign-off; the correction path covers the rest. |

## Open

Table 4 is the surviving deferred list. Each row names what would trigger the work.

Table 4: Open deferred items

| Ref | Story | Item | Trigger or note |
| --- | --- | --- | --- |
| 7 | 9-3 | No test drives two racing requests on the same idempotency key to force the `uq_custody_ledger_source_event` 23505-to-409 classification path itself (Task 4.... | Only `Promise.all` in story-9-3.test.ts is line 1278 (last-unit race, different keys). Classification path lives at custody-ledger.ts:536 (`{ entry_id, constraint }`). Trigger: any change to persistEvent's 23505 handling or to `uq_custody_ledger_source_event`. |
| 42 | 8-8 | Replay detection on the witness sign-off/waive/notice routes is a pre-read (`holdPoint.status !== 'open'` before `persistEvent`), not a persisted-event signa... | quality.ts:3005 and :3098 `const replayed = holdPoint.status !== 'open';` unchanged. House pattern across Epic 8; fixing means comparing `persisted.event_id` against the refreshed row on three routes. Trigger: a reported wrong 201/403 on a witness retry. |
| 47 | 6-4 | Genealogy over-reports for orders with multiple sequential completions on the same order (consumed-material window is order-scoped by `completed_at` cutoff, ... | src/production/lot-genealogy.ts:35,144 still cuts by `completed_at` per order. Architecturally deep, deferred per explicit user decision. Trigger: a customer running multi-completion orders needing per-completion recall precision. |
| 69 | 1-3 | SCIM directory mutations commit before the event+audit write, so a crash between the two transactions leaves an access-rights change with no audit entry [src... | `src/adapters/iam/scim.ts:102-109` `upsertUserWithRoles` then separate `persistEvent`; trigger: an IdP sync incident or when `persistEvent` grows a client-in parameter |
| 70 | 1-3 | Audit `location_id` is client-supplied for wildcard-grant callers; a `*` grant matches any UUID, so a wildcard admin can stamp an arbitrary location into the... | `src/api/v1/events.ts:141-145` coerces only inventory-master events to `NO_LOCATION_UUID`; location register now exists so the blocker is gone, but reject-vs-coerce is a design call |
| 88 | 1-10 | `deploy/pipeline/verify.sh` (Task 3.6's dry-run mode) is never invoked automatically by any CI/CD job or schedule, so there is no automated drift detection i... | Still only echoed from `deploy/pipeline/bootstrap.sh:134`; trigger: first time GitHub settings drift bites, needs a PAT/secret decision |
| 98 | 1-10 | GitHub Actions and Docker actions are referenced by mutable version tags rather than commit SHAs [.github/workflows/ci.yml, .github/workflows/cd.yml] - defer... | `ci.yml:30-93` all `@v4`; trigger: pre-production security review, bundle with line 99 |
| 99 | 1-10 | Runner binary download is not integrity-verified before installation [deploy/pipeline/runner/bootstrap-runner.sh:52-58] - deferred, hardening should be handl... | No `sha256` in `deploy/pipeline/runner/bootstrap-runner.sh`; bundle with line 98 |
| 103 | 1-11 | Unbounded sequential per-event fan-out: `resolveTargetUserIds` is uncapped and each recipient triggers serial insert/delivery/opt-in/subscription/push round-... | `src/notify/dispatch.ts:185-187` serial `for` over uncapped user list; trigger: multi-site rollout or a role with >100 holders |
| 116 | 2-3 | Expiry is compared in server-local time (`todayLocalYmd`/`localToday`) while `lot_master` availability filters on SQL `CURRENT_DATE` [src/compliance/lot-seri... | `lot-serial-validation.ts:103-109` host-local getters vs `lot_master.ts:214` `CURRENT_DATE`; trigger: a clock-standardization pass (copy `gate.ts:29-34` IST body, or move to DB date) |
| 118 | 2-3 | FEFO/FIFO selection cannot split a request across lots and rejects `NO_AVAILABLE_LOT` even when combined stock across lots would satisfy it, while an un-lott... | `:215` still single-lot `NO_AVAILABLE_LOT`; trigger: business asks for multi-lot picks |
| 132 | 2-7 | Demand-window anchor and obsolescence aging both use SQL now() rather than the supplied scope.business_date, so a backdated or replayed job decides against w... | `planning-jobs.ts:202,760` still `now()`; trigger: real scheduler or replay support |
| 161 | 3-6 | Cancelling a pick line must also release its allocation, and a task whose lines are all cancelled can never complete because completion throws on `activeCoun... | `pick.ts:773` still throws on `activeCount === 0`; no writer sets pick `cancelled` (only indents/maintenance); trigger: a pick-cancellation story |
| 162 | 3-6 | Pick line confirmation never gates on the parent task's status, so lines belonging to a cancelled task would still confirm and take a fresh allocation that n... | `pick.ts:421` checks line status only; same trigger as line 161 |
| 172 | 3-7 | No site-isolation in `applyDispatchPackedProjection`/`applyDispatchShippingDocumentsGeneratedProjection`/`applyDispatchDispatchedProjection` - the edge/direc... | `src/compliance/dispatch.ts:164,273,400` have no site check; trigger: edge sync carrying dispatch events or multi-site rollout |
| 192 | 3-9 | Concurrent checks select same reserve bin - no lock on source bin selection in `runForwardPickReplenishmentCheck`. Two concurrent checks for different (sku, ... | No `FOR UPDATE`/advisory lock in `src/warehouse/replenishment-job.ts`; trigger: concurrent check scheduling or multi-zone volume |
| 209 | 4-4 | Supplier onboarding auto-approval emits supplier.onboarding_submitted and supplier.onboarding_approved in two separate persistEvent transactions with unrelat... | src/api/v1/suppliers.ts:240 and :264 still separate persistEvent calls with separate randomUUID correlation ids (251, 275). Needs a multi-event persist path; trigger: a real stranded-supplier incident. |
| 211 | 4-4 | Multiple POs draftable from a single approved indent; only the first is ever issuable [src/compliance/purchase-order.ts:262] - deferred, low impact: applyPoD... | No existing-PO-per-indent guard in src/compliance/purchase-order.ts (only PO_ALREADY_DECIDED at 485). Fix needs a decision on the rule (one open PO per indent?) plus a guard query; trigger: first real second-draft on an indent. |
| 220 | 4-6 | CHECK vocabulary widening never propagates to existing databases [read/projections/supplier.sql:117] - deferred, pre-existing guarded-migration pattern repo-... | supplier.sql:57-84 guarded DO blocks unchanged; repo-wide migration convention (drop-and-recreate on widen). Trigger: the first CHECK that actually needs widening on a live DB. |
| 231 | 4-5 | Receipts are aggregated at SKU grain, so a PO repeating one SKU across lines cannot attribute them [src/read/projections/three_way_match.ts:171] - deferred b... | three_way_match.ts:266,339 still marks line_count > 1 as ambiguous_sku. Needs a per-PO-line receipt attribution key on grn_line (schema change). Trigger: a supplier PO that repeats a SKU across lines in production. |
| 233 | 4-5 | Payment clearance has no partial or per-invoice release [src/read/projections/three_way_match.ts:361] - deferred, matches the msme_ageing_feed precedent: eve... | three_way_match.ts:436-460 still full-set feed by design. Needs real payment state which does not exist. Trigger: ERP asks for per-invoice clearance. |
| 239 | 4-5 | Receipts aggregate at SKU grain (`ambiguous_sku` fail-closed) [src/read/projections/three_way_match.ts:171] - already logged above in the dev-story 4-5 entry... | Duplicate bookkeeping row of line 231; fold. |
| 240 | 4-5 | GRN-to-PO link has no `po_ref_ext` supplier correspondence check [src/compliance/three-way-match.ts:193-228] - deferred, structurally not implementable from ... | three-way-match.ts:226 comment still documents the gap; erp_purchase_order carries only supplier_ref_ext and supplier still has no ERP ref mapping. Needs a mapping table. Trigger: a cross-supplier GRN mis-link in production. |
| 246 | 4-2 | Scorecard reads are procurement-module-wide, not site-scoped [src/read/projections/supplier_scorecard_metric.ts:14] - deferred, same pre-existing pattern as ... | supplier_scorecard_metric.ts:14-16 comment unchanged; supplier.sql still has no site_id column. Blocked on the supplier site_id deferral. |
| 251 | 4-2 | A replayed direct event without caller-supplied idempotency_key/event_id creates duplicate domain_events and audit rows while the projection replay guard sti... | store.ts:800 dedupes only when idempotency_key or event_id is supplied. AD-16 caller-carried keys is the documented platform convention; same root as 261. Trigger: a platform decision on caller-less dedupe. |
| 261 | 5-2 | Global idempotency-key reuse silently no-ops a lifecycle transition: `uq_idempotency` is global and `persistEvent` returns the existing event on a key hit, s... | uq_idempotency handling unchanged (store.ts:1235); platform convention AD-16. Same root as 251. |
| 273 | 5-5 | ECO add change lines cannot express backflush: eco_change_line has no supply_method field, so every ECO-added line silently defaults to directed_issue while ... | eco_change_line.sql has no supply_method column; eco.ts:842 add-insert omits supply_method (defaults to directed_issue); eco.ts:815 only copies it for revision copies. Needs a schema column + payload field + insert change across 5.3. Trigger: a user tries to author a backflush line through an ECO. |
| 278 | 5-6 | Cost rollup capture runs outside the persist transaction, so a same-revision `bom_line` amendment between capture and commit yields a silently stale snapshot... | bom-costing.ts:301 guard still compares only current_revision_id; the release gate re-checks freshness via MAX(bom_line.updated_at) (bom.ts:592, 660). Real fix means capturing inside the persist txn; trigger: a stale rollup ever passes release. |
| 285 | 7-1 | stream_type/event_type mismatch bypasses all shape validation and the direct-writes guard [src/api/v1/events.ts:119, src/events/store.ts:199-221] - deferred,... | Root of a 5-bullet family. src/events/store.ts persistEvent still has no registry membership or stream/event consistency check (grep SUPPORTED_EVENT_TYPES / streamType in store.ts: no hits at the persist path). Needs a platform decision on what to do with events posted under an unregistered type (many story tests deliberately post synthetic shapes). Trigger: any forged-event finding that is not caught by a per-seam gate. |
| 304 | 7-2 | A swallowed notification failure permanently loses the escalation: emitNotification returning ok:false after the work_order_overdue event committed leaves th... | src/maintenance/pm-jobs.ts:256-268 still only counts `emitted.ok`; work order stays 'overdue' with no retry. Needs a retry/outbox design (AD-17 non-throwing emit is deliberate). Trigger: a notification outage incident. |
| 306 | 7-2 | 2026-08-17 manifestation: story-5-2 "migrates a qualifying kit ... effective_from should default to today" fails between ~00:00-08:30 IST when the UTC date d... | test/integration/story-5-2.test.ts:838-842 still compares `new Date(row.effective_from).toDateString()` to local `new Date()`; pg parses DATE to local midnight so a naive toISOString fix is wrong too. Needs `effective_from::text` in the SELECT plus a UTC date literal. Trigger: CI runs in the IST window. |
| 310 | 7-2 | Same-event-type idempotency-key reuse with different content returns the original event (201/200 with the wrong resource and the new write silently dropped):... | src/api/v1/maintenance.ts:252-270 replayIdOrReject still compares event_type + id UUID-ness only; no payload hash anywhere (grep payload_hash / IDEMPOTENCY_CONFLICT: none). 6-1 added a stream_id check in its own copy. Platform decision (content hash on uq_idempotency). Trigger: a client bug that reuses keys. |
| 312 | 7-2 | 2026-08-17 manifestation (02:00-08:30 IST window): story-5-3 "does not report superseded or retired lines in the where-used impact walk" fails when the clock... | Same root as L306 (DATE vs local clock). Memory 2026-09-04 confirms the story-5-3 flake still fires. |
| 328 | 7-4 | Damaged-return disposition and scrap routing are not modelled: every returned spare goes back to owned on-hand at the reservation's location through applySto... | maintenance-spares.ts applySpareReturned has no condition/scrap branch (grep condition/damaged/scrap: only a comment at :570). Trigger: an FR for spares scrap routing. |
| 329 | 7-4 | Spares cost is not accumulated onto the work order: the reservation carries quantity and SKU but no valuation, and no maintenance cost ledger is written on i... | No valuation on the reservation (grep cost/valuation in maintenance-spares.ts: none); 7.6 total_parts_cost is payload-declared, not derived from issues. Trigger: FR-M-15 valuation requirement. |
| 330 | 7-4 | Warranty and AMC are not checked at reservation time: a spare can be reserved against a work order for an asset still under warranty with no prompt [src/comp... | No warranty/amc reference in maintenance-spares.ts. Trigger: FR-M-10/11. |
| 344 | 7-5 | Calibration due-date scheduling and workload planning are not built: calibration_interval_days is captured on instrument_register for the alert horizon and f... | calibration_interval_days is only validated/stored (maintenance.ts:2748, calibration-register.ts:237); calibration-jobs.ts never reads it. Feature-sized. Trigger: FR for a calibration calendar. |
| 345 | 7-5 | Calibration is not tied to the Story 7.2 work-order tree: recording a certificate creates no maintenance_work_order row, and no work order can be raised for ... | No maintenance_work_order write in calibration-register.ts. Trigger: FR for re-calibration work orders. |
| 347 | 7-5 | The lockout is not extended to measurement write paths beyond qc.result_recorded: the Story 1.7 gate in src/compliance/calibration.ts still fires for exactly... | src/compliance/calibration.ts:25-26 still returns unless stream in QC_STREAM_TYPES and event_type === QC_RESULT_RECORDED. Needs a decision on which events are "measurements". Trigger: a new measurement-bearing event type. |
| 348 | 7-5 | Offline certificate capture is not delivered: every route in this story is central-only and no PowerSync sync rule covers the four new tables [src/api/v1/mai... | edge/src has no certificate capture (grep certificate in edge/src: none); 7.8 covered fault/status/meter/spare/completion only. Trigger: technicians recording certificates offline. |
| 349 | 7-5 | Certificate document and PDF attachment storage is not provided: certificate_number and issuing_lab are the only evidence the register holds, and the scanned... | No attachment storage in the platform (supplier-invoice carries reference fields only). Trigger: a platform attachment-store decision. |
| 356 | 7-6 | Routing above-threshold repair-vs-capitalize flagged work orders into the FR-FA capitalization queue is out of scope for Phase 1: work orders whose total_cos... | capitalization_flagged is set (maintenance-plan.ts, config.maintenance.capitalizationThreshold) and nothing consumes it. Trigger: FR-FA capitalization epic. |
| 358 | 7-6 | Staged pre-due statutory examination alerts (30/14/7-day style) are not built: the overdue scan flips straight to 'overdue' and emits the escalating notifica... | statutory-jobs.ts:109 comment "No staged pre-due alerts exist (Binding ...)". Product feature. Trigger: FR for pre-due warnings. |
| 359 | 7-6 | Statutory examinations are not tied to the Story 7.2 work-order tree: recording an examination creates no maintenance_work_order row, and no work order can b... | Same shape as L345 for statutory. Trigger: FR for re-examination work orders. |
| 360 | 7-6 | The statutory use-lock is not extended to measurement write paths beyond weighbridge.recorded and return-to-service: the status-change handler and the weighb... | Same design question as L347 (what counts as "use"). Trigger: a new use-bearing event type. |
| 369 | 7-6 | `resolveApprover` acquires a second pool connection from inside an applier that already holds an open transaction with FOR UPDATE row locks, and it reads the... | asset-operational-status.ts:255 still calls `resolveApprover(RETURN_TO_SERVICE_DOA_TYPE, 0)` with no client; indents.ts:66 helper reads via findRoleHolder on its own connection. Threading the client through a shared helper used by indents/bom-execution is a refactor. Trigger: a DOA-change race or pool exhaustion under load. |
| 370 | 7-6 | An uppercase-but-canonical UUID in a payload fails every re-derivation comparison: the shape validators use case-insensitive UUID regexes while PostgreSQL re... | Compare sites moved: asset-operational-status.ts:220,:278; maintenance-statutory.ts:437; maintenance-plan.ts:571,:866 all `!==` raw declared strings against pg lowercase text; no toLowerCase in those seams. Five one-liners across three files, or canonicalize in the schema validators (6-1 precedent). Trigger: any client that emits uppercase UUIDs. |
| 376 | 7-6 | The POST-triggered scan's `business_date` is validated for calendar form only, never for plausibility, so a typo or a bad client clock (`2999-01-01`) flips e... | maintenance.ts:273 requireBusinessDate still form-only; a bound would break the deliberate 2027 scan date in story-7-6 tests and legitimate backfill runs. Needs an agreed window. Trigger: a bad-clock incident. |
| 399 | 6-1 | Kit-assembly order naming (sprint-change-proposal FR-I-09 scope note, Stories 6.1 to 6.3 collectively). The `source_reference_type` vocabulary is fixed at er... | production-order.ts:80 SOURCE_REFERENCE_TYPES still erp_sales_order/indent/rd_project/manual. Trigger: FR-I-09 lands. |
| 403 | 7-7 | No amendment, void or supersede path exists for a coverage record. `asset_coverage` is append-only in Phase 1 (Binding Decision 5) and a renewal is a NEW row... | No `coverage_(amended/voided/superseded)` event in src/events/schema.ts or src/compliance/maintenance-coverage.ts. Needs an event family (BD5). Trigger: first mistyped coverage in production that cannot wait for expiry. |
| 407 | 7-7 | The Story 7.3 accept handler derives its own `business_date` as a UTC `slice(0, 10)` of the wall clock, and the Story 7.7 warranty derivation consumes that s... | src/api/v1/maintenance.ts:1410 still `new Date().toISOString().slice(0, 10)`; 6 api files use UTC slice vs 4 using `toIstCalendarDate`. Platform convention split. Trigger: platform-wide business-date convention story. |
| 409 | 7-7 | The inherited platform gaps logged under Stories 7.5 and 7.6 are re-affirmed unchanged and are NOT fixed in this story: a `maintenance.*` event carried on a ... | Umbrella of six platform items. Confirmed still open: store.ts:330 checks stream_type only as non-empty string (no registry); no index on `domain_events` payload object_id in init-db.sql. Trigger: platform hardening story. Same root as 413 and 461. |
| 424 | 6-1 | The coverage scan loop is unbounded, exactly like its Story 7.5 and 7.6 twins: `listCoverageStagesDue` takes no cursor, and the loop opens one pool connectio... | `listCoverageStagesDue` (src/read/projections/asset_coverage.ts:291) still takes no cursor/limit; coverage-jobs.ts:129 consumes the full set in one request. Shared job-runner change across four scans. Trigger: first proxy timeout on a scan. |
| 429 | 7-7 | `coverage_type` is declared on `CoverageExpiryFlaggedPayload` as a derived-and-checked field, but `asset_coverage_alert` has no `coverage_type` column, unlik... | No `coverage_type` in read/projections/asset_coverage_alert.sql or its accessor. Spec/column change. Trigger: first "which AMCs are expiring" report request. |
| 432 | 7-7 | `lower()` is collation-dependent and nothing under `deploy/` pins `LC_COLLATE`, `LC_CTYPE` or `POSTGRES_INITDB_ARGS`, and the column carries no `COLLATE` cla... | No `LC_COLLATE`/`POSTGRES_INITDB_ARGS`/`COLLATE` anywhere under deploy/. Deployment-wide. Trigger: first second-host or restore-to-different-locale deployment. |
| 443 | 7-7 | `applyWarrantyOverrideRecorded` holds `maintenance_work_order` FOR UPDATE on the transaction connection and then calls `resolveApprover`, which takes no clie... | `resolveApprover(transactionType, value)` at src/api/v1/indents.ts:66-74 still takes no client. Platform DOA client-threading change. Trigger: pool-exhaustion hang observed under load. |
| 490 | 7-8 | Parked dependents (Binding Decision 3) are visible only on the device: when a head capture settles `STREAM_CONFLICT`, every later outbox row on the same stre... | Binding Decision 3; `edge/src/local-db/outbox.ts` `parked_behind_event_id` still device-only. Trigger: supervisor asks for central visibility of parked rows |
| 493 | 7-8 | The technician worklist (`GET /api/v1/edge/maintenance/worklist`) is company-wide and bounded by `limit` (default 200, max 500), not site-scoped, because wor... | `src/api/v1/edge.ts:616-619` unchanged; AD-9 (no location on work orders). Trigger: asset register or work order gains a location |
| 494 | 7-8 | `STREAM_CONFLICT` for non-maintenance streams still settles `needs_attention` on the device with no central queue; the sync-conflict queue is maintenance-onl... | `edge.ts:529` raise guarded by `stream_type === 'maintenance'`. Trigger: a second stream (production/qc) needs a supervisor conflict queue |
| 495 | 7-8 | `in_progress` and `on_hold` work orders past their grace window are not swept to `overdue`: `listGraceExpiredWorkOrders` still selects `status = 'open'` only... | `maintenance_work_order.ts:372` `WHERE status = 'open'`, pinned by story-7-8 test. Trigger: PO wants overdue on in-flight orders |
| 501 | 7-8 | No asset-tag list is synced to the device, so a mistyped tag on a NON-safety fault settles `needs_attention` on the tablet with no central trace; only safety... | `edge/src/local-db/worklist.ts` carries only worklist assets. Trigger: plant reports mistyped-tag faults lost |
| 510 | 7-8 | The worklist fans out `listSpareReservations` per work order serially with no batching beyond `limit` (up to 500 round trips); the spec's Task 6.4 defines th... | `edge.ts:667` still per-order. Trigger: worklist latency at >100 open work orders |
| 522 | 8-1 | FEFO/FIFO auto-select locks `stock_balance` rows before the lot and gate rows, inverting the mandated lot to gate to stock order and opening a deadlock windo... | `lot-serial-validation.ts:174-181` still `SELECT ... FOR UPDATE` on stock_balance before the lot lock. Trigger: 40P01 deadlock in logs under dispatch/conditional-release load |
| 523 | 8-1 | A serial-pinned issue (serials but no top-level lot_id) never reaches assertQcGateAllows, so a gated serial lot surfaces INSUFFICIENT_STOCK instead of LOT_ON... | `lot-serial-validation.ts:602` gate is inside the `lotId` branch only; serial branch `:611` has none. Trigger: serialised SKUs on QC-gated lots in pilot |
| 530 | 8-1 | Plan-approve and conditional-release audit rows stamp `location_id` from the arbitrary `functionMatches[0]` assignment rather than the task/lot site [src/api... | `functionMatches` gone; `quality.ts:693` (plan approve) and `:1035` (conditional release) still call `auditRejectedAttempt` without `siteId`, falling back to `:175` assignment location. Plan has no site; release needs the lot site fetched before the catch. Trigger: audit review needs per-site attribution |
| 538 | 8-2 | The `qc.sampling_state_adjusted` authority check compares the acting role against `config.quality.qcHeadRoles` only; no DOA transaction type governs reduced-... | BSD 9. Trigger: access matrix adds a DOA approver for reduced inspection |
| 539 | 8-2 | Re-inspection, corrections and resubmitted lots are not modelled: a task moves `open` to `sampling_determined` to `inspected` exactly once, results are immut... | Task lifecycle still single-pass (`qc_inspection_result.sql` immutable). Trigger: QC asks to re-inspect a not_accepted lot instead of dispositioning it |
| 541 | 8-2 | Instrument-type matching between the frozen plan line (`instrument_type`) and the register asset is not enforced: any registered, calibrated instrument may r... | Same root as 556; `applyResultBatch` unchanged. Trigger: metrology audit finding or 7.5 per-parameter calibration ranges |
| 543 | 8-2 | The Story 1.7 synthetic `qc.result_recorded` shape is still accepted on `POST /api/v1/events` and the edge route with no task binding and no projection; it r... | BSD 1, spine test 4 pins it. Trigger: retiring the 1.7 synthetic shape from the spine allowlist |
| 554 | 8-2 | A pre-8.2 approved `inspection_plan_version` could carry an AQL that passed Story 8.1's looser regex-only gate but is not one of Story 8.2's 26 preferred AQL... | `sampling.ts:110,132` unchanged; 8.1/8.2 shipped same day so no such row expected. Trigger: first AQL_NOT_IN_STANDARD on a real task (then a one-time audit query) |
| 556 | 8-2 | A numeric characteristic with no declared `instrument_type` is still routed through the instrument-bound "result" endpoint's full asset/calibration gate [src... | Same root as 541; `inspection_plan_characteristic.sql:31,46` allows NULL instrument_type on any kind, so the combination is legal today |
| 574 | 8-2 | AC-proof gaps in test/integration/story-8-2.test.ts: AC1's "null level defaults to II" is never actually exercised by a null-level/non-null-AQL plan; AC3's A... | Genuine AC proofs missing, not symmetry. Trigger: next change touching story-8-2.test.ts or switching logic |
| 580 | 8-3 | The split and downgrade relabels write ONE parent-side `lot_trace` row per event, not a signed pair per stock grain [src/quality/lot-split.ts appendRelabelTr... | Still structural: `CREATE UNIQUE INDEX ... idx_lot_trace_event_id ON lot_trace (event_id)` at read/projections/lot_trace.sql:46; appendRelabelTrace at src/quality/lot-split.ts:207 writes one row. Trigger: lot_trace moves to per-movement grain. |
| 582 | 8-3 | Re-inspection of a split child is out of scope: children inherit the parent's `sampling_id`, `sampling_outcome`, counts and inspector, and are created direct... | quality.ts:2765 inserts child task at `'inspected'`. Trigger: the 8.2 re-inspection work item. |
| 585 | 8-3 | Child and downgrade lot numbers are derived as `<parent>-NN` and `<parent>-DG` with `DUPLICATE_LOT` as the only collision guard [src/compliance/quality.ts ap... | quality.ts:2696 and :3016 unchanged. Trigger: a site lot-numbering standard lands. |
| 603 | 8-3 | `recordQualityAcceptanceMetricBase` has no try/catch, so a thrown `AppError` (e.g. the seam's own documented `SCORECARD_DERIVATION_MISMATCH`, or `DUPLICATE_E... | Confirmed: 0 `sendAppError` in supplier-scorecards.ts; router.ts:139 awaits handler with no catch; server.ts:1191 emits 500 INTERNAL_ERROR. Fix means adding a sendAppError helper and wrapping all 4 routes (>15 lines). Trigger: first scorecard route that must surface a seam code. |
| 604 | 8-3 | `recordQualityAcceptanceMetricBase` performs no site-scoping check before writing a scorecard metric against an arbitrary disposition [src/api/v1/supplier-sc... | 0 `assert*SiteAccess` in supplier-scorecards.ts. Trigger: scorecards get a site dimension. |
| 609 | 8-3 | Broad test-coverage gaps in test/integration/story-8-3.test.ts, none tied to a confirmed application bug (the two gaps that WERE tied to confirmed bugs - the... | Still true: 0 hits for conditional_release / DUPLICATE_LOT / NCR_NOT_FOUND in story-8-3.test.ts. Bundle into the existing test-hardening pass. |
| 615 | 8-4 | Guarded DDL blocks across every `read/projections/*.sql` file match a constraint by `conname` only, and no file carries `ADD COLUMN IF NOT EXISTS` reconcilia... | Repo-wide (~100 sql files); lot_trace.sql:31-46 shows the same pattern needed a bespoke re-add block again. Trigger: next constraint redefinition on a shipped table. |
| 620 | 8-4 | No `.gitattributes` exists, so the ~100 `read/projections/*.sql` files land CRLF or LF by coin flip and at least one is internally mixed; this already causes... | Still absent; `git ls-files --eol`: 47 crlf, 81 lf, 1 mixed. Needs a new file plus a `git add --renormalize` commit touching ~130 files - standalone commit, not a sweep fix. Trigger: next CRLF drift test failure. |
| 635 | 6-3 | Finished-goods valuation of output lots is not modelled: a completion relieves WIP but writes no cost onto the output lot and no `inventory_valuation` row, s... | Still true: production-completion.ts writes no inventory_valuation row for output lots (only WIP relief unit_cost at :366). Same root as Story 6.2 BD9 cross-stream valuation gap. Trigger: costing/FG valuation epic. |
| 636 | 6-3 | Cost allocation between the primary output, co-products and by-products is not implemented (the standard by-product-at-zero-cost convention included), becaus... | Same root as 635; nothing to allocate until outputs are costed. Trigger: 635. |
| 641 | 6-3 | Physical scrap intake into a scrap stock class is Phase 2 (Epic 16, FR-SC); `production_scrap_declaration` is the AD-10 source document for it. | Phase 2 scope; trigger: Epic 16 FR-SC. |
| 652 | 6-3 | Every guarded `ADD CONSTRAINT` block is a check-then-ALTER inside a read-committed DO block, so two concurrent `npm run db:migrate` runs can collide with `42... | migrate.ts has no advisory lock (0 hits). Wants one shared pg_advisory_xact_lock in src/events/migrate.ts; trigger: concurrent migrate seen in CI/deploy. |
| 654 | 6-3 | Neither `production_completion.quantity` nor the accumulating `production_order.completed_quantity` carries an upper bound, so a pathological magnitude raise... | Same house-wide root as the 6.2 posting_value entry and asset-meter/maintenance-plan comments (22003 unmapped). Trigger: a house-wide 22003 mapping in the error mapper. |
| 662 | 8-6 | `compliance_bis_licence.sku` / `label_master.sku` carry no FK to `item_master` [read/projections/compliance_bis_licence.sql, read/projections/label_master.sq... | Still no REFERENCES in either projection (grep 0 hits). 8.7 shipped CRUD without it. Trigger: orphan SKU rows surface in the register. |
| 677 | 8-6 | `capaAgingMetric` takes no `siteFilter` and returns CAPAs enterprise-wide, a literal AC 7 gap even though disclosed and schema-grounded (`qc_capa` has no sit... | reporting.ts:300-303 still (period, client) only; qc_capa has no site_id (grep 0). Trigger: schema decision to add qc_capa.site_id. |
| 706 | 8-7 | `resolveComplianceAuthority` does not resolve delegation transitively and does not verify the delegate is active; it also passes amount `0` where amount-agno... | master-data.ts:169 findMatchingDoaEntry(type, 0), :185 single-hop findActiveDelegation. Mirrors resolveQcAuthority; same root as the 7-6 DOA entries. Trigger: DOA-layer rework. |
| 713 | 8-7 | The BIS expiry sweep timer has no enable/kill switch and does not run a first tick at boot, so nothing sweeps for the first interval and a misbehaving sweep ... | server.ts:1295 setInterval only, no enabled flag, no immediate run. House-wide for all four cycles. Trigger: ops asks to disable a sweep without redeploy. |
| 714 | 8-7 | SIGTERM clears the sweep interval without awaiting an in-flight cycle, so the pool can close under a running sweep. House timer pattern shared by all four cy... | server.ts:1298-1306 clearInterval then closePool; `guarded` tracks `running` but shutdown does not await it. House-wide. Trigger: observed pool-closed error during shutdown. |
| 720 | 8-7 | `GRANT SELECT, INSERT, UPDATE` on `compliance_bis_licence` and `label_master` is table-wide, so `app_user` can rewrite `approved_by`, `approved_at`, `licence... | compliance_bis_licence.sql:141, label_master.sql:122; no column-level GRANT anywhere in read/projections (0 hits). House-wide posture; trigger: SoD audit finding. |
| 734 | 8-8 | The BSD-4 flag-ownership test is string equality on `lot_master.quality_hold_reason`. Two independent holds that happen to share a reason string are indistin... | lot_master.sql has no owning-hold column (0 hits for hold_id). Real fix is a schema column. Trigger: two concurrent holds with the same reason observed. |
| 736 | 8-8 | `stock_balance.stock_class` still has no CHECK constraint; the vocabulary lives in two application-code sets (`src/compliance/stock-balance.ts`, `src/complia... | stock_balance.sql:24 plain TEXT; vocabulary in stock-balance.ts and cycle-count.ts. Trigger: after Epic 10 finalises the value set. |
| 742 | 9-4 | Loss is always booked as customer ownership; processor-owned material cannot be declared lost (src/compliance/custody-ledger.ts:897). Own-material loss is ou... | custody-ledger.ts:1232 ownership: 'customer' hard-coded on loss. Trigger: business asks for processor-owned loss declaration. |
| 743 | 9-4 | Output quantity is unbounded relative to material consumed; no yield reconciliation (src/compliance/jobwork-output.ts:243). Story 9.6 scope. | jobwork-output.ts has no yield check (0 hits). Story 9.6 scope. |
| 749 | 9-4 | `getLotByNumberAndSku` (src/compliance/jobwork-dispatch.ts:291, custody-ledger.ts) has no site predicate because `lot_master` carries no `site_id` column any... | lot_master.sql has no site_id (0 hits); lot_master.ts:165 signature unchanged. Schema change across every consumer. Trigger: cross-site lot-number collision observed. |
| 753 | 9-5 | Alert stamps and the frozen deemed supply are not derivable from events (read/projections/jobwork_return_clock.sql:9). The file claims the BSD-8 rebuildable-... | read/projections/jobwork_return_clock.sql:11-12 still claims the BSD-8 rebuildable idiom; :44-47 alert_90_sent_at / alert_30_sent_at / deemed_supply_* are sweep-written with no source_event_id. Needs an event or a header retraction (design). Trigger: first projection rebuild procedure for 9.x. |
| 756 | 9-5 | No DDL tie between `challan_class` and `expiry_date` on the return clock (read/projections/jobwork_return_clock.sql:57). `chk_jobwork_return_clock_expiry` on... | sql:57 and :99 still only `expiry_date > challan_date`; mirrored at deploy/compose/init-db.sql:12528/12570 and pinned by schema-drift.test.ts:1790. Closing needs DROP+ADD in two mirrored files plus the drift test. Single writer (projection ts:90) and the backfill (sql:112-124) both compute in SQL. Trigger: any second insert path. |
| 768 | 9-5 | Return-clock counters are monotonic - `updateReturnClockCounters` only ever adds. A `custody.return_recorded` posted against the wrong sku or with the wrong ... | No compensating event exists; a reversal event is a design piece. Trigger: first mis-posted return in the pilot. |
| 769 | 9-5 | `correctChallanClassification` (src/compliance/jobwork-return-clock.ts:297) records no actor, no reason and no audit trail, because the Story 9.5 chunk 1 rev... | jobwork-return-clock.ts:~320-380 is projection-only UPDATE, no actor/reason. Deliberate chunk 1 decision; same root as 753 (sweep/correction state not event-backed). Revisit together. |
| 775 | 9-5 | `findRoleHolder` (src/notify/escalate.ts:79) resolves an escalation target with NO site context: `WHERE role = $1 AND active = true ORDER BY created_at ASC L... | src/notify/escalate.ts:79 unchanged, `target: { role, location_id: null }` at :92. Story 1.11 shared machinery; fix threads site through EscalationDefinition. Trigger: second site goes live. |
| 776 | 9-5 | Both job-work report routes (src/api/v1/service-orders.ts, ITC-04 and aging) have no `LIMIT`, no cursor and no period cap, and the aging route takes no date ... | service-orders.ts ITC-04 (:1074-1092) and aging (:1226-1255) materialise all rows. Contract change. Trigger: clock count in the thousands. |
| 782 | 9-5 | The sweep holds `FOR UPDATE` on up to `clockSweepBatchSize` clock rows for the whole tick while a user posting a return takes the same rows through a plain `... | :184 `FOR UPDATE OF c SKIP LOCKED`; no lock_timeout anywhere in sweep. Fix is per-row transactions or a smaller batch (design). Trigger: an operator-reported stall on a return post. |
| 791 | 9-5 | The chunk 1 return-clock backfill is asserted as TEXT in the drift test and never executed, so a backfill that inserts zero rows, a wrong `expiry_date`, or d... | sql:112-124 INSERT ... SELECT never run by any test. Needs an integration arm against a DB holding 9.2/9.4 receipts. Trigger: first migrate of a database with pre-9.5 receipts (deploy). |

## Awaiting decision

Table 5 lists what the room could not decide.

Table 5: Awaiting a human decision

| Ref | Story | Item | Question and owner |
| --- | --- | --- | --- |
| 534 | 8-2 | Table II-A / II-B / II-C are transcribed from the standard's band structure (eleven plan diagonals on normal and reduced, ten on tightened, with the Ac 0 dia... | Ravi brings the QC head with IS 2500 (Part 1):2000 to verify the four corner cells in src/quality/aql-tables.ts. |

## Closed this pass

### Resolved in code

Table 6 lists entries the survey found already closed in code.

Table 6: Resolved entries

| Ref | Story | Item | Evidence |
| --- | --- | --- | --- |
| 32 | 9-3 | Unconditional applier calls with no visible event_type guard [src/events/store.ts:259-260] - deferred, re-check in Group 2 (`custody-ledger.ts` body not yet ... | Guards are inside the appliers: src/compliance/custody-ledger.ts:604-605 and :751-752 (`if (envelope.event_type !== ...) return;`). Group 2 re-check done; store.ts:1133-1134 calls are safe. |
| 35 | 9-3 | Two DDL files synced only by comment, no automated drift check [deploy/compose/init-db.sql, read/projections/custody_ledger_entry.sql] - deferred, pre-existi... | test/unit/schema-drift.test.ts:1726-1743 registers `custody_ledger_entry` (canonical file, table, three indexes) and :1828-1864 pins the reference_ext upgrade path and index guard against init-db.sql. |
| 55 | 1-1 | No authentication or authorization anywhere [src/server.ts:515] - deferred, pre-existing (Story 1.2 scope) | `src/middleware/auth.ts` + `src/middleware/rbac.ts` exist; `src/api/v1/events.ts:130` `getAuthContext(req)`; `src/api/v1/stock.ts:4` `requireRole` |
| 58 | 1-1 | `trace_id` is generated fresh per error and never logged [src/middleware/error.ts:455] - deferred, pre-existing | `src/middleware/context.ts:67-69` per-request `setTraceId`; `error.ts:83-88` uses `getTraceId(req)`; persisted via `src/read/projections/audit_log.ts` |
| 72 | 1-3 | `read/projections/users.sql` carries no grants (they live only in `deploy/compose/init-db.sql`), so a migrate-only-provisioned database cannot execute SCIM w... | `read/projections/users.sql:27-32` GRANTs for `app_user` and `readonly_user` |
| 79 | 1-7 | Duplicated DDL and redundant inline-plus-guard constraint blocks between the canonical migration and the compose mirror [read/projections/instrument_calibrat... | `test/unit/schema-drift.test.ts:1026-1027` pins `instrument_calibration_statuses` canonical vs init-db |
| 110 | 2-2 | `in_transit` column never written by any code path [src/read/projections/stock_balance.ts] -- deferred, Story 2.5 will add inter-location transfers that popu... | `src/compliance/transfer-request.ts:496` `SET in_transit = in_transit + $1` |
| 111 | 2-2 | Consolidated API response merges across `stock_class` [src/api/v1/stock.ts:47-63] -- deferred, class breakdown is a future feature (Story 2.8 consignment seg... | `src/api/v1/stock.ts:107-131` per-class breakdown with `owner_party_code` |
| 126 | 2-5 | `in_transit`/`transfer_request` DDL duplicated across `read/projections/*.sql` and `deploy/compose/init-db.sql`, kept in sync only by comment, with no drift-... | `test/unit/schema-drift.test.ts:133-139` pins `transfer_request`; `in_transit` also in the EXPECTED list |
| 155 | 3-5 | DDL duplicated across `init-db.sql` and `read/projections/*.sql` (putaway_task columns, velocity_class table) -- deferred, pre-existing mirror pattern shared... | `test/unit/schema-drift.test.ts:359-364` `velocity_class`; `putaway_task` in EXPECTED |
| 182 | 3-8 | `test/integration/story-3-7.test.ts` never runs: its `before` hook calls `POST /api/v1/scim/Users` and `POST /api/v1/scim/Users/:id/access-tokens`, neither o... | `story-3-7.test.ts:78,94` uses `/api/v1/scim/v2/Users` + `/api/v1/auth/dev-token` |
| 183 | 3-8 | `.env.test` sets `DB_PORT=5432`, but the project's own test PostgreSQL container (`ims2-test-postgres`) publishes on host port 5442; 5432 is held by an unrel... | `.env.test:5` `DB_PORT=5442` |
| 244 | 4-2 | Quality-acceptance metric source does not exist until Epic 8 Story 8.3 [src/compliance/supplier-scorecard.ts:178] - CLOSED by Story 8.3 (2026-08-30): `qc.lot... | supplier-scorecard.ts:189 handles qc.lot_dispositioned; supplier-scorecards.ts:558 quality-acceptance route exists. Ledger already says CLOSED. |
| 255 | 5-2 | Immutability guard accepts a missing or foreign `revision_id` (no existence or bom_id cross-check); `bom_line` has no FK to `bom_revision`, so a phantom/fore... | Closed by the Group 2 entry at line 265 (revision_id derived server-side from bom.current_revision_id; events.ts rejects non-inventory streams). |
| 260 | 5-2 | RESOLVED by 5-3-eco-workflow-and-where-used-impact (2026-08-11): Cross-revision immutability bypass - `applyBomLineAmended` now filters the `bom_line` lookup... | bom.ts:1326+ applyBomLineAmended selects `WHERE bom_line_id = $1 AND revision_id = $2 FOR UPDATE` (line ~1366) with a comment citing this ledger entry. |
| 265 | 5-2 | Immutability guard accepts a missing or foreign `revision_id` [src/compliance/bom.ts:907, 1019] - RESOLVED: Group 2 confirmed all three consumers (add line, ... | Already closed in ledger; consistent with 255. |
| 317 | 7-3 | DOWNTIME_ALREADY_OPEN (the 23505 resolution for uq_maintenance_downtime_work_order) is not directly exercised by a test: the seam opens the window only insid... | test/integration/story-7-3.test.ts:1261 "Table 4: the DOWNTIME_ALREADY_OPEN resolver ... returns the existing window id". |
| 331 | 7-4 | Offline spares-issue confirmation is not delivered: every route in this story is central-only and no PowerSync sync rule covers the four new tables [src/api/... | edge/src/capture/maintenance.ts:153 captures 'maintenance.spare_issued'; edge/src/local-db/schema.ts has spare_reservation; Story 7.8 done. |
| 357 | 7-6 | Offline technician workflows and closure codes are not delivered: every route in this story is central-only and no PowerSync sync rule covers the four new ta... | edge/src/components/work-order-closure-capture.tsx and closure_code in edge/src/local-db/schema.ts + worklist.ts; 7.8 done. |
| 393 | 6-1 | Material staging, issue, backflush and the production WIP ledger (Story 6.2, FR-MO-04 to FR-MO-06). This story creates no stock movement and no allocation: t... | sprint-status.yaml:934 6-2 done (production_order_stage, production_wip_ledger, six routes). |
| 394 | 6-1 | Completions, co-products, by-products, scrap declarations and completion tolerances (Story 6.3, FR-MO-07 to FR-MO-10). The completed transition is a pure sta... | sprint-status.yaml:935 6-3 done (production_completion, production_scrap_declaration, tolerance). |
| 395 | 6-1 | Lot genealogy, the closure gate, closed-order immutability, offline execution and the central-only enforcement of release, cancel and close (Story 6.4, FR-MO... | sprint-status.yaml:936 6-4 done; edge/src/sync/connector.ts carries CLOSURE_GATE_BLOCKED. |
| 396 | 6-1 | The consumption variance report (Story 6.4, FR-B-08). The report has no FR-MO line in this story's scope. | src/api/v1/production-orders.ts and src/compliance/production-order.ts implement the consumption variance report (grep consumption.variance hits). |
| 398 | 6-1 | Rework orders raised from QC dispositions (Story 6.3, FR-MO-10), blocked on Epic 8. The Epic 8 QC-disposition hard prerequisite binds the Story 6.4 closure g... | Epic 8 done (3e5ba49); 6-3 review proves the completion-to-reject-to-NCR-rework-to-rework-order chain end to end (sprint-status.yaml:935). |
| 428 | 7-7 | Replay of a `maintenance.breakdown_work_order_created` event persisted before Story 7.7 re-derives `warranty_flagged` and `warranty_coverage_id` against the ... | Persisted payloads now carry the derived pair: src/compliance/maintenance-fault.ts:636-637 writes `p['warranty_flagged']`/`p['warranty_coverage_id']` back before insert; src/events/schema.ts:2307-2312 documents "always present on the persisted payload". No rebuild-through-seam path exists in src, so the mismatch-vs-write-back distinction is moot. |
| 435 | 7-7 | `asset_coverage_alert` denormalises `asset_id` and `expiry_date` at flag time with no foreign key to `asset_coverage` and no consistency guard, while `app_us... | Premise is false: read/projections/asset_coverage.sql:112 grants only `INSERT, SELECT` to app_user (alert.sql:64 likewise), so a coverage expiry cannot change after the alert is flagged. |
| 439 | 7-7 | The `test/unit/schema-drift.test.ts` suite is left red at 107 pass and 1 fail on the pre-existing `gate_dwell_metric` CRLF-versus-LF mismatch that Task 1.5 n... | `normalizeSql` at test/unit/schema-drift.test.ts:14-20 collapses all whitespace (CRLF included) before comparison; session memory records the gate_dwell_metric CRLF failure fixed in 8.3 and no longer in the noise floor. |
| 469 | 6-2 | Lot-controlled consumption enforcement and FEFO-directed staging are out of scope (Story 6.4, FR-MO-11): staging/issue in 6.2 accept lot-less drains exactly ... | 6.4 done; `src/compliance/production-material.ts:440` LOT_REQUIRED on `item.lot_controlled`, `:1105` backflush resolves lots from the FEFO drain |
| 470 | 6-2 | Completion quantity tolerances and over/short completion are out of scope (Story 6.3, FR-MO-09): confirmations in 6.2 are not capped against the order quanti... | 6.3 done; `src/compliance/production-completion.ts:416-426` cumulative tolerance + `OVER_COMPLETION_TRANSACTION_TYPE` DOA (`:74`) |
| 471 | 6-2 | Completions, co/by-product posting, and QC Hold hand-off are out of scope (Story 6.3, FR-MO-07): the confirmation event records backflush consumption only; f... | `production-completion.ts:558` outputClass primary/co_product/by_product; `:617` hand-off via `receiveQcCompletion` (`:30`) |
| 472 | 6-2 | Process scrap declarations (Story 6.3, FR-MO-08) and consumption variance reporting (Story 6.4, FR-B-08) are out of scope: 6.2's explosion consumes the BOM's... | `production-completion.ts:69` `production_order.scrap_declared` + `production_scrap_declaration` projection; `production-order.ts:30` `computeConsumptionVariance` |
| 473 | 6-2 | Closure gate, lot genealogy, offline execution and replay are out of scope (Story 6.4, FR-MO-11/12/13): no edge capture module and no PowerSync bucket exist ... | 6.4 done; `production-order.ts:910` server-derived closure verdict; `edge/src/sync/connector.ts:184` production replay codes; `src/api/v1/edge.ts` `assertEdgeProductionEventAllowed` |
| 475 | 6-2 | Quarantine/quality-hold screening of staging source bins is out of scope: stock-class scoping is the Phase-1 guard; lot QC-hold enforcement arrives with 6.4'... | `production-material.ts:761` and `:900` call `assertQcGateAllows` on staging and issue |
| 476 | 6-2 | Rework orders from QC dispositions are out of scope (Story 6.3, blocked on Epic 8). | `production-order.ts:632-636` rework linkage validated against `qc.rework_requested`; `src/production/rework-order.ts` |
| 486 | 6-2 | Cumulative confirmations are not bounded by the order quantity, so repeated confirmations inflate WIP and drained stock past 100% of the order (stock exhaust... | Same root as 470; `production-completion.ts:416` cumulative tolerance |
| 516 | 8-1 | SOD recorder set read directly from domain_events instead of a read projection [src/compliance/quality.ts:1296] - CLOSED by Story 8.2 (2026-08-29): `knownRes... | `src/compliance/quality.ts:2224` `listResultRecorderUserIds` |
| 517 | 8-1 | Registry `streamType` is not consumed by any enforcement path; the QC stream gate uses a hardcoded `QUALITY_EVENT_TYPES` set that can drift from `SUPPORTED_E... | `test/unit/quality-event-registry.test.ts:23` pins the set |
| 519 | 8-1 | The seven QC DDL files lack the `ADD COLUMN IF NOT EXISTS` and drop-then-add self-heal patterns sibling projections use; Story 8.3 must drop-and-re-add `chk_... | `qc_inspection_task.sql:140` `pg_get_constraintdef` guarded drop-then-add |
| 521 | 8-1 | Transfer passes the lot UUID into the number-keyed `stock_balance` ledger, so a lot-specified transfer cannot find its stock and fails INSUFFICIENT_STOCK [sr... | `src/compliance/transfer-request.ts:31` `lotNumberForUuid` bridge |
| 528 | 8-1 | Task read routes (`listQcTasksHandler`, `getQcTaskHandler`) enforce module/function RBAC but no location scope, so a single-site `qc` read grant can enumerat... | `src/api/v1/quality.ts` uses `permittedLocationsForModuleScope` (3 sites) and site-stamped audits |
| 622 | 8-4 | Client-supplied `decided_at` / `logged_at` timestamps are unbounded in both past and future directions across the QC seam, and for Story 8.4 `released_at` is... | For the 8.4 scope: assertRetentionClockBounds applied at quality.ts:4092 (logged_at) and :4156 (decided_at); :291 documents why. Family-wide remainder is the accepted 8.1 occurred_at decision. |
| 627 | 8-4 | `getQcInspectionTaskByLotId` returns `rows[0]` with no `ORDER BY` and no `LIMIT` while nothing in schema enforces one task per lot; if a lot ever carries two... | `CONSTRAINT uq_qc_inspection_task_lot UNIQUE (lot_id)` at read/projections/qc_inspection_task.sql:70 (guard block :100) - schema does enforce one task per lot, so rows[0] is deterministic. |
| 638 | 6-3 | Expected-versus-actual consumption reconciliation and the FR-B-08 consumption variance report are out of scope (Story 6.4); this story writes the scrap decla... | Story 6-4 done (sprint-status.yaml:936); production_consumption_variance table exists (init-db.sql:11711, read/projections/production_consumption_variance.sql). |
| 639 | 6-3 | The FR-MO-12 closure gate itself is out of scope (Story 6.4), including the zero-WIP and every-lot-dispositioned checks that read this story's `short_closed_... | Story 6-4 "Lot Genealogy, Closure, and Offline" done (sprint-status.yaml:400,936); short_closed_at is read at production-completion.ts:299,899. |
| 640 | 6-3 | Offline completion capture and a `CENTRAL_ONLY_OPERATION` guard for the three new event types are out of scope (Story 6.4, FR-MO-13); the production stream s... | Story 6-4 includes Offline Execution and is done (sprint-status.yaml:936); edge/src/sync/connector.ts references production. |
| 664 | 8-6 | `licence_number`/`sku` have no case-folding, so near-duplicate casing can produce duplicate register rows [read/projections/compliance_bis_licence.sql] - def... | compliance_bis_licence.sql:88-90 unique index on lower(btrim(licence_number)); label_master.sql:74-75 same for label_version. |
| 665 | 8-6 | `label_master` has no unique `(sku, label_version)` and no CHECK enforcing the draft to approved to superseded transition path [read/projections/label_master... | uq_label_master_version at label_master.sql:74; draft-only approval enforced in applier master-data.ts:619-621 (LABEL_VERSION_NOT_DRAFT); CHECK cannot see OLD (sql:25). |
| 667 | 8-6 | `uq_compliance_bis_licence_scope` keys on `(licence_number, sku, site)` without `valid_from`, so a legitimate renewal cannot be inserted as a second row [rea... | 8.7 chose in-place renewal (sprint-status.yaml:1099); requireNoOverlap master-data.ts:239-262 plus PATCH path :452. |
| 668 | 8-6 | `findValidBisLicence`'s tie-break (`site_id IS NOT NULL DESC, valid_to DESC, licence_id`) is arbitrary when two distinct licence numbers are simultaneously v... | BIS_LICENCE_OVERLAP guard (master-data.ts:239-262) prevents overlapping same-scope windows, so the tie is unreachable via the write path (ORDER BY unchanged at compliance_bis_licence.ts:100). |
| 688 | 8-6 | `r_number` licence type is declared in the schema but every test seeds `'cml'` only [test/integration/story-8-6.test.ts: seedLicence]. | story-8-6.test.ts now has 1 r_number hit; story-8-7.test.ts exercises r_number/supersede (6 hits). |
| 696 | 8-6 | Label supersede-then-reapprove workflow is untested [test/integration/story-8-6.test.ts]. | Re-approval of non-draft is rejected LABEL_VERSION_NOT_DRAFT (master-data.ts:619-621) and story-8-7.test.ts covers supersede workflow. |
| 721 | 8-7 | Story 6.4 schema, payload interfaces and migration-list entries ride in the same working tree as Story 8.7, so the 8.7 schema gates and drift pins validate a... | Both committed and done (sprint-status.yaml:936,1099; HEAD is 3868f81 on 9-4). Split-at-commit is moot. |
| 727 | 8-7 | No test asserts that the BIS expiry sweep is actually registered on a timer in `server.ts`. Every test invokes `runBisLicenceExpiryCycle` by direct import, s... | server.ts:1244-1277 exports backgroundCycles(); test/unit/background-cycles.test.ts:121 asserts the schedule list (all four cycles). |

### Killed

Table 7 lists entries the room will not do.

Table 7: Killed entries

| Ref | Story | Item | Reason |
| 333 | 7-4 | maintenance_storekeeper is a NEW role string with no registry to validate it against: roles are free-form text in user_role_assignments, so a deployment that... | No role is seeded anywhere; every role (maintenance_supervisor included) exists only through user_role_assignments. Deployment checklist item, not code; story-7-4 pins the role name. |
| --- | --- | --- | --- |
| 5 | 9-3 | Own-material path has no over-balance, replay, or concurrency integration arm (only consumption has all three) [test/integration/story-9-3.test.ts] - deferre... | Coverage symmetry only; own-material drains through `applyStockIssue` (src/compliance/custody-ledger.ts:786), already covered by pre-existing stock-issue tests. test/integration/story-9-3.test.ts:959 covers the AC6 happy path. |
| 6 | 9-3 | Concurrency test only races two full-balance consumers (10 vs 10), not a partial-overlap case (6 vs 6) - deferred, same code path, lower marginal value | Same code path as the existing race (story-9-3.test.ts:1276); no new branch exercised. |
| 8 | 9-3 | `qtyToScaled`'s more-than-3-decimal-places behavior isn't pinned by a dedicated unit test - deferred, low risk, all real inputs are DB-cast strings | Same root as line 17; test/unit/custody-statement.test.ts:67,81-82 already pins pad and reject cases. Pinning a truncation nobody wants is coverage theatre. |
| 9 | 9-3 | `expectReject`'s field-hint check is a substring match, not exact - deferred, low risk given the current field-name set has no collisions | test/unit/custody-ledger-predicates.test.ts:150 `err.message.includes(fieldHint)`; hint is matched against a message, exact match would be wrong anyway. |
| 10 | 9-3 | Unit UUID-shape validation loop only covers the consumption shape, not own-material's id fields - deferred, coverage-symmetry gap only, underlying logic is s... | test/unit/custody-ledger-predicates.test.ts:277-292 loops consumption only; the 205-232 derived-field loop already covers own-material. Symmetry only, shared validator. |
| 18 | 9-3 | `padLeft` truncates from the wrong end for overflowing numeric columns in the text renderer - deferred, only reachable at ~10^13+ magnitude quantities | custody-statement.ts:166-168; width 20 on NUMERIC(18,3) means overflow needs 10^13+ units. Unreachable. |
| 19 | 9-3 | `buildCustodyStatement` trusts caller-supplied row order with no runtime verification - deferred, matches documented design | Documented contract at custody-statement.ts:102-103 ("The builder does not re-sort so the SQL order is the single truth"). Only caller is the SQL with ORDER BY. |
| 23 | 9-3 | `custody_balance_after` advertised as a derived field for own-material but never written back [src/compliance/custody-ledger.ts:666] - deferred, own-material... | custody-ledger.ts:126 lists it in OWN_MATERIAL_DERIVED_FIELDS (input is rejected), applier at :751-812 never writes it, while :738 and :1304 do for consumption/return. Rejecting a meaningless input field is fine; nothing reads it. |
| 24 | 9-3 | Replay path skips `assertSiteWriteAccess` when the order no longer exists but the idempotency key was already used [src/api/v1/service-orders.ts:571,576] - d... | src/api/v1/service-orders.ts:604-614 `if (existing) assertSiteWriteAccess(...)` unchanged. Requires a service_order row to vanish post-posting; no delete path exists (app_user lacks DELETE). |
| 25 | 9-3 | Two different drain paths for consumption (gated `applyStockBalanceProjection`) vs own-material (`applyStockIssue` directly) [src/compliance/custody-ledger.t... | By design (decision 6): custody-ledger.ts:686 `applyStockBalanceProjection` vs :786 `applyStockIssue`. Header at :44 documents lock order. Not a gap. |
| 26 | 9-3 | `entry_id` provenance differs between receipt (server-minted) and consumption/own-material (client-minted) write paths - deferred, already disclosed in Debug... | custody-ledger.ts:692,800,1061,1228 use the client ids (`p.consumption_id` etc). Already disclosed; unifying would break 9.2 receipt idempotency. |
| 27 | 9-3 | `format` query param on statement route is case-sensitive - deferred, minor usability gap | service-orders.ts:1291-1293 rejects anything but exact `json`/`text` with a clear 400. Normal API behaviour; YAGNI. |
| 28 | 9-3 | `postCustodyEvent` re-fetches and linear-scans the full ledger to find the just-inserted row [src/api/v1/service-orders.ts:606-607] - deferred, minor ineffic... | service-orders.ts:644-645 `listCustodyLedgerByOrder(...).find(...)`; same idiom at :766 for outputs. Per-order row counts are small; premature optimisation. |
| 33 | 9-3 | `business_date` correctness depends entirely on applier-side IST conversion, no DB constraint ties it to `occurred_at` [read/projections/custody_ledger_entry... | read/projections/custody_ledger_entry.sql:45 plain `DATE NOT NULL`, no CHECK. A timezone-aware CHECK across two DDL files plus the drift test buys nothing; single writer already converts. |
| 34 | 9-3 | `MIN(uom)` masks a cross-row UOM mismatch per sku in closing balances [src/read/projections/custody_ledger_entry.ts:199] - deferred, theoretical, no current ... | src/read/projections/custody_ledger_entry.ts:211 unchanged. Theoretical; no writer produces mixed UOM per sku. Same root as line 15 if the PO ever cares about UOM. |
| 40 | 8-8 | Round 2: the raise route's write-site assertion is TOCTOU against the applier - a QC inspection task created between the route's unlocked read and the applie... | quality.ts:2789-2850 still checks in-route (`assertWriteSiteAccess(req, task.site_id)` at ~:2840). Window is between an unlocked read and an in-transaction re-read of a task that is created seconds apart by the same tenant; the applier's derived site_id is still correct. Moving auth into the applier is a design change nobody needs. |
| 46 | 6-4 | Permanent error-code lists hand-duplicated across `edge/src/sync/connector.ts` and `src/sync/upload.ts` with no shared constant - deferred, requires a cross-... | edge/src/sync/connector.ts:23 and src/sync/upload.ts:18 still separate sets with cross-reference comments (:205). Edge is a separate build; a shared package for one Set is not worth it. |
| 48 | 6-4 | `consumption-variance.ts` `requirementByLine` map silently overwrites on a same-`bom_line_id` collision across supply methods, contradicting the file's own "... | src/production/consumption-variance.ts:142 comment and :188-189 unchanged; no reproduction path found, lines are disjoint by construction. |
| 49 | 6-4 | `getActualConsumptionByLine` SQL's `bool_or`/`MIN` aggregation misrepresents `supply_method`/item/sku when one BOM line has both directed-issue and backflush... | consumption-variance.ts:69-79 unchanged. Same low-probability class as line 48; only `has_directed_issue` is derived and that is exactly what `bool_or` answers. |
| 50 | 6-4 | Variance line silently dropped (no log) when ledger data can't identify `component_item_id`/`component_sku` [src/production/consumption-variance.ts] - deferr... | consumption-variance.ts:212 `continue`; file has no logger import. Only reachable when BOTH requirement and ledger rows lack item/sku, which the DDL forbids. |
| 51 | 6-4 | `assertClosureAllowed`'s WIP-zero numeric cast has no try/catch around a bad-shape DB error [src/compliance/production-order.ts] - deferred, would 500 instea... | src/compliance/production-order.ts:390 casts via `$1::numeric` in SQL; a bad shape is a projection-code regression, 500 is the right outcome for that. |
| 56 | 1-1 | `readStream` has no pagination or limit [src/events/store.ts:396-421] - deferred, pre-existing (Not required in Story 1.1) | Only caller is `src/api/v1/events.ts:220` (single-aggregate stream); per-aggregate streams are short, nobody pages |
| 57 | 1-1 | Global idempotency uniqueness is likely too broad [events/domain_events.sql:20] - deferred, pre-existing (Matches spec requirements) | `events/domain_events.sql:14` `uq_idempotency` is the spec'd design; idempotency keys are client UUIDs |
| 59 | 1-1 | Migration has no versioning and a brittle path [src/events/migrate.ts:210] - deferred, pre-existing (Full migration system out of scope) | Guarded idempotent DDL is the pattern used through Epic 9; a version table is a rewrite with no consumer |
| 60 | 1-1 | Extra/unknown properties are silently accepted [src/events/store.ts] - deferred, pre-existing | No `.strict()` anywhere; permissive payloads are normal for event envelopes, per-type guards validate required fields |
| 65 | 1-3 | `range_digest` is computed at read-time over the returned page, not via write-time hash chaining [src/api/v1/audit.ts:42-44] - deferred, matches existing spe... | `src/api/v1/audit.ts:122,128` by-design; tamper evidence rests on DB immutability triggers |
| 71 | 1-3 | Archive CLI loads the entire eligible row set into memory with no batching [src/cli/archive-audit-log.ts:19] - deferred, no eligible rows can exist until 8 f... | No eligible rows until 8 FY after go-live (`archive-audit-log-core.ts:24`) |
| 77 | 1-7 | `location.disputed` generated event uses a narrow raw insert instead of the central `persistEvent` path [src/compliance/location.ts:68-81] - closed as by-des... | Ledger itself marks it closed by-design, not deferred work |
| 78 | 1-7 | Calibration lockout is a non-transactional TOCTOU read [src/compliance/calibration.ts:32] - deferred, pre-existing architectural pattern shared with the tagg... | `src/compliance/calibration.ts:39` plain status read; flip window is admin maintenance writes only |
| 83 | 1-8 | `svc_powersync` (WITH REPLICATION) is created only in `deploy/compose/init-db.sql`; the guarded grant in `sync/migrations/powersync.sql` silently skips on a ... | Same guarded-grant pattern for every role (`init-db.sql:6`, `sync/migrations/powersync.sql:3`) |
| 84 | 1-8 | AC4 "related balance or state updated exactly once" is not exercised because the duplicate test uses an inert `maintenance` stream with no projection [test/i... | `test/integration/story-1-8.test.ts:340-368` asserts `count(*)=1` in domain_events; coverage-symmetry only |
| 89 | 1-10 | `bootstrap-runner.sh` is not idempotent against a partially-provisioned host: a corrupt cached runner package is not re-verified, and re-running against a ho... | Docstring scopes it to a clean host; re-image and re-run |
| 90 | 1-10 | `backend-tests` and `spine-acceptance-contract` CI jobs duplicate the entire postgres provisioning block verbatim [.github/workflows/ci.yml:44-57,72-90] - de... | `.github/workflows/ci.yml:44-57,77-90`, two YAML blocks, isolation is required |
| 105 | 1-11 | Task 6.1's non-blocking guarantee is tested at the function level (`emitNotification()` called directly) rather than through a real emitting HTTP handler ret... | Coverage-symmetry only; `story-1-11.test.ts:230,289,346` cover the contract |
| 106 | 1-11 | Value-band and vacation-delegation escalation resolution (Task 4.3) is not wired; only `findRoleHolder` is used, `findFirstActiveDoaEntry`/`findActiveDelegat... | Consumers call the helpers directly now (`doa.ts:423`, `indents.ts:83`, `cycle-counts.ts:146`, `instruments.ts:277`); wiring `escalate.ts` is moot |
| 112 | 2-2 | Zero permitted locations returns 200 empty instead of 403 [src/api/v1/stock.ts:42-45] -- deferred, consistent with existing RBAC handler patterns in the code... | `stock.ts:72-73` filter; consistent RBAC pattern repo-wide |
| 117 | 2-3 | Serial quantity reconciliation compares a summed JS float against the event quantity (`0.1 x 3 !== 0.3`) [src/compliance/lot-serial-validation.ts:362] - defe... | `:732-737` strict `!==`; serials are discrete, no fractional serial exists |
| 119 | 2-3 | `override_expired_lot: true` from a non-override role is rejected `403 FUNCTION_ACCESS_DENIED` even when the lot is not expired, because the role gate fires ... | `:442-448` fail-closed on the assertion; no client sends the flag by default |
| 123 | 2-5 | `SUPPORTED_EVENT_TYPES` registry added to `schema.ts` but never imported or enforced by `store.ts` - the four transfer event types are unvalidated dead metad... | Registry now has a real consumer (`src/compliance/business-stream.ts:62-66`); a store allowlist is YAGNI given per-type guards |
| 124 | 2-5 | Stable error codes `APPROVAL_REQUIRED`, `QUANTITY_EXCEEDS_APPROVED`, `LOT_MISMATCH` not registered in the architecture stable-error list [planning-artifacts/... | `ARCHITECTURE-SPINE.md:337` has `APPROVAL_REQUIRED` but is already stale for dozens of later codes; a two-code patch is cosmetic |
| 125 | 2-5 | Approve/reject mutate `transfer_request.status` via direct `UPDATE`, and the persisted `approval_decided` event has no projection handler, so the read-model ... | Approval stays an amendment of the request row; no approval_decided projection. |
| 130 | 2-7 | `reorder_point` = ceil(avg_daily_demand * lead_time_days + safety_stock) is computed and stored into NUMERIC(18,6) with no upper bound, so extreme demand/lea... | Needs demand x lead > 1e12; unreachable magnitude |
| 131 | 2-7 | Minimum sample-day guard is hardcoded (DEFAULT_MIN_SAMPLE_DAYS = 2) but Task 5 specifies a *configured* minimum [src/compliance/planning-jobs.ts:45] - deferr... | `planning-jobs.ts:48`; nobody has asked for it to be configurable |
| 136 | 2-9 | No batch-size bound on the ERP sync array: runErpSync processes the whole posted array in one transaction, one savepoint per record. Low risk (trigger RBAC-r... | RBAC-restricted trigger role; no bound needed |
| 137 | 2-9 | Per-record exceptions lost on late infra rollback: exceptions queued inside the batch transaction are discarded if a later COMMIT/statement fails and rolls b... | Self-heals on next retry |
| 143 | 3-2 | Idempotent migration `ADD CONSTRAINT UNIQUE (correlation_id)` guards on constraint name only, not data; DB init aborts if any pre-existing duplicate `correla... | Per-event random UUIDs; `init-db.sql:2225-2232` already cleans the duplicate index; theoretical |
| 148 | 3-4 | RBAC helper divergence - `receiving.ts`'s role-check additionally allows `module === 'inventory'` where `asn.ts` doesn't; both independently redefine the sam... | `receiving.ts:85` intentionally admits `inventory`; cosmetic |
| 149 | 3-4 | Unrelated `gate_event` unique-index hotfix bundled into this story's migration file - no relation to goods receiving. [deploy/compose/init-db.sql] - deferred... | Historical, cosmetic |
| 150 | 3-4 | `resolveSiteByToken`'s RBAC pre-check uses the latest `weighbridge_event` row regardless of status, while the compliance layer (correctly) uses the first `ac... | `receiving.ts:136` is a defense-in-depth pre-check only |
| 156 | 3-5 | Reslotting job upserts each SKU independently without transaction wrapping; partial state on crash auto-heals on next run since the job recomputes from scrat... | On-demand recompute, self-heals next run |
| 157 | 3-5 | Location facts silently skipped when putaway task has no `lot_id`; completion succeeds but lot-location projection becomes stale. Lot-less putaway is an edge... | `putaway.ts:150` guard; receiving always sets `lot_id`, latent |
| 167 | 3-7 | Missing `lot_on_hold_blocked` BOOLEAN field in `DispatchDispatchedPayload` - informational field that would indicate whether dispatch was gated by a hold, no... | Informational field nobody reads |
| 168 | 3-7 | Missing FK constraints on `packing_record.dispatch_order_id` to `erp_sales_order.id` and `dispatch_document.generated_by` to `users.id` - orphan records poss... | Project pattern omits FKs on projections |
| 173 | 3-7 | `renderLabels` returns an empty array when `carton_count` is 0 even though label generation was requested - pre-existing, not touched by this fix pass. [src/... | 0 cartons = 0 labels; `document-renderer.ts:198-199` is correct |
| 174 | 3-7 | Schema-drift `EXPECTED` array in `test/unit/schema-drift.test.ts` has no entry covering `dispatch_order_status`'s new columns (`packed_at`, `packed_by`, `dis... | Entry exists at `:393-396`; the guard pins constraints/indexes only (`:1148` note), columns are outside its mechanism |
| 187 | 3-8 | Putaway tasks directed before this migration never receive a `zone_id`. The additive `zone_id` column ships with no backfill statement, and `setDirectedSugge... | `putaway_task.sql:91` additive, no backfill; system is pre-go-live, no production rows predate it |
| 188 | 3-8 | GRN headers created before this migration can never backfill `received_at`. The `ON CONFLICT (grn_id)` clause updates only `status` and `updated_at`, so a he... | `grn.ts:167-169` immutability is by contract; same pre-go-live reasoning as line 187 |
| 196 | 4-3 | Online duplicate pre-check TOCTOU - `findOpenDuplicate` runs before `persistEvent` with no locking. Concurrent request can create duplicate between pre-check... | Seam already catches 23505 inside the txn (src/compliance/indent.ts:315-320); the route pre-check is UX only. |
| 197 | 4-3 | No FK from indent_line to indent - `indent_line.indent_id` has no FK constraint. Orphaned lines possible if indent insert fails after lines inserted. [read/p... | Still no REFERENCES in read/projections/indent_line.sql; lines are inserted in the same txn as the indent. Theoretical orphan. |
| 198 | 4-3 | Missing index on indent.status for non-open statuses - Queries for `status = 'closed'`, `'rejected'`, `'cancelled'`, `'ordered'` do sequential scan. [src/rea... | Only uq_indent_number_ext + partial idx_indent_dup_window exist (indent.sql:47-48); list query is LIMIT/OFFSET paged (src/read/projections/indent.ts:134). No measured perf problem. |
| 199 | 4-3 | Missing index on indent.requester_user_id for cross-status queries - `mine=true` query with non-open status does full scan. [src/read/projections/indent.ts:1... | Same root as 198; paged query, no evidence of slowness. |
| 200 | 4-3 | indent_number_seq doesn't reset per year - Single sequence, globally monotonic but year-ambiguous. `IND-2026-9999` followed by `IND-2027-10000`. [read/projec... | indent.sql:81 single sequence; globally unique is the documented behaviour. Cosmetic. |
| 201 | 4-3 | cancelIndentBase doesn't validate state before persisting - Persists `indent.cancelled` event without checking status. Seam validates inside transaction. [sr... | src/api/v1/indents.ts:527-536 still loads the indent then calls persistEvent; seam rejects inside the txn so nothing is committed on a bad state. Pre-check would only change 409 timing. |
| 202 | 4-3 | applyIndentClosed doesn't verify purchase_order_id - Allows closing any ordered indent without checking PO is set. [src/compliance/indent.ts:850-857] - defer... | applyIndentClosed (indent.ts:771) requires status = 'ordered'; applyIndentOrdered (indent.ts:702-728) sets status and purchase_order_id in the same UPDATE, so the invariant holds. |
| 203 | 4-3 | Search pattern escaping non-standard PG setting - Escapes `%`, `_`, `\` for ILIKE but assumes `standard_conforming_strings` is on. [src/read/projections/inde... | src/read/projections/indent.ts:114 unchanged; setting is PG default since 9.1. |
| 204 | 4-3 | Edge capture minimal client-side validation - Only validates SKU and quantity. Required fields rely on HTML `required` attributes. [edge/src/components/inden... | edge/src/components/indent-capture.tsx:102-108 uses HTML required; server validates everything. |
| 210 | 4-4 | Concurrent same-po_id purchase_order.drafted persists a phantom domain event [src/compliance/purchase-order.ts:360-365] - deferred, matches the accepted inde... | purchase-order.ts:406 still swallows 23505; reachable only via crafted direct /events with a duplicate po_id. Accepted indent precedent. |
| 221 | 4-6 | Concurrent db:migrate runs can abort on TOCTOU inside guarded DO blocks [src/events/migrate.ts:73] - deferred, pre-existing repo-wide pattern: two runners ca... | migrate.ts still has no advisory lock (loop at 259-262). Two simultaneous migrate runners is an ops error, not a product path; rerun heals. |
| 222 | 4-6 | init-db.sql runs outside any transaction [deploy/compose/init-db.sql] - deferred, pre-existing file-wide pattern: an interrupted container first boot (OOM, e... | Unchanged; documented re-run-heals stance via db:migrate. Container first-boot OOM is an ops concern. |
| 226 | 4-6 | MSME ageing report has no pagination or row limit [src/read/projections/msme_ageing.ts:39] - deferred, pre-existing repo-wide pattern: every Phase 1 list end... | msme_ageing.ts:46-74 still unbounded; sibling lists (purchase_order.ts:144, indent.ts:134) ARE paged now, but the ageing report is a whole-set statutory feed by design (msme_ageing_feed regenerates the full set). |
| 232 | 4-5 | The match compares against the PO's CURRENT line set, not a snapshot taken at match time [src/compliance/three-way-match.ts:236] - deferred, no PO line amend... | No purchase_order.line_amended event exists (grep purchase-order.ts, three-way-match.ts); tolerance_snapshot is captured (three-way-match.ts:325). Only revisit if PO line amendment is ever added. |
| 245 | 4-2 | Responsiveness holiday calendar is deployment configuration with an empty default [src/config/index.ts config.scorecard] - deferred: `SCORECARD_RESPONSIVENES... | config/index.ts:411 parses SCORECARD_RESPONSIVENESS_HOLIDAYS; this is a deploy checklist item, not code work. |
| 269 | 5-2 | Partial-migration failure window: `bom.sql` and `bom_revision.sql` are applied as separate implicit transactions by migrate.ts, so a process death after bom.... | migrate.ts:259-262 still one pool.query per file; re-run heals, documented stance. Process death exactly between two files is unreachable magnitude. |
| 279 | 5-6 | `bom.sync_conflict_raised` for an unknown-BOM conflict uses the SCIM system-actor UUID as the stream id sentinel, conflating the actor and stream namespaces ... | erp-projections.ts:43-44 unchanged; cosmetic namespace conflation, no functional break. |
| 281 | 5-6 | `bom_line` is_released_structure comment mirror diverges between the canonical file and init-db.sql [read/projections/bom_line.sql] - deferred, pre-existing ... | Diffed: SQL bodies (ALTER + backfill UPDATE) are identical between bom_line.sql:117-125 and init-db.sql:4251-4258; only the comment prose differs. Cosmetic. |
| 287 | 7-1 | Failed registrations write no audit entry (409 DUPLICATE_ASSET, 400 INVALID_PARAMS) [src/api/v1/assets.ts:127-129] - deferred, platform convention matching t... | Matches the supplier precedent; only compliance.ts has an AUDITED_REJECTIONS set for DOA codes. No FR requires audit of rejected registrations. |
| 291 | 7-2 | Schema-drift gate cannot detect extra constraints in the CREATE body: extractCreateTable compares init-db vs canonical only, and the guarded extractDoBlock c... | test/unit/schema-drift.test.ts:22 extractCreateTable compares init-db vs canonical byte-for-byte after normalize, so an extra constraint in one file IS caught; only the story-list claim is overstated. Harness wording, not a gap. |
| 300 | 7-2 | TIMESTAMPTZ columns typed string while pg returns Date objects: created_at/last_reading_at/completed_at etc. are Date at runtime; every consumer today is JSO... | No setTypeParser anywhere in src/; every consumer is JSON/Date-safe as the bullet itself says. Typing cosmetics. |
| 316 | 7-3 | Superseding an active SLA policy (edit or deactivate) is out of scope for Phase 1: a second POST on an occupied (criticality_class, safety_flag) key returns ... | Phase 1: SLA policy change is a data fix. |
| 321 | 7-3 | The schema-drift gate compares index NAMES only (.includes(indexName)) and ignores statement ordering, so a mirror-ordering drift (indexes created before the... | schema-drift.test.ts:2562-2564 still `.includes(index)`; the CREATE TABLE body is compared whole, so index ordering only matters if a migration is written wrong, which migrate-twice catches. Harness cosmetics. |
| 325 | 7-4 | Amending or removing an asset parts-list line is out of scope for Phase 1: a second POST on an occupied (asset_id, sku) key returns 409 ASSET_PART_ALREADY_LI... | Phase 1: parts-list correction is a data fix. |
| 332 | 7-4 | The where-used read is not paginated beyond a flat limit/offset over asset_parts_list joined to asset, and gives no total count, so a caller cannot tell a fu... | Flat limit/offset is the platform list convention; a parts list per SKU is small. |
| 337 | 7-4 | Crash between alert commit and notification emission permanently loses that day's notification [src/maintenance/spares-jobs.ts:196-210, 302-314] - deferred, ... | Accepted AD-17 crash-window tradeoff, identical to every other job; no retry infrastructure exists to reuse. |
| 339 | 7-4 | Spare SKU canonicalization is case-insensitive (lowercase) while the Epic 2 item_master/stock_balance are case-sensitive [src/compliance/maintenance-spares.t... | Bullet already records the decision as RESOLVED keep-the-story-choice; maintenance-spares.ts:121 comment documents it. |
| 346 | 7-5 | Per-parameter and per-range calibration points, measurement uncertainty budgets and traceability chains are not modelled: a certificate carries validity date... | YAGNI for a Phase 1 register; no FR cites it. |
| 350 | 7-5 | status_event_version is written as NULL on every register-driven calibration status change: the register writes instrument_calibration_statuses from INSIDE p... | Column is nullable by design (instrument_calibration.ts:11,37); status_event_id carries the traceability; no reader depends on the version. |
| 365 | 7-6 | A completed work order on a weighbridge asset invalidates the stamp regardless of the work order's content: ANY completion flips a compliant weighbridge_lega... | Documented deliberate fail-closed choice (maintenance-plan.ts:854); re-stamp is the remedy. |
| 371 | 7-6 | The 423 weighbridge lockout runs in the pre-transaction assert block, ahead of the in-transaction idempotency replay short-circuit, so a retry of an already-... | store.ts:522 lockout is pre-transaction, replay at :1235; a retry of a lapsed-stamp weighment being refused is acceptable fail-closed and vanishingly rare. |
| 377 | 7-6 | The scan loop is unbounded: `listOverdueExaminationsDue` takes no LIMIT and no cursor, and the loop opens one pool connection and one transaction per due row... | statutory_examination.ts:224 still no LIMIT, but the row count is bounded by the number of statutory examinations per plant (tens, not thousands). |
| 379 | 7-6 | The AC4 broadcast dedup evaluates `metadata->>'correlation_id'` against every `notification.created` row in `domain_events` with no supporting expression ind... | maintenance.ts:3710 query is filtered by idx_domain_events_type (init-db.sql:25); returns zero rows on the normal path; notification volume is small. |
| 380 | 7-6 | The per-asset cost rollup ADDs into `NUMERIC(14,3)` with no ceiling, so once an asset's lifetime running total passes the column range every subsequent work-... | 10^11 currency lifetime per asset is unreachable; already a clean 422 (store.ts:2163). |
| 384 | 7-6 | No RBAC negative path exists for any of the nine Story 7.6 routes: every write in the suite uses supervisor headers, and the only 403s asserted are the DOA `... | Coverage-symmetry only; requireRole is the shared platform middleware already tested elsewhere. |
| 385 | 7-6 | The concurrency tests cannot reach the 23505 race path their titles name: `applyStatutoryExaminationRecorded` and `applyAssetOperationalStatusProjection` bot... | Serialization by the asset FOR UPDATE lock is the intended behaviour; the 23505 branch is a backstop. Rename-only. |
| 386 | 7-6 | Work orders are hand-inserted straight into `maintenance_work_order` with a phantom `plan_id`, so all nine cost and stamp-invalidation tests complete work or... | Fixture shape; story-7-6.test.ts:252 insertOpenWorkOrder. Rewriting fixtures through the 7.2 generator is churn without a defect. |
| 387 | 7-6 | Ten `persistEvent(... as any)` casts appear in the direct-event forgery tests, disabling the compile-time envelope contract in exactly the tests that exist t... | Now 13 casts (story-7-6.test.ts:655,1049,1299 ...); forgery tests need to break the type on purpose. Cosmetic. |
| 389 | 7-6 | The Story 1.9 Spine allowlist widening asserts membership only, through a `.sort()`ed `deepStrictEqual` inside a `before()` hook, so the load-bearing route o... | story-1-9.test.ts:170,:632 still sorted membership; route-order regressions surface as 404s in the story suites themselves. |
| 397 | 6-1 | Existence resolution of `source_reference_id` against the Story 2.9 `erp_sales_order` and Story 4.3 indent projections. Phase 1 records the reference on the ... | production-order.ts:219-224 records a bounded string by design; an FK to two heterogeneous projections is YAGNI for a reference field. |
| 404 | 7-7 | No reservation-time warranty prompt is added to the Story 7.4 spare-reservation flow (Binding Decision 14). The FR-M-10/11 check is anchored to work-order cr... | No FR asks for it; work-order-creation anchor is the epic decision. |
| 405 | 7-7 | Insurance claims administration stays out of scope (PRD Non-Goal). Policy references, coverage windows and expiry alerts are recorded; claims themselves are ... | PRD non-goal, not a gap. |
| 406 | 7-7 | No edge or offline capture path exists for coverage recording or the warranty override, and no PowerSync sync rule replicates either (Binding Decision 12). E... | Coverage recording is desk work; edge already carries `warranty_flagged` on the worklist (edge/src/local-db/schema.ts:145, worklist.ts:45) which is all the technician needs. |
| 408 | 7-7 | The one-override-per-work-order grain is NOT pre-checked in `recordWarrantyOverrideBase`, unlike the other pre-checks the story's Task 7.1 lists. A handler p... | Recorded as deliberate deviation; the replay contract is the correct behaviour, seam + 23505 resolver enforce the grain. |
| 421 | 6-1 | The release gate's `depth_truncated` path has no test anywhere in the repo: story-5-5/5-6 only assert `depth_truncated: false`, and triggering the truncation... | Fail-closed branch; only `BOM_MAX_DEPTH` env (src/config/index.ts:497) could reach it. Coverage-symmetry only. |
| 422 | 6-1 | The release gate's `empty_requirement_set` path has no test and is not constructible through the API: BOM creation requires at least one line (`BOM_LINE_REQU... | Unreachable by construction (`BOM_LINE_REQUIRED`); defensive branch. |
| 423 | 6-1 | The delegated Table 5 error codes `COMPONENT_SKU_UNRESOLVED`, `EXPLOSION_QUANTITY_INVALID` and `BOM_EXPLOSION_CYCLE_DETECTED` have no tests and are not reach... | Unreachable via API; `EXPLOSION_QUANTITY_INVALID` is in fact tested at test/integration/story-5-5.test.ts:862-874. |
| 430 | 7-7 | `asset_coverage.updated_at` is defined `TIMESTAMPTZ NOT NULL DEFAULT now()` but is written by no accessor and no trigger, while still being exported on `Cove... | Cosmetic; column is dead until 403's amendment path lands (the only `updated_at = now()` at asset_coverage.sql:155 targets maintenance_work_order). |
| 431 | 7-7 | The uniqueness grain folds case but not whitespace: `uq_asset_coverage_reference` keys on `lower(reference_number_ext)` with no `btrim`, and `chk_asset_cover... | Seam trims before write; index shape matches repo precedent. Defence-in-depth only. |
| 433 | 7-7 | `reference_number_ext` is unbounded `TEXT` under a B-tree expression index, and only Group B caps it at 512 characters. A direct insert or a replay carrying ... | Seam caps at 512; only direct SQL or a forged replay reaches 54000. Unreachable magnitude. |
| 434 | 7-7 | A stage still unfired at the moment a coverage lapses is lost silently: the scan requires `expiry_date >= business_date`, so once the contract expires it dro... | Expired status filter is enough; no lapsed-without-warning alert class. |
| 436 | 7-7 | `maintenance_warranty_override.warranty_coverage_id` is `UUID NOT NULL` with no `REFERENCES asset_coverage`, so replaying `warranty_override_recorded` into a... | Repo-wide no-cross-projection-FK convention; partial-tail rebuild is not a supported operation. |
| 438 | 7-7 | `business_date DATE` and `flagged_at TIMESTAMPTZ` on an alert row are independent columns with no CHECK relating them and `flagged_at` is envelope-supplied, ... | Envelope timestamp trust is platform-wide; theoretical sort anomaly. |
| 444 | 7-7 | Preventive and scheduled work orders are never warranty-checked: the AC 2 derivation is installed only in `applyBreakdownWorkOrderCreated`, the Story 7.2 pat... | Warranty check is breakdown-only by FR-M-11; preventive work is not a claim. |
| 445 | 7-7 | `applyWorkOrderCompleted` locks the asset, then `statutory_examination` FOR UPDATE for the Story 7.6 stamp invalidation, then the work order, and only then r... | Lock order unchanged (maintenance-plan.ts:843, 852, 859, 896) but the scenario needs a client looping a guaranteed-403 completion; theoretical contention. |
| 446 | 7-7 | Review decision D5 guarantees one notification per coverage per RUN, not per business date. Both scan skip paths (the in-transaction existence check and the ... | coverage-jobs.ts:198-225 unchanged; single-scanner cadence, D5 accepted the race. |
| 447 | 7-7 | All three Story 7.7 shape asserts raise 400 `INVALID_PAYLOAD`, a code the story's Error Code Contract never lists (its only 400-class entry is `INVALID_PARAM... | Doc-only gap; code follows repo precedent (29 uses in maintenance-coverage.ts). |
| 449 | 7-7 | When `status` is supplied without `business_date` the server substitutes `toIstCalendarDate(new Date())` but the response carries only `coverages`, so two id... | maintenance.ts:4069 still returns `{ coverages }` only; caller can pass business_date explicitly. Cosmetic. |
| 450 | 7-7 | The warranty-override reason-code allow-list is compared with `Array.includes`, an exact case-sensitive and whitespace-sensitive test in both the handler and... | config/index.ts:260-272 exact-match dup check; env is operator-controlled, exact match is defensible. |
| 451 | 7-7 | The override handler checks DOA authority (403) then the reason code (422), while the seam checks the reason code (422) then DOA authority (403 or 404), so a... | Both paths reject (handler 4249, seam maintenance-coverage.ts:579-597); status ordering is cosmetic. |
| 452 | 7-7 | `completeWorkOrderBase` runs the warranty pre-check immediately after the not-found check, ahead of any already-completed check, so a flagged and already-com... | maintenance.ts:939-955 unchanged, but unreachable: a flagged WO cannot reach `completed` without an override; seam (maintenance-plan.ts:878-896) orders it correctly. |
| 456 | 7-7 | Two parallel scans on one `business_date` - the double-notification race the row lock in `coverage-jobs.ts:158-168` exists to serialize - are never tested; t... | Nondeterministic race test; grain already pinned by sequential re-run. |
| 457 | 7-7 | The `notifications_dropped > 0` reconciliation path (zero-recipient or emit-failure, the Story 7.4 lesson the suite's own comment cites) never fires; every t... | Test-only; needs tearing down shared role fixtures. |
| 458 | 7-7 | Notification fan-out is asserted by counting `user_role_assignments` rows and trusting the scan's `notifications_delivered` counter; no test reads delivery o... | Dispatch surface out of story scope; test-only. |
| 459 | 7-7 | Projection-only fixtures (`insertCoverageFixture`, `insertPreventiveWorkOrder`) insert rows with no backing domain event, invent a `plan_id` with no plan row... | Deliberate fixture strategy; no replay re-derivation path exists (see 428). |
| 460 | 7-7 | The renewal-overlap window (a renewal recorded before lapse leaves BOTH rows raising stages, documented in `coverage-jobs.ts:120-124`) is dodged: the renewal... | Duplicate of the Group B D1 ledger entry; test-only. |
| 462 | 7-7 | Capitalization at exactly the threshold (`>` versus `>=` against `capitalizationThreshold`) is unpinned; only threshold-plus-one and a far-below value are te... | 7.6 surface; boundary-test symmetry only. |
| 463 | 7-7 | `TODAY` is captured once at module load while handlers read the wall clock per request, so a suite run spanning UTC midnight shifts every fixture offset by o... | Suite-wide convention; acknowledged in file comment. |
| 464 | 7-7 | Gate persistence after warranty lapse (a flagged work order still rejecting 403 without an override after its warranty expires; the flag is immutable at crea... | Flag is immutable at creation (maintenance-plan.ts:896 keys on the column); test would need projection mutation. |
| 477 | 6-2 | The WIP ledger carries an UPDATE grant for app_user (recorded deviation): the story's Table 2 says "INSERT, SELECT only (append-only)" but the Applier Contra... | Documented deviation, not a gap; grant is load-bearing for FOR UPDATE (`production_wip_ledger.sql:182`) |
| 478 | 6-2 | The staging projection carries a DELETE grant for app_user (recorded deviation, code-review decision 2026-08-28): the production_order.cancelled applier roll... | Documented review decision, cancel rollback needs it (`production_order_stage.sql:100`) |
| 482 | 6-2 | `business_date` and stage `created_at` derive from the client-controlled `staged_at` while the requirement set is pinned to server time, so a backdated event... | Per-spec Event Contract behaviour, consistent with 6.1 convention; `production-material.ts:749` unchanged by design |
| 483 | 6-2 | `posting_value` NUMERIC(14,3) and derived `required_quantity` NUMERIC(18,6) can overflow to an unmapped 500 (SQLSTATE 22003) on pathological magnitudes (quan... | Unreachable magnitude (qty 10^12 x cost 10^11); `production_wip_ledger.sql:48` |
| 484 | 6-2 | LIMIT/OFFSET pagination on the append-only WIP ledger can skip or duplicate rows across pages when a posting lands between fetches; no keyset or total is ret... | Platform-wide pagination idiom (`production_wip_ledger.ts:217`); fixing one table alone is inconsistent |
| 491 | 7-8 | No automatic re-application of a resolved conflict (Binding Decision 6): `POST /api/v1/maintenance/sync-conflicts/:conflictId/resolve` records `discarded` or... | Discard and redo is the resolution; no pre-filled re-apply. |
| 492 | 7-8 | Closure coding is optional (all-or-none) for preventive work orders and mandatory only for `origin = 'breakdown'` (Binding Decision 8), pending product-owner... | Confirmed: closure codes stay optional on preventive completions. |
| 496 | 7-8 | The raise-commit to notification crash window: `raiseMaintenanceSyncConflict` persists the queue row, then emits the supervisor notification through the deco... | Theoretical; `GET /sync-conflicts?status=open` already covers it |
| 497 | 7-8 | The edge upload `DUPLICATE_EVENT` pre-check (`findExistingEdgeEvent`) is a SELECT outside the persist transaction; the race path is the `uq_idempotency` / `d... | Harmless (same idempotent row either way); `edge.ts:279` |
| 498 | 7-8 | A plant reconnecting after an outage emits one 409 `DUPLICATE_EVENT` per re-posted event on `POST /api/v1/edge/events`; that burst is EXPECTED and any 4xx-ra... | Ops runbook note, not code |
| 500 | 7-8 | The rebase-safe set (`REBASE_SAFE_EVENT_TYPES`) is a hand-enumerated list holding exactly `maintenance.work_order_overdue`; any new job that writes on a work... | Policy statement, nothing to do; `edge.ts:471` |
| 502 | 7-8 | The worklist snapshot metadata (`total`, `truncated`, `fetched_at`) lives in `localStorage` on the device rather than in a `localOnly` table, so clearing sit... | Cosmetic; banner returns on next refresh (`edge/src/components/edge-client.tsx:119`) |
| 503 | 7-8 | Re-affirmed inherited platform gaps, exactly as logged under 7.5/7.6/7.7: a mismatched `stream_type` skips every seam; same-event-type key reuse with a diffe... | Duplicate of entries already in the 7.5/7.6/7.7 blocks; verify there, not here |
| 508 | 7-8 | `status_updated_at` / `status_updated_by` / `status_note` are written only by `applyWorkOrderStatusUpdated`; `setWorkOrderCompleted` and `setWorkOrderOverdue... | `maintenance_work_order.ts:204,250` still skip them, but `completed_at`/`overdue_at` carry the truth; cosmetic |
| 509 | 7-8 | `idx_maintenance_work_order_closure_asset` omits `origin`, so the default breakdown-first `listRecentClosuresForAsset` filter cannot use the index (bounded b... | LIMIT 5, cosmetic (`maintenance_work_order_closure.sql:38`) |
| 511 | 7-8 | `getStreamHeadVersions` returns a `Map` keyed by lowercased UUIDs; callers lowercase consistently per the L304 convention, but the contract is undocumented a... | `src/sync/stream-heads.ts:34` lowercases; one-line doc nit |
| 518 | 8-1 | `requiresBusinessStream: true` on `qc.completion_received` also routes it through the cost_centre/project_code tagging-rule engine, which can demand fields t... | Still `true` at `schema.ts:5425`, but the trigger fired: 6.3 (`production-completion.ts:30`) and 9.4 both integrate through `receiveQcCompletion` and pass; no tagging failure materialised |
| 520 | 8-1 | `qcGateExclusionSql` interpolates its `alias` identifier unescaped into SQL; all three callers pass the literal `'stock_balance'` today, so a future caller p... | Internal helper, 2 literal callers (`qc_inspection_task.ts:340`); YAGNI |
| 524 | 8-1 | Cycle-count negative adjustment writes down on_hand for a named finished-goods lot with no QC-gate check [src/compliance/cycle-count.ts:1035] - deferred, out... | Counting a held lot is legitimate; a count never releases a hold. Document it. |
| 525 | 8-1 | assertQcGateAllows still returns silently when no lot reference is supplied [src/compliance/quality.ts:1621] - deferred, require a reference for specific-lot... | `quality.ts:4597-4610` by design; lot-less callers are lot-less operations |
| 526 | 8-1 | The obsolescence `last_issue_at` stamping runs on all rows at the grain with no QC predicate, so a hidden gated lot still has its clock bumped [src/read/proj... | Minor data quality (`stock_balance.ts:446` GREATEST over all rows) |
| 527 | 8-1 | The conditional-release expiry check derives the business date from the client-supplied `occurred_at`, so an authorized actor posting a transfer/production e... | Accepted and documented at `quality.ts:4741` `gateBusinessDateOf` |
| 529 | 8-1 | The edge QC `CENTRAL_ONLY_OPERATION` deny rule is shadowed by the `requireRole` module wrapper, so a device uploading a `qc.*` event surfaces `MODULE_ACCESS_... | `edge.ts:728` `requireRole({module: resolveModuleFromBody})` still wraps first; 403 either way, no bypass |
| 536 | 8-2 | Under a full_inspection plan (no AQL) the lot outcome is not_accepted on ANY nonconforming unit, since no Ac / Re exists (Ac 0 semantics of a 100 % inspectio... | A null-AQL plan is Ac=0 by definition; current behaviour is correct. |
| 537 | 8-2 | Every later determination attempt for a task replays the frozen plan through the REST handler (200 with the stored plan when the task is no longer open); the... | Acceptable by design (fail-closed seam, route contract holds) |
| 540 | 8-2 | The switching score counts every lot once, using the frozen plan's Ac and the switching state's severity at completion time; when several lots of one (plan, ... | Consistent with clause 9.3 lot-sequence wording |
| 542 | 8-2 | The `qc_inspection_result` insert is one statement per reading (up to 500 round trips per batch) [src/read/projections/qc_inspection_result.ts insertQcInspec... | 500 cap, one transaction (`qc_inspection_result.ts:78`); optimisation only |
| 548 | 8-2 | `code_letter`/`resolved_code_letter` have no vocabulary/format constraint on `qc_sampling_plan` [read/projections/qc_sampling_plan.sql] - deferred, seam-deri... | Seam-derived write-back (`qc_sampling_plan.sql:34`), not client-writable |
| 549 | 8-2 | Text-hygiene inconsistency: `lot_number`, `instrument_id` lack non-empty checks that sibling free-text columns in the same files carry [read/projections/qc_s... | Cosmetic hygiene |
| 550 | 8-2 | No index on `qc_inspection_task.sampling_id` / `inspected_by` [read/projections/qc_inspection_task.sql] - deferred, performance-only, table is small at pilot... | Performance-only, small table (`qc_inspection_task.sql:93-94` cover the reads that matter) |
| 555 | 8-2 | `resolveQcResultDuplicateConflict` on a `uq_qc_inspection_result_unit` violation returns every submitted `sample_unit_no`, not the one(s) that actually colli... | Diagnostic detail only; the 409 is correct |
| 557 | 8-2 | `determineSampling`'s "AQL null but inspection_level non-null" defensive branch looks unreachable (the invariant is already enforced at plan-creation time) a... | Dead-code hygiene (`sampling.ts:81-87`); harmless defensive branch |
| 558 | 8-2 | `lockTaskForInspection` reads the task unlocked to get `lot_id`, locks that lot row, then re-reads the task `FOR UPDATE` without re-verifying the two `lot_id... | lot_id immutable on a task; no exploit path |
| 562 | 8-2 | Several lookup functions trust caller-supplied enum types instead of runtime-validating with the module's own `isInspectionLevel`/`isCodeLetter` guards: `cod... | All call sites pre-validate; internal module |
| 563 | 8-2 | Test-coverage gaps in `test/unit/aql-tables.test.ts`: the monotonicity test only checks `ac`, never `re`; the long arrow-chain test (`singleSamplingPlan('R',... | Coverage-symmetry only |
| 564 | 8-2 | `codeLetterFor`'s `Number.isSafeInteger` ceiling conflates "lot size too large" with "too small" (same null return) for lots above 2^53-1; no structural/type... | Lots above 2^53-1; unreachable magnitude |
| 568 | 8-2 | `adjustSamplingStateBase` always returns `201`, never `200`, on an idempotency-key replay, unlike every sibling write handler in this file [src/api/v1/qualit... | `quality.ts:1465` still 201; data correct, status-code cosmetic, fix needs a new idiom |
| 569 | 8-2 | `determineSamplingBase`'s replay short-circuit has a narrow race window: if a concurrent determination commits between this request's task fetch and its froz... | Self-healing; constraint gives the right data |
| 570 | 8-2 | `sample_unit_no`/`limit`/`offset` query-param regexes have no upper bound, so an extremely long digit string could produce an imprecise `Number()` value sent... | Same `^\d+$` idiom at 5+ routes in `quality.ts:520-1838`; nothing reachable beyond a 4xx/500 on absurd input |
| 575 | 8-2 | Missing negative-path/boundary integration tests: a genuine same-idempotency-key replay of results/observations/completion never asserts the 200 status; malf... | Coverage-symmetry only |
| 576 | 8-2 | Test-helper quality nits: inspectLot's multi-line branch computes the wrong required-reading count (currently dead/unreachable) and its string-based quantity... | Low value, test-only |
| 581 | 8-3 | `lot_trace.business_stream` is NOT NULL and the Story 8.3 events carry no business stream of their own, so the relabel trace reuses the parent lot's most rec... | Deliberate; resolveLotBusinessStream (lot-split.ts:189-198) still does it. Real lots always have a movement trace; fabricating a stream is worse. |
| 583 | 8-3 | Accept and reject carry no DOA gate and no segregation-of-duties check, so the inspector who recorded the results also records the disposition [src/complianc... | Reject needs no second signature; conditional release is already DOA-gated. |
| 584 | 8-3 | A downgrade child lot is created ungoverned (no `qc_inspection_task`), so seconds stock never re-enters the QC gate [src/compliance/quality.ts applyNcrOutcom... | Binding Scope Decision 8 already answered this; applyNcrOutcomeRecorded (quality.ts:3016-3133) never calls insertQcInspectionTask by design. |
| 586 | 8-3 | `qc.rework_requested` is admissible only when the owning NCR already names its `event_id`, and that id is minted client-side on the outcome command (`rework_... | quality.ts:1250 validates UUID, :3102 consumes; replay is byte-identical and PK-deduped. No exploit. |
| 587 | 8-3 | The scrap outcome's "Blocked (scrap-pending)" state is `gate_status = 'rejected'` plus `lot_master.quality_hold_status = 'held'` with reason `scrap_pending`,... | SCRAP_PENDING_HOLD_REASON quality.ts:278; 8.5 hold code preserves it (:5097, :5211). BSD 9; Epic 16 consumes the event. |
| 591 | 8-3 | `getQcNcrById`'s `forUpdate` parameter has no guard requiring an explicit `client` [src/read/projections/qc_ncr.ts:135-146] - deferred, pre-existing: matches... | qc_ncr.ts:209-220 unchanged; consistency-only across ~30 files. |
| 595 | 8-3 | `LOT_ON_HOLD`'s `reason` is hardcoded to `'manual_hold'` regardless of the actual hold cause [src/compliance/quality.ts lockLotForDisposition, applyNcrOutcom... | quality.ts:2490, :2964, :4171 still literal; SELECTs at :2430/:2950 omit quality_hold_reason. Body detail nobody consumes; story-8-1/8-3/8-4 tests pin the literal, so a "fix" costs test churn. |
| 596 | 8-3 | `applyConditionalReleaseRecorded` was modified despite the story's own "copy structurally and NOT to modify" instruction [src/compliance/quality.ts applyCond... | Documentation nit; the two added fields are forced by insertQcLotDisposition's shape. |
| 597 | 8-3 | `QC_SPLIT_INVALID` is thrown at three different HTTP statuses (400 shape-validation, 409 business-conflict, 500 defensive-invariant) [src/compliance/quality.... | Confirmed: reject() 400 at :1150-1185, 409 at :2778/:2811, 500 at lot-split.ts:174 (commented unreachable). One-code-one-status is design churn with no caller impact. |
| 599 | 8-3 | No UOM-compatibility check before a downgrade relabels quantity onto a different SKU, and the downgrade SKU is validated for existence only (not active/sella... | quality.ts:2999-3012 existence-only. No UoM master exists anywhere (same root as L630); nothing to check against. |
| 605 | 8-3 | `recordQualityAcceptanceMetricBase` always responds `201`, never `200`, on an idempotency-key replay [src/api/v1/supplier-scorecards.ts] - deferred, matches ... | Accepted precedent (8.2 adjustSamplingStateBase); cosmetic status nuance. |
| 610 | 8-3 | Task 10's named fixture-reuse list (`heldLot`, `seedLotWithStock`, `createPlanVersion`, etc. from story-8-1/8-2) doesn't exist as exports in either source fi... | Documentation nit only. |
| 611 | 8-3 | Minor test-quality nits in test/integration/story-8-3.test.ts: `countRows`'s `table`/`where` arguments are string-interpolated rather than parameterized (cur... | countRows (test:460) only receives literals; low value. |
| 617 | 8-4 | `GRANT INSERT, SELECT, UPDATE` is table-wide on projections where only two or three columns are ever written, and the GRANT blocks never REVOKE, so an over-g... | 0 REVOKE / 0 column grants across projections; over-engineering for a single app_user role. |
| 618 | 8-4 | The schema-drift harness compares only the `CREATE TABLE` block and the named constraint DO blocks; grant DO-block bodies and index DDL bodies in `init-db.sq... | Harness coverage symmetry; the GRANT DELETE scenario is theoretical. |
| 619 | 8-4 | `'<table>'::regclass` in the guarded DO blocks is unqualified and resolves through `search_path`, so a same-named table in an earlier schema is inspected and... | 449 unqualified regclass casts, 0 qualified; single-schema deployment, theoretical. |
| 621 | 8-4 | `NUMERIC(18,6)` columns read back scale-padded, so a value inserted as `'1.5'` reads as `'1.500000'`; nothing documents that consumers comparing a projection... | compareDecimalStrings already exists with 7 call sites; no raw comparer today. |
| 626 | 8-4 | `requireTask` resolves and 404s before `assertWriteSiteAccess`/`assertReadSiteAccess` runs, so a caller scoped to one site receives 404 for a nonexistent tas... | Confirmed quality.ts:1045-1055 then assert*SiteAccess at :1134 etc. Task ids are UUIDs; existence oracle over unguessable ids is theoretical. |
| 629 | 8-4 | Every integration test file TRUNCATEs ~45 tables including `domain_events` and `audit_log` in its `before` hook, which is safe only because `package.json` pi... | package.json:13-15 pins the flag on all three scripts; theoretical. |
| 631 | 8-4 | The retention-sample quantity is recorded book-only and never deducted from the lot's stock balance, and `INSUFFICIENT_STOCK` sits in `AUDITED_REJECTIONS` un... | BSD 8 makes it evidentiary. AUDITED_REJECTIONS (quality.ts:267-278) is file-wide and INSUFFICIENT_STOCK is reached by split (:2680) and downgrade (:3029), so the entry is not dead. |
| 642 | 6-3 | A co-product or by-product output whose item has no released production BOM cannot be completed (`OUTPUT_SPECIFICATION_UNRESOLVED`), because Story 8.1 keys a... | Self-declared "not deferred work"; design consequence. Remove from ledger. |
| 643 | 6-3 | The scrap valuation prorates open WIP by `scrap_quantity / order_quantity`, which is a proportional convention rather than a measured consumption: a scrap de... | Proportional convention is acceptable; no requirement asks for measured scrap cost (production-completion.ts:518,754). |
| 644 | 6-3 | Per-item or per-BOM completion tolerance is WITHDRAWN, not deferred (Binding Decision 6): one symmetric `PRODUCTION_COMPLETION_TOLERANCE_PERCENT` governs bot... | Explicitly withdrawn, not work. Remove from ledger. |
| 645 | 6-3 | Kit-assembly order naming in `source_reference_type` remains unchanged from Stories 6.1 and 6.2 (the FR-I-09 scope note). | Scope note, not a gap. |
| 651 | 6-3 | `btrim(x) <> ''` strips ASCII space only, so a tab, newline or non-breaking space passes the non-blank CHECK on `short_close_reason` and `reason_code` - defe... | Repo-wide idiom (28 projection files); every API path trims in JS first. |
| 653 | 6-3 | `idx_production_completion_task`, `idx_production_completion_item` and `idx_production_scrap_declaration_business_date` are not read by any current query, wh... | Index tuning without a measured slow query; 6.4 has landed and nobody has reported one. |
| 655 | 6-3 | Each relief posting's `posting_value` rounds to `NUMERIC(14,3)` independently, so N partial value-mode reliefs of one source posting do not sum back to that ... | Ledger itself says harmless; 'all' mode drains exactly. |
| 656 | 6-3 | `business_date` on both new tables is a free `DATE NOT NULL` with no database-level relationship to the adjacent `completed_at` / `declared_at` timestamp - d... | Reachable only by direct SQL; seam derives and checks. |
| 657 | 6-3 | `production_completion.qc_task_id` has no uniqueness and no foreign key, so the one-inspection-task-per-output-lot invariant is documented in a banner and en... | uq_qc_inspection_task_lot makes the duplicate unreachable through the applier. |
| 658 | 6-3 | `uq_production_completion_grain` includes `output_class`, so one BOM line could in principle be inserted twice under two different classes for one event - de... | Unreachable per chk_bom_line_output_class. |
| 663 | 8-6 | `compliance_bis_licence.site_id` carries no FK to a sites table [read/projections/compliance_bis_licence.sql] - deferred, same Story 8.7 governance boundary. | No sites table to reference; theoretical. |
| 666 | 8-6 | `label_master.approved_by` carries no FK to a users/identity table [read/projections/label_master.sql] - deferred, same Story 8.7 governance boundary. | No other approver column in the repo carries such an FK; house posture. |
| 672 | 8-6 | `applyBatchReleaseRecorded` calls `getItemBySku` a second time to read `legal_metrology_required`, duplicating the lookup `resolveBisCoverage` already did on... | quality.ts:4064 and :4260 still both call; one indexed lookup inside a lock, not worth touching 3 call sites. |
| 673 | 8-6 | The quality dashboard's `from`/`to` params have no upper bound on range width, so an arbitrarily large window can force an expensive unbounded aggregate scan... | Matches supplier-scorecard; no cap anywhere in the house. |
| 678 | 8-6 | `ncrAgingMetric`'s and `capaAgingMetric`'s age-bucket histograms fetch every open row into Node and bucket in JS rather than aggregating in SQL [src/quality/... | reporting.ts:275,328 still loop rows; scale-only concern at open-NCR volumes the pilot will not reach. |
| 679 | 8-6 | `buildQualityDashboard` awaits its seven metric queries sequentially rather than via `Promise.all` [src/quality/reporting.ts: buildQualityDashboard] - deferr... | Shared optional PoolClient cannot multiplex; sequential is the correct shape. |
| 683 | 8-6 | `dormant` mode is never exercised end-to-end via a real `POST .../release` - only proven via child-process config-load string checks and hand-fed pure-predic... | Coverage-symmetry; pure predicate + config-load tests are the intended proof. |
| 684 | 8-6 | `conditional_releases` metric and its `deviation_id` drill-through field (named in AC 5) have zero positive-value test coverage - only the empty/no-data shap... | story-8-6.test.ts has 0 deviation_id hits; coverage-symmetry only. |
| 685 | 8-6 | No test for a single item that is both `bis_licence_required` AND `legal_metrology_required` (block ordering, whether one masks the other) [test/integration/... | Block ordering is deterministic in quality.ts:4237-4265; coverage-only. |
| 686 | 8-6 | `DASHBOARD_SERIES_LIMIT` (200) is only asserted as a bare constant, never exercised by actually producing >200 series rows and confirming truncation [test/un... | qc-dashboard-metrics.test.ts:38 asserts 200; producing >200 series is a fixture cost with no bug class behind it. |
| 687 | 8-6 | Fragile inter-`it` state coupling: `codedRejectHeld`/`acceptedC1`/`seededCapaId` are assigned inside one `it` and read by later, nominally independent `it` b... | Cosmetic test hygiene; suite runs serially. |
| 689 | 8-6 | The drill-through disposition check asserts only HTTP 200, not response content, unlike the NCR/CAPA drill-throughs in the same test [test/integration/story-... | Coverage-symmetry only. |
| 690 | 8-6 | `istDate()` reimplements IST offset arithmetic by hand rather than reusing a production helper, risking a rare midnight-rollover flake in the boundary test [... | Ledger itself says not an unambiguous fix; independent expected values are deliberate. |
| 691 | 8-6 | Global-vs-site-specific licence precedence (Decision 6) is only proven at site A; no test confirms a global row covers a second, different site [test/integra... | Coverage-symmetry only. |
| 692 | 8-6 | The AC 3 label test accumulates a draft row before inserting the approved row in one linear narrative, implicitly also testing (but never calling out) that a... | Already covered implicitly; naming only. |
| 693 | 8-6 | `Decision 8`'s uniqueness test only proves the constraint fires on a second INSERT, never demonstrates the read-side consequence `findCurrentApprovedLabel` p... | Coverage-symmetry only. |
| 694 | 8-6 | `valid_from` exactly equal to `asOf` (licence starts today) is untested - only the `valid_to` boundary is covered [test/integration/story-8-6.test.ts]. | story-8-6.test.ts has a valid_from-today case (1 hit); and boundary is a plain <= compare. |
| 695 | 8-6 | Site-specific licence row invalid/expired while a global row is valid (fallback to global) is untested [test/integration/story-8-6.test.ts]. | Coverage-symmetry only. |
| 697 | 8-6 | CAPA overdue boundary (`due_on` exactly `asOf` vs `asOf + 1`) is untested [test/integration/story-8-6.test.ts]. | Coverage-symmetry only. |
| 698 | 8-6 | Dashboard `from == to` (zero-width window) and a calendar-invalid-but-well-formed date (e.g. `2026-02-30`) are both untested [test/integration/story-8-6.test... | 0 hits; coverage-symmetry only. |
| 699 | 8-6 | Site-A dashboard isolation test only checks the absence of site-C rows, never pins site-A's own expected counts [test/integration/story-8-6.test.ts]. | Coverage-symmetry only. |
| 700 | 8-6 | No unauthorized/unauthenticated access test for `GET /api/v1/qc/reports/dashboard` [test/integration/story-8-6.test.ts]. | Story 1.9 spine allowlist test covers auth on every registered route house-wide. |
| 704 | 8-7 | Whole 500-licence BIS expiry batch runs in one transaction, holding `FOR UPDATE` row locks against the release path for the full cycle. Fix is one transactio... | bis-licence-expiry.ts:50-52,73-140: bounded batch (config batch size), advisory lock, per-row SAVEPOINT; no FOR UPDATE in the file (0 hits). |
| 707 | 8-7 | `ROLLBACK TO SAVEPOINT` inside the sweep's catch block can itself throw on a fatal connection error, masking the original failure. Pre-existing sweep pattern... | bis-licence-expiry.ts:127 still; outer catch at :146 does ROLLBACK().catch() and marks cycleFailed, so the original failure is logged either way. |
| 708 | 8-7 | Re-approval of a `superseded` label version reports the same error code as re-approval of an `approved` one, so a superseded label cannot be distinguished fr... | master-data.ts:619-624 LABEL_VERSION_NOT_DRAFT carries `status: existing.status` in detail; distinguishable already. |
| 715 | 8-7 | Story 6.4 scope: `GET /api/v1/production-orders/lots/:lotId/genealogy` is registered after the `:orderId` siblings, and its comment claims the opposite. It s... | server.ts:1010 registered after :982-994; three-segment path cannot be shadowed by a two-segment param route; future-route hypothetical. |
| 716 | 8-7 | Story 6.4 scope: the `consumptionVarianceTolerancePercent` regex rejects `.5` and `+10` while its error message describes "a decimal percentage of at least 0... | config/index.ts:570 regex; message at :572 is close enough; cosmetic. |
| 722 | 8-7 | Story 6.4 scope: the `variance` payload on `ProductionOrderStateChangedPayload` permits contradictory states - `computed: false` with a populated `breached_l... | schema.ts:2898 optional object; server-derived only, never client-supplied. |
| 723 | 8-7 | Story 6.4 scope: the `production_consumption_variance` block in `deploy/compose/init-db.sql` opens with a duplicated one-line header immediately followed by ... | init-db.sql:11683 is the standard "Mirror of read/projections/..." header used by 16 mirrored blocks; the fuller header is the canonical file's own. Not an artifact. |
| 729 | 8-7 | The BIS expiry sweep asserts `failed` and `cycleFailed` only in their success state. Proving the per-row SAVEPOINT isolation (one poisoned row must not abort... | Ledger itself concludes a test that cannot fail is worse than none; no fault-injection seam wanted. |
| 735 | 8-8 | `QUALITY_EVENT_TYPES` in `src/compliance/quality.ts` repeats the four `qc.witness*` literals rather than importing them from `src/compliance/qc-witness.ts`, ... | quality.ts:227-232 documents the cycle; pinned by qc-witness-predicates.test.ts. |
| 737 | 8-8 | A witness hold point and a Story 8.5 governed hold cannot coexist on one lot: both insert into `qc_quality_hold`, and `uq_qc_quality_hold_open` allows one op... | Governed hold supersedes; the witnessed inspection waits for clearance. |
| 744 | 9-4 | job_work_output has no FK to service_order and no site-consistency CHECK (read/projections/job_work_output.sql). Matches the existing projection convention. | Matches projection convention (no REFERENCES in job_work_output.sql). |
| 745 | 9-4 | Direct-event path takes posted_by from the payload rather than the actor for a statutory attribution field (src/compliance/custody-ledger.ts:897). Consistent... | custody-ledger.ts:256,360 require posted_by as payload field; consistent with 9.3 convention, route path stamps actor. |
| 746 | 9-4 | The story-9-4 regression arm re-asserts a fixture rather than regressing anything (test/integration/story-9-4.test.ts:884). Task 6.3 requirement is the separ... | story-9-4.test.ts:973 is a smoke arm; Task 6.3 is the suite run. |
| 747 | 9-4 | Idempotent-retry path builds a null site_id when the order row is unreadable, returning 400 instead of replaying (src/api/v1/service-orders.ts:695). Pre-exis... | service-orders.ts:620,741,832 `body.site_id ?? existing?.site_id ?? null` is the route idiom; unreadable order row is a 4xx anyway. |
| 754 | 9-5 | The widened `uq_custody_ledger_source_event` removes the blanket per-event replay guard (read/projections/custody_ledger_entry.sql:78). `UNIQUE(source_event_... | custody_ledger_entry.sql:87 is (source_event_id, sku, lot_id) by design; live replay guard is store.ts event_id + idempotency_key. Partial index is belt-and-braces. |
| 755 | 9-5 | The custody unique key omits `location_id`, and `NULLS NOT DISTINCT` collapses two rows for the same event and sku when `lot_id` is null (read/projections/cu... | Ledger's own text says unreachable today; latent for a category that does not exist. |
| 757 | 9-5 | The Task 0 index-widening DROP guard is keyed on `current_schema()` (read/projections/custody_ledger_entry.sql:70). Under a `search_path` whose head is not t... | sql:78-84; migrate runs in the default schema, theoretical. |
| 763 | 9-5 | The clock is opened on `challan_qty`, not `received_qty` (src/compliance/jobwork-receipt.ts:545), so a challan of 100 against a receipt of 90 leaves 10 units... | The Section 143 clock runs on the challan document quantity; current code is correct. |
| 764 | 9-5 | `calendarDaysUntil` and `returnClockExpiryDate` (src/compliance/jobwork-return-clock.ts:65, :72) parse `YYYY-MM-DD` by splitting on `-` with no format valida... | jobwork-return-clock.ts:68,75-77; internal callers only, all pass an IST calendar date. |
| 767 | 9-5 | The fixed-width custody statement (src/compliance/custody-statement.ts:233) declares `WIDTH` 100; a signed three-decimal `count_adjustment` variance plus a 3... | Cosmetic alignment of a text statement. |
| 770 | 9-5 | Story 9.4's dispatch applier locks clock rows inside its per-sku loop (src/compliance/jobwork-dispatch.ts:425-470), so clocks for one sku are locked while th... | jobwork-dispatch.ts:435 ledger insert then :468 reconcile per sku, all under the order advisory lock; no cross-transaction hazard. |
| 771 | 9-5 | UNVERIFIED: one review layer reports that the physical-verification line filter (src/compliance/cycle-count.ts:1388) covers only `stock_class` and a non-zero... | Verified: cycle-count.ts:1407-1408 filters `physical_verification_id AND stock_class AND variance_quantity <> 0`, but physical_verification_line (physical_verification.sql:40-52) has NO adjustment_status column; the per-line approve/reject lifecycle is cycle_count's (cycle-count.ts:857-891). PV reconciliation runs at sign-off (:1353-1357), which is the approval. Not a live defect. |
| 777 | 9-5 | `reconcileReturnClocks`'s `strict: true` branch (src/compliance/jobwork-return-clock.ts) has had neither a caller nor a test since the chunk 2 review made th... | jobwork-return-clock.ts:238 dead; all callers pass strict:false (custody-ledger.ts:1102, :1293, jobwork-dispatch.ts:475). Deliberately kept; no action. |
| 779 | 9-5 | The schema-drift mirror assertions (test/unit/schema-drift.test.ts) are unanchored substring matches against the whole concatenated `init-db.sql`, so in prin... | Hardening only; strings unique today. |
| 781 | 9-5 | Sweep batch ordering (`ORDER BY expiry_date ASC LIMIT batchSize`) puts past-expiry rows first, so during any backlog above the batch size the whole tick is s... | :182-183 `ORDER BY c.expiry_date ASC ... LIMIT`; self-healing, truncated flag visible. |
| 783 | 9-5 | A `capital_goods` clock older than a year can never be corrected back to `input`: the chunk 2 retro-expiry guard refuses it permanently, the clock runs to da... | Permanent refusal stays; a reason-coded override is a fraud path. |
| 784 | 9-5 | AC5's terminal breach notice carries no `escalation` object (disclosed in Debug Log 7), so an unacknowledged deemed-supply notice never climbs, while AC5's o... | jobwork-clock-sweep.ts:163-165: the breach notice already fans out DIRECTLY to compliance_officer AND site_head, so there is nobody left to climb to. Wording-only. |
| 787 | 9-5 | The aging report's buckets are hard-coded 30 and 90 while the sweep's alert stages come from `config.jobwork.clockLeadDays1/2`, so lowering the leads desynch... | Buckets stay statutory 30 and 90. |
| 788 | 9-5 | The Task 0 widened unique index is tested for permissiveness but never for uniqueness: the arm proves two rows now insert under one `source_event_id`, and th... | Coverage-symmetry only; store-level replay guard is what is relied on (see 754). |
| 789 | 9-5 | `SKU_COMPANY` is seeded into the 9.5 kit BOM as `supply_source: 'company'` and then never received, consumed or asserted on, so the negative that matters - c... | Guard exists at custody-ledger.ts:214 (`supply_source === 'customer' \/\/ null`); company lines never enter the receipt seam. Test symmetry only. |
| 790 | 9-5 | 1095-day expiry arithmetic is pinned on a single date pair. A 1095-day span with no leap day, one with two, a leap-day challan date, and a capital-goods year... | Date.UTC day-add (jobwork-return-clock.ts:69) and SQL make_interval (projection ts:90) are leap-safe by construction. |
| 792 | 9-5 | Mutation verification for the story remains single-point against Task 8.4's two-point standard: one seam mutation observed failing two arms is two entry path... | Process metric, not a code gap. |
| 794 | 9-5 | `custody_ledger_entry.reference_ext` renders nothing on a custody statement when null, and every pre-9.5 return row carries null, so the statement silently o... | custody-statement.ts:231 is already null-safe; historical rows lack the value; cosmetic evidence line. |

### Folded into another entry

Table 8 lists duplicates closed with their root entry.

Table 8: Folded duplicates

| Ref | Story | Item | Root |
| --- | --- | --- | --- |
| 17 | 9-3 | `qtyToScaled` silently truncates input beyond 3 decimals rather than rounding/rejecting [src/compliance/custody-statement.ts:28] - deferred, low risk, all re... | Same root as line 8 (qtyToScaled truncation). |
| 73 | 1-3 | SCIM real-change paths commit the directory mutation before persistEvent, so a persistEvent failure yields a committed change with a 500 and no edit-log reco... | Same root as line 69 (SCIM two-phase write). |
| 163 | 3-6 | Story 3.3's weighbridge `business_date` is derived from wall-clock `now()` instead of the event's `occurred_at`, so the AC1/AC2 assertion pinned to '2026-07-... | Same root as line 144 (weighbridge localYmd). |
| 216 | 4-7 | Supplier-invoice write routes (capture/duplicate-override/link-po/stage/confirm) perform no site-write authorization [src/api/v1/supplier-invoices.ts:495-537... | Same root as line 208. |
| 230 | 4-5 | Three-way-match write routes (link-po, match run, credit/debit note, clearance-feed run) perform no site-write authorization [src/api/v1/three-way-match.ts:9... | Same root as line 208. |
| 238 | 4-5 | Three-way-match write routes perform no site-write authorization [src/api/v1/three-way-match.ts:459-497] - already logged above in the dev-story 4-5 entry; r... | Same root as line 208. |
| 247 | 4-2 | Scorecard metric write routes perform no site-write authorization [src/api/v1/supplier-scorecards.ts] - deferred, same module-wide procurement RBAC pattern a... | Same root as line 208; moot until supplier carries a site_id. |
| 295 | 7-2 | stream_type/event_type mismatch bypasses the registry's streamType: a maintenance.* event posted with a non-maintenance stream_type skips both seams' gates a... | Same root as line 285 (persistEvent registry). |
| 296 | 7-2 | Client-supplied future metadata.occurred_at stamps projection timestamps in the future: the envelope validator accepts any ISO-8601 timestamp with no upper b... | Same root as line 237. |
| 338 | 7-4 | No direct HTTP POST /api/v1/events bypass test for a forged maintenance.* alert under a mismatched stream_type [src/api/v1/events.ts:119-128] - deferred, tie... | Same root as line 285. |
| 351 | 7-5 | A maintenance.* event posted with a non-maintenance stream_type skips every maintenance seam gate, including this story's [src/events/store.ts, src/api/v1/ev... | Same root as line 285. |
| 352 | 7-5 | Same-event-type idempotency-key reuse with different content returns the original event: replayIdOrReject compares event_type and the id field's UUID-ness, n... | Same root as line 310 (idempotency payload reuse). |
| 361 | 7-6 | Statutory examination document and PDF attachment storage is not provided: certificate_number_ext and device_key are the only evidence the register holds, an... | Same root as line 349 (attachment storage). |
| 362 | 7-6 | A maintenance.* event posted with a non-maintenance stream_type skips every maintenance seam gate, including this story's [src/events/store.ts, src/api/v1/ev... | Same root as line 285. |
| 363 | 7-6 | Same-event-type idempotency-key reuse with different content returns the original event: replayIdOrReject compares event_type and the id field's UUID-ness, n... | Same root as line 310. |
| 413 | 6-1 | No stream_type/event_type registry cross-check in `persistEvent`: a forged `production_order.*` event posted under a foreign stream_type (for example `stream... | Same root as line 409 (persistEvent stream_type registry and same-key payload compare). |
| 461 | 7-7 | Same-key idempotent replay with a DIFFERENT body silently returns the original 201; no test in any story observes it [test/integration/story-7-7.test.ts:1630... | Same root as line 409. |
| 465 | 7-7 | The Story 7.2 PM-generation seam path is never exercised by the preventive-work-order warranty test: the suite proves only the `warranty_flagged` column DEFA... | Same question as line 444, closed with it. |
| 507 | 7-8 | Parked dependents have no un-park path (Decision 3): rows parked `needs_attention` + `STREAM_CONFLICT` sit in `SETTLED_STATUSES` and are never re-uploaded ev... | Same root as line 490. |
| 535 | 8-2 | The reduced-inspection Ac / Re pair sequence is transcribed as 0/1, 0/2, 1/3, 1/4, 2/5, 3/6, 5/8, 7/10, 10/13, 14/17, 21/24 (eleven diagonals, aligned with T... | Same verification as line 534. |
| 598 | 8-3 | Split/downgrade relabel trace attributes the whole moved quantity to a single `location_id` when more than one stock location contributed, and silently skips... | Same root as line 580 (one lot_trace row per event). |
| 630 | 8-4 | `uom` on a retention sample is bounded to 32 characters but validated against no unit-of-measure vocabulary, so `"bananas"` is accepted - deferred, no UoM ma... | Same root as line 599 (no UoM master). |
| 733 | 8-8 | `governing_role` and `delegation_applied` are captured on the waiver payload and stored, but only `approver_user_id` and `doa_entry_id` are compared on apply... | Same root as line 706 (DOA layer off-transaction, single-hop, amount-agnostic). |
| 748 | 9-4 | `resolveApprover` (called from `applyCustodyLossProjection`, src/compliance/custody-ledger.ts:853) runs on a second pooled connection outside the projection ... | Same root as line 706. |
| 762 | 9-5 | FIFO deliberately includes breached clocks so the closure gate stays reachable, and `deemed_supply_qty` is deliberately frozen at breach (both Debug Log 3). ... | Same root as line 761 (allocation policy). |
