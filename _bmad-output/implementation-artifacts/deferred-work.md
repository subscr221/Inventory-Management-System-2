# Deferred Work

## Deferred from: code review of 1-1-core-infrastructure-deployment-and-event-store-schema.md (2026-07-12)

- No authentication or authorization anywhere [src/server.ts:515] — deferred, pre-existing (Story 1.2 scope)
- `readStream` has no pagination or limit [src/events/store.ts:396-421] — deferred, pre-existing (Not required in Story 1.1)
- Global idempotency uniqueness is likely too broad [events/domain_events.sql:20] — deferred, pre-existing (Matches spec requirements)
- `trace_id` is generated fresh per error and never logged [src/middleware/error.ts:455] — deferred, pre-existing
- Migration has no versioning and a brittle path [src/events/migrate.ts:210] — deferred, pre-existing (Full migration system out of scope)
- Extra/unknown properties are silently accepted [src/events/store.ts] — deferred, pre-existing

## Deferred from: code review of 1-3-statutory-edit-log.md (2026-07-14)

- Archival retention math uses calendar-year subtraction, not financial-year boundaries [src/cli/archive-audit-log.ts] — deferred, pre-existing open question in spec (story's own "Open Questions for the Team" already flags the FY start date as unconfirmed)
- `range_digest` is computed at read-time over the returned page, not via write-time hash chaining [src/api/v1/audit.ts:42-44] — deferred, matches existing spec design (Dev Notes define `range_digest` this way); strengthening to cryptographic chaining is an architecture-level change beyond this story's scope

## Deferred from: code review of 1-3-statutory-edit-log.md (2026-07-18)

- SCIM directory mutations commit before the event+audit write, so a crash between the two transactions leaves an access-rights change with no audit entry [src/adapters/iam/scim.ts:62] - deferred, pre-existing two-phase design from Story 1.2 with an in-code follow-up note ("bring the event into the same transaction as the directory write")
- Audit `location_id` is client-supplied for wildcard-grant callers; a `*` grant matches any UUID, so a wildcard admin can stamp an arbitrary location into the audit row [src/api/v1/events.ts:75] - deferred, blocked on the location register (Stories 1.6/2.1) for server-side validation
- Archive CLI loads the entire eligible row set into memory with no batching [src/cli/archive-audit-log.ts:19] - deferred, no eligible rows can exist until 8 financial years after go-live; add batching before the retention horizon
- `read/projections/users.sql` carries no grants (they live only in `deploy/compose/init-db.sql`), so a migrate-only-provisioned database cannot execute SCIM writes as `app_user` - same split-brain class the audit tables just had fixed [read/projections/users.sql] - deferred, pre-existing Story 1.2 scope; apply the same canonical-file grant pattern there
- SCIM real-change paths commit the directory mutation before persistEvent, so a persistEvent failure yields a committed change with a 500 and no edit-log record (failure-path AC1 hole) [src/adapters/iam/scim.ts] - deferred, same pre-existing two-phase design already tracked above; the fix (single transaction) covers both entries

## Deferred from: code review of 1-7-calibration-lockout-enforcement (2026-07-19)

- `location.disputed` generated event uses a narrow raw insert instead of the central `persistEvent` path [src/compliance/location.ts:68-81] - closed as by-design, not deferred work. This is the explicit Story 1.6 review decision so operator tagging rules cannot reject a valid generated dispute; reverting to `persistEvent` re-broke the Story 1.6 tagging-immunity regression test. Reopen only if generated events later need audit/idempotency parity without tagging.
- Calibration lockout is a non-transactional TOCTOU read [src/compliance/calibration.ts:32] - deferred, pre-existing architectural pattern shared with the tagging and location assertions; the status-flip window is limited to admin maintenance writes.
- Duplicated DDL and redundant inline-plus-guard constraint blocks between the canonical migration and the compose mirror [read/projections/instrument_calibration.sql, deploy/compose/init-db.sql] - deferred, the mirror duplication is required by the story; drift risk noted for future maintenance.

## Deferred from: code review of 1-8-offline-edge-pwa-shell-and-powersync-sync-layer (2026-07-20)

- `svc_powersync` (WITH REPLICATION) is created only in `deploy/compose/init-db.sql`; the guarded grant in `sync/migrations/powersync.sql` silently skips on a migrate-only, non-compose database - deferred, consistent with this repo's established role-provisioning pattern (all roles are created in init-db.sql; migrations only guard grants).
- AC4 "related balance or state updated exactly once" is not exercised because the duplicate test uses an inert `maintenance` stream with no projection [test/integration/story-1-8.test.ts] - deferred, acceptable under the test-capture scope.

## Deferred from: code review of 1-10-ci-cd-pipeline-construction (2026-07-20)

- `deploy/pipeline/verify.sh` (Task 3.6's dry-run mode) is never invoked automatically by any CI/CD job or schedule, so there is no automated drift detection if branch protection or environment settings are later changed through the GitHub UI [deploy/pipeline/verify.sh] - deferred, not required by Task 3.6's literal ask
- `bootstrap-runner.sh` is not idempotent against a partially-provisioned host: a corrupt cached runner package is not re-verified, and re-running against a host with the runner service already installed aborts under `set -e` [deploy/pipeline/runner/bootstrap-runner.sh:52-58,71-77] - deferred, script's own docstring scopes reproducibility to a clean host only
- `backend-tests` and `spine-acceptance-contract` CI jobs duplicate the entire postgres provisioning block verbatim [.github/workflows/ci.yml:44-57,72-90] - deferred, correct per Task 2.5's per-job database isolation requirement, but a third copy of provisioning knowledge to keep in sync

## Deferred from: code review of 1-10-ci-cd-pipeline-construction (2026-07-20, third pass)

- Runtime database role passwords (`app_user`, `readonly_user`, `replication_user`, `svc_powersync`) are created with committed default passwords rather than deployment secrets [deploy/compose/init-db.sql, deploy/compose/docker-compose.yml] - deferred, pre-existing (predates Story 1.10) and lives in Story 1.11's active working files, so out of Story 1.10's diff scope. A fail-closed attempt during the second review pass was reverted because it broke `deploy/compose/.env`-driven flows (`sync:smoke`, `provision.sh`), which define no `READONLY_PASSWORD`. Needs a dedicated secrets-hardening story that also updates `.env.example`, `deploy/compose/.env`, and the smoke harness together.

## Deferred from: code review of 1-10-ci-cd-pipeline-construction (2026-07-20, fourth pass)

- GitHub Actions and Docker actions are referenced by mutable version tags rather than commit SHAs [.github/workflows/ci.yml, .github/workflows/cd.yml] - deferred, supply-chain hardening beyond Story 1.10's acceptance criteria
- Runner binary download is not integrity-verified before installation [deploy/pipeline/runner/bootstrap-runner.sh:52-58] - deferred, hardening should be handled with the broader runner-provisioning story

## Deferred from: code review of 1-11-notification-and-alerting-foundation (2026-07-20)

- Unbounded sequential per-event fan-out: `resolveTargetUserIds` is uncapped and each recipient triggers serial insert/delivery/opt-in/subscription/push round-trips, so a role held by hundreds of users can overrun the dispatch interval [src/notify/dispatch.ts:85-139] - deferred, acceptable at pilot single-site scale and mitigated by the dispatch re-entrancy guard patch.
- Notification schema is duplicated across `deploy/compose/init-db.sql` and `read/projections/notification.sql` with only a "change both together" comment and no drift-guard test, unlike Story 1.9's route-surface guard [deploy/compose/init-db.sql, read/projections/notification.sql] - deferred, add a mirror-assertion test later (this file was already silently reverted once during a concurrent Story 1.10 edit).
- Task 6.1's non-blocking guarantee is tested at the function level (`emitNotification()` called directly) rather than through a real emitting HTTP handler returning 200/201 with a broken dispatcher [test/integration/story-1-11.test.ts] - deferred, tighten when a real emitting consumer (e.g. an approval-card decision) exists to wrap it.
- Value-band and vacation-delegation escalation resolution (Task 4.3) is not wired; only `findRoleHolder` is used, `findFirstActiveDoaEntry`/`findActiveDelegation` are unused [src/notify/escalate.ts] - deferred, documented scope decision in Completion Notes; no current or near-term consumer defines a value-banded or delegated escalation target.

## Deferred from: code review of 2-2-real-time-multi-location-stock-balances (2026-07-21)

- `in_transit` column never written by any code path [src/read/projections/stock_balance.ts] -- deferred, Story 2.5 will add inter-location transfers that populate this field.
- Consolidated API response merges across `stock_class` [src/api/v1/stock.ts:47-63] -- deferred, class breakdown is a future feature (Story 2.8 consignment segregation).
- Zero permitted locations returns 200 empty instead of 403 [src/api/v1/stock.ts:42-45] -- deferred, consistent with existing RBAC handler patterns in the codebase.

## Deferred from: code review of 2-3-lot-batch-and-serial-traceability (2026-07-21, adversarial pass 3)

- Expiry is compared in server-local time (`todayLocalYmd`/`localToday`) while `lot_master` availability filters on SQL `CURRENT_DATE` [src/compliance/lot-serial-validation.ts:67; src/read/projections/lot_master.ts] - deferred, pre-existing clock-source split; standardize on DB `CURRENT_DATE` in a dedicated pass.
- Serial quantity reconciliation compares a summed JS float against the event quantity (`0.1 x 3 !== 0.3`) [src/compliance/lot-serial-validation.ts:362] - deferred, serials are discrete (default quantity 1); no realistic fractional-serial case exists today. Revisit if fractional serial quantities become real.
- FEFO/FIFO selection cannot split a request across lots and rejects `NO_AVAILABLE_LOT` even when combined stock across lots would satisfy it, while an un-lotted issue drains across lots [src/compliance/lot-serial-validation.ts:137; src/api/v1/lots.ts:180] - deferred, single-lot pick matches AC1's single-lot framing; split-pick is new scope for a dedicated story.
- `override_expired_lot: true` from a non-override role is rejected `403 FUNCTION_ACCESS_DENIED` even when the lot is not expired, because the role gate fires on the flag alone in shape validation before any lot lookup [src/compliance/lot-serial-validation.ts:277] - deferred, defensible fail-closed on the override assertion; revisit (move the check into lot validation, gated on actual expiry) if a client is found sending the flag by default.

## Deferred from: code review of 2-5-inter-location-transfer-requests (2026-07-21)

- `SUPPORTED_EVENT_TYPES` registry added to `schema.ts` but never imported or enforced by `store.ts` — the four transfer event types are unvalidated dead metadata [src/events/schema.ts] - deferred, wiring event-type validation into the write path is cross-cutting enforcement work beyond this story.
- Stable error codes `APPROVAL_REQUIRED`, `QUANTITY_EXCEEDS_APPROVED`, `LOT_MISMATCH` not registered in the architecture stable-error list [planning-artifacts/architecture] - deferred, planning-artifact update.
- Approve/reject mutate `transfer_request.status` via direct `UPDATE`, and the persisted `approval_decided` event has no projection handler, so the read-model status change is a side-write not derived from the event [src/api/v1/transfer-requests.ts] - deferred, architecture-pattern decision (event-vs-amendment was already flagged as an open decision in the spec, Task 4).
- `in_transit`/`transfer_request` DDL duplicated across `read/projections/*.sql` and `deploy/compose/init-db.sql`, kept in sync only by comment, with no drift-guard test [read/projections/in_transit.sql, read/projections/transfer_request.sql, deploy/compose/init-db.sql] - deferred, mirror-assertion test is test-infra work shared with the same class already tracked for Stories 1.9/1.11.

## Deferred from: code review of 2-7-safety-stock-reorder-points-and-obsolescence-flagging (2026-07-22)

- `reorder_point` = ceil(avg_daily_demand * lead_time_days + safety_stock) is computed and stored into NUMERIC(18,6) with no upper bound, so extreme demand/lead combinations overflow as an uncaught 500 [src/compliance/planning-jobs.ts:169] - deferred, roll into the shared quantity-bound hardening pass tracked across Stories 2.4/2.6.
- Minimum sample-day guard is hardcoded (DEFAULT_MIN_SAMPLE_DAYS = 2) but Task 5 specifies a *configured* minimum [src/compliance/planning-jobs.ts:45] - deferred, needs a DDL/params-field decision before it is worth wiring.
- Demand-window anchor and obsolescence aging both use SQL now() rather than the supplied scope.business_date, so a backdated or replayed job decides against wall-clock time [src/compliance/planning-jobs.ts:146, :407] - deferred, revisit when a real scheduler replaces the Phase-1 synthetic HTTP trigger.

## Deferred from: code review of 2-9-erp-inbound-reference-projections (2026-07-22)

- No batch-size bound on the ERP sync array: runErpSync processes the whole posted array in one transaction, one savepoint per record. Low risk (trigger RBAC-restricted to svc_erp_adapter/system_administrator). src/api/v1/erp-projections.ts:162-178
- Per-record exceptions lost on late infra rollback: exceptions queued inside the batch transaction are discarded if a later COMMIT/statement fails and rolls back; self-healing on the next retry. src/adapters/erp/sync.ts:280-337
- Dropped PO lines persist as phantoms: soft-close is header-only; erp_purchase_order_line has no status column and the app role has no DELETE grant on it (deliberate - reference tables withhold DELETE). Clean fix needs a schema decision (add a line status column + migration + drift guard + read filter, or grant DELETE). A hard-DELETE patch was attempted and reverted (permission denied). src/read/projections/erp_purchase_order.ts, src/adapters/erp/sync.ts

## Deferred from: code review of story-3.2 (2026-07-23)

- `entered_at` is client-supplied with no upper bound and is the gate-event list sort key; a future-dated or bad-clock row pins to the top of every list. Pre-existing (validation not in the 9fbdcf1 diff). [src/compliance/gate.ts:69]
- Idempotent migration `ADD CONSTRAINT UNIQUE (correlation_id)` guards on constraint name only, not data; DB init aborts if any pre-existing duplicate `correlation_id` data exists. Conditional and unlikely given per-event random IDs. [deploy/compose/init-db.sql:2141-2150]
- Story 3-3 (surfaced by this review's full-suite run, pre-existing): `src/compliance/weighbridge.ts:30` has its own `localYmd` using pure local getters with no IST offset (same defect family gate just fixed), and `test/integration/story-3-3.test.ts:255` hardcodes `business_date === '2026-07-22'` (its authoring date) against an `occurred_at = now` value - the test is date-dependent and fails on any commit from 2026-07-23 onward. Fix both together in a story 3-3 follow-up: IST-correct localYmd + derive the expected date in the test.

## Deferred from: code review of story-3.4 (2026-07-23)

- RBAC helper divergence — `receiving.ts`'s role-check additionally allows `module === 'inventory'` where `asn.ts` doesn't; both independently redefine the same helper instead of sharing code. [src/api/v1/asn.ts:24, src/api/v1/receiving.ts:54] - deferred, pre-existing pattern from earlier stories, not a regression introduced here.
- Unrelated `gate_event` unique-index hotfix bundled into this story's migration file — no relation to goods receiving. [deploy/compose/init-db.sql] - deferred, pre-existing fix folded in opportunistically, functionally correct.
- `resolveSiteByToken`'s RBAC pre-check uses the latest `weighbridge_event` row regardless of status, while the compliance layer (correctly) uses the first `accepted` row — pre-check only; downstream enforcement is unaffected. [src/api/v1/receiving.ts:83-89] - deferred, low likelihood, RBAC gate is defense-in-depth not the source of truth.
- Under-receipt below the lower tolerance bound is unenforced (`under_receipt_tolerance_pct` fetched but unused). A symmetric-reject patch was attempted and reverted: with the tolerance typically unset (0%), the lower bound equals full `ordered_qty`, so per-event enforcement rejected every normal partial/multi-shipment receipt (broke 5 happy-path tests). Needs a PO-closure signal this event doesn't have. [src/compliance/receiving.ts:219-238] - deferred, needs a PO-closure-triggered discrepancy check as its own follow-up story.

## Deferred from: code review of 3-5-directed-putaway-and-location-override-recording (2026-07-25)

- DDL duplicated across `init-db.sql` and `read/projections/*.sql` (putaway_task columns, velocity_class table) -- deferred, pre-existing mirror pattern shared with prior stories; add a mirror-assertion test later. [deploy/compose/init-db.sql, read/projections/putaway_task.sql, read/projections/velocity_class.sql]
- Reslotting job upserts each SKU independently without transaction wrapping; partial state on crash auto-heals on next run since the job recomputes from scratch every invocation. [src/warehouse/reslotting-job.ts:116-127] - deferred, low severity on-demand job.
- Location facts silently skipped when putaway task has no `lot_id`; completion succeeds but lot-location projection becomes stale. Lot-less putaway is an edge case not exercised by current receiving flow (Story 3.4 always sets lot_id). [src/compliance/putaway.ts:93] - deferred, revisit if a future story introduces lot-less putaway tasks.

## Deferred from: code review of 3-6-pick-task-generation-and-execution-fr-w-04 (2026-07-27)

- Cancelling a pick line must also release its allocation, and a task whose lines are all cancelled can never complete because completion throws on `activeCount === 0`, which would permanently pin the task, its allocations and its dispatch order. The unreachable `releasePickLineAllocation` accessor was deleted as dead code during the review sweep, so whoever introduces cancellation must add both the `releaseStock` call and an all-cancelled completion path. [src/read/projections/pick_line.ts, src/compliance/pick.ts] - deferred, latent: no code path currently cancels a pick line or task.
- Pick line confirmation never gates on the parent task's status, so lines belonging to a cancelled task would still confirm and take a fresh allocation that nothing later converts to `picked` or releases. [src/compliance/pick.ts:204-232] - deferred, latent for the same reason: `pick_task.status` models `cancelled` but nothing sets it today.
- Story 3.3's weighbridge `business_date` is derived from wall-clock `now()` instead of the event's `occurred_at`, so the AC1/AC2 assertion pinned to '2026-07-22' only ever passed on the day it was written; it is the single failing test in the suite. [test/integration/story-3-3.test.ts:255, src/compliance/weighbridge.ts] - deferred, pre-existing and outside Story 3.6's diff (neither file was touched); belongs to Story 3.3 as its own fix.

## Deferred from: code review of story-3-7-packing-shipping-and-dispatch-documents (2026-07-27)

- Missing `lot_on_hold_blocked` BOOLEAN field in `DispatchDispatchedPayload` — informational field that would indicate whether dispatch was gated by a hold, not critical for Phase 1. [src/events/schema.ts:558-563]
- Missing FK constraints on `packing_record.dispatch_order_id` -> `erp_sales_order.id` and `dispatch_document.generated_by` -> `users.id` — orphan records possible, but follows pre-existing project pattern of omitting FKs on projections. [read/projections/packing_record.sql, dispatch_document.sql]

## Deferred from: code review of story-3-7-packing-shipping-and-dispatch-documents (2026-07-28)

- No site-isolation in `applyDispatchPackedProjection`/`applyDispatchShippingDocumentsGeneratedProjection`/`applyDispatchDispatchedProjection` — the edge/direct event path still bypasses site scoping (only the HTTP-layer `assertSiteAccess` checks it), so a caller at site A can pack/dispatch orders belonging to site B via edge sync. Pre-existing gap from the prior review round, not touched by this fix pass. High-impact; recommend prioritizing next round. [src/compliance/dispatch.ts]
- `renderLabels` returns an empty array when `carton_count` is 0 even though label generation was requested — pre-existing, not touched by this fix pass. [src/warehouse/document-renderer.ts:189]
- Schema-drift `EXPECTED` array in `test/unit/schema-drift.test.ts` has no entry covering `dispatch_order_status`'s new columns (`packed_at`, `packed_by`, `dispatched_at`, `dispatched_by`) added via Story 3.6's `pick_task.sql` ALTER — future drift on these columns goes undetected. Pre-existing, not touched by this fix pass. [test/unit/schema-drift.test.ts]

## Deferred from: code review of story-3-7-packing-shipping-and-dispatch-documents (2026-07-29)

- Lot-lock ordering can deadlock two concurrent orders sharing lots - both LOT_ON_HOLD queries lock candidate lots via `FOR UPDATE OF lm` with no deterministic `ORDER BY lm.lot_id`, so two transactions on different dispatch orders sharing two or more lots can lock in opposite order and deadlock. The round-2 `FOR UPDATE` race fix is otherwise correct and verified. [src/compliance/dispatch.ts:190-201, 295-305] - deferred, pre-existing lock-ordering class shared with prior warehouse stories; add deterministic lot-id ordering when lock contention is addressed project-wide.

## Deferred from: dev-story 3-8-warehouse-task-management-and-productivity-tracking (2026-07-29)

- `test/integration/story-3-7.test.ts` never runs: its `before` hook calls `POST /api/v1/scim/Users` and `POST /api/v1/scim/Users/:id/access-tokens`, neither of which is a registered route (the real surface is `POST /api/v1/scim/v2/Users` plus `POST /api/v1/auth/dev-token`, as every other story suite uses). The hook fails with 401 UNAUTHORIZED, the suite aborts, and all 13 Story 3.7 acceptance tests report as cancelled - so Story 3.7 currently has no executing acceptance coverage at all. Story 3.8 mirrored the Story 3.6 harness instead. [test/integration/story-3-7.test.ts:59-82] - deferred, belongs to Story 3.7 (still in-progress); pre-existing at baseline cd7e3d9 and outside this story's diff.
- `.env.test` sets `DB_PORT=5432`, but the project's own test PostgreSQL container (`ims2-test-postgres`) publishes on host port 5442; 5432 is held by an unrelated container. Every integration suite therefore fails with "password authentication failed" unless `DB_PORT=5442` is exported, which is almost certainly the "4 DB auth pre-existing" failures recorded in Story 3.7's completion notes. [.env.test] - deferred, environment/config decision rather than a code defect; needs the team to confirm which port the committed default should name.

## Deferred from: code review of 3-8-warehouse-task-management-and-productivity-tracking-fr-w-07 (2026-07-29)

- Putaway tasks directed before this migration never receive a `zone_id`. The additive `zone_id` column ships with no backfill statement, and `setDirectedSuggestion` is the only writer and is predicated on `status = 'ready'`, so it runs only at direction time. Pre-existing directed tasks keep `zone_id` NULL indefinitely and fall into the null-zone bucket in both the AC1 board filter and the AC2 per-zone productivity rollup, indistinguishable from a legitimately undirected task. [read/projections/putaway_task.sql:79; src/read/projections/putaway_task.ts:263-270] - deferred, inherent to an additive migration rather than a defect in the new code; needs a backfill pass planned alongside the deployment.
- GRN headers created before this migration can never backfill `received_at`. The `ON CONFLICT (grn_id)` clause updates only `status` and `updated_at`, so a header created pre-migration keeps a NULL `received_at` even when a later line supplies the instant, permanently excluding that GRN from the gate-dwell fallback leg. [src/read/projections/grn.ts:118-124] - deferred, the immutability is explicitly mandated by Task 1.4's header-identity contract, and the paired pre-migration weighments carry an equally NULL `occurred_at`, so those vehicles sit outside dwell reporting as a whole rather than being half-reported.

## Deferred from: code review of 3-9-forward-pick-replenishment-fr-w-08 (2026-07-30)

- Concurrent checks select same reserve bin - no lock on source bin selection in `runForwardPickReplenishmentCheck`. Two concurrent checks for different (sku, zone) pairs at the same site could select the same reserve bin. If the bin's available covers each individually but not both, the second completion drives `stock_balance.available` negative or fails with `INSUFFICIENT_STOCK`, leaving an orphaned task. [src/warehouse/replenishment-job.ts:129-151] - deferred, Phase-1 limitation; FEFO/velocity ranking and bin locking are future enhancements.

## Deferred from: code review of 4-3-purchase-requisition-and-indent-loop (2026-08-02)

- Online duplicate pre-check TOCTOU - `findOpenDuplicate` runs before `persistEvent` with no locking. Concurrent request can create duplicate between pre-check and insert. [src/api/v1/indents.ts:1016-1042] - deferred, seam handles it inside transaction; pre-check is UX convenience returning 409 to user.
- No FK from indent_line to indent - `indent_line.indent_id` has no FK constraint. Orphaned lines possible if indent insert fails after lines inserted. [read/projections/indent_line.sql:117] - deferred, schema design decision; code always inserts together in same transaction.
- Missing index on indent.status for non-open statuses - Queries for `status = 'closed'`, `'rejected'`, `'cancelled'`, `'ordered'` do sequential scan. [src/read/projections/indent.ts:1595-1598] - deferred, performance issue not correctness; partial index covers open statuses only.
- Missing index on indent.requester_user_id for cross-status queries - `mine=true` query with non-open status does full scan. [src/read/projections/indent.ts:1196] - deferred, performance issue; partial index covers open statuses only.
- indent_number_seq doesn't reset per year - Single sequence, globally monotonic but year-ambiguous. `IND-2026-9999` followed by `IND-2027-10000`. [read/projections/indent.sql:88] - deferred, cosmetic; documented behavior; globally unique is sufficient.
- cancelIndentBase doesn't validate state before persisting - Persists `indent.cancelled` event without checking status. Seam validates inside transaction. [src/api/v1/indents.ts:1387-1414] - deferred, seam validation is sufficient; handler commits to event before seam rejects.
- applyIndentClosed doesn't verify purchase_order_id - Allows closing any ordered indent without checking PO is set. [src/compliance/indent.ts:850-857] - deferred, ordered status implies PO was set; no current path creates ordered without PO.
- Search pattern escaping non-standard PG setting - Escapes `%`, `_`, `\` for ILIKE but assumes `standard_conforming_strings` is on. [src/read/projections/indent.ts:1576] - deferred, non-default PostgreSQL setting; project uses defaults.
- Edge capture minimal client-side validation - Only validates SKU and quantity. Required fields rely on HTML `required` attributes. [edge/src/components/indent-capture.tsx:1942-1951] - deferred, HTML required handles it; server validates all fields.

## Deferred from: code review of 4-4-purchase-order-management (2026-08-03)

- PO write routes (approve/reject/issue/confirm/releases/ceiling) perform no site-write authorization [src/api/v1/purchase-orders.ts:349-592] - deferred, pre-existing module-wide pattern: the sibling indents.ts write routes are identically read-scoped but write-unscoped (verified), and the compliance seam still enforces SOD + DOA-resolved-approver identity. A procurement writer scoped to site A can act on a PO at site B. Resolve as a procurement-module RBAC decision (add a site-write scope check to both indents and purchase-orders), not a 4.4-only fix.
- Supplier onboarding auto-approval emits supplier.onboarding_submitted and supplier.onboarding_approved in two separate persistEvent transactions with unrelated correlation ids [src/api/v1/suppliers.ts:237-284] - deferred, belongs to the Story 4.1 supplier scope (inherited into this working tree, disclosed in Dev Agent Record). A failure after the first commit strands the supplier submitted-but-not-approved.
- Concurrent same-po_id purchase_order.drafted persists a phantom domain event [src/compliance/purchase-order.ts:360-365] - deferred, matches the accepted indent precedent: on a 23505 insert race the loser catches and returns while the outer domain_events insert still writes a drafted event with no projection applied. Reachable only via crafted direct /api/v1/events with an identical po_id.
- Multiple POs draftable from a single approved indent; only the first is ever issuable [src/compliance/purchase-order.ts:262] - deferred, low impact: applyPoDrafted has no guard that the source indent has no existing non-rejected PO, so a second draft succeeds but its later issue emits indent.ordered against an already-ordered indent (requires status approved) and is rejected, leaving a permanently un-issuable approved PO.

## Deferred from: code review of 4-7-supplier-invoice-capture (2026-08-06)

- DATE columns serialize as shifted timestamps on non-UTC servers [src/read/projections/supplier_invoice.ts:10] - deferred, pre-existing repo-wide: no pg.types.setTypeParser override exists anywhere, so node-postgres parses DATE (oid 1082) into a local-midnight JS Date and sendJson emits a shifted UTC ISO string (2026-04-01 renders as 2026-03-31T18:30:00.000Z under IST). Affects every existing DATE projection column (purchase_order.promised_delivery_date, cycle_count.business_date, etc.), not just invoice_date/statutory_due_date. Fix once globally with a type parser returning the raw string.
- Supplier-invoice write routes (capture/duplicate-override/link-po/stage/confirm) perform no site-write authorization [src/api/v1/supplier-invoices.ts:495-537] - deferred, same pre-existing module-wide pattern as the purchase-orders and indents entries above; a site-A procurement writer can capture against a site-B PO. Resolve as one procurement-module RBAC decision covering indents, purchase-orders, and supplier-invoices together.

## Deferred from: code review of 4-6-msme-compliance-tracking (2026-08-06, Group 1)

- CHECK vocabulary widening never propagates to existing databases [read/projections/supplier.sql:117] - deferred, pre-existing guarded-migration pattern repo-wide: the IF NOT EXISTS constraint guard skips re-adding a CHECK whose body needs to widen (e.g. a future MSME re-classification value), so existing DBs keep the old vocabulary until a manual or differently-named migration lands. Same shape as every guarded additive migration since Story 3.10.
- Concurrent db:migrate runs can abort on TOCTOU inside guarded DO blocks [src/events/migrate.ts:73] - deferred, pre-existing repo-wide pattern: two runners can both observe a constraint missing and both attempt ADD CONSTRAINT; the second raises 42710 and the unguarded DO block aborts mid-migration. No advisory lock around the migration runner anywhere in the repo.
- init-db.sql runs outside any transaction [deploy/compose/init-db.sql] - deferred, pre-existing file-wide pattern: an interrupted container first boot (OOM, eviction) leaves a partially-applied schema; the db:migrate upgrade path heals it via IF NOT EXISTS guards, but init-db itself is not re-runnable in an automated way on partial failure.

## Deferred from: code review of 4-6-msme-compliance-tracking (2026-08-06, Group 3)

- MSME ageing report has no pagination or row limit [src/read/projections/msme_ageing.ts:39] - deferred, pre-existing repo-wide pattern: every Phase 1 list endpoint (listSuppliers, listPurchaseOrders, listIndents) is similarly unbounded; response size and query time grow with MSME invoice volume. Resolve as one repo-wide pagination decision, not a 4.6-only fix.

## Deferred from: dev-story 4-5-goods-receipt-and-three-way-match (2026-08-06)

- Three-way-match write routes (link-po, match run, credit/debit note, clearance-feed run) perform no site-write authorization [src/api/v1/three-way-match.ts:98] - deferred, same pre-existing module-wide pattern as the indents, purchase-orders, and supplier-invoices entries above; a site-A procurement writer can bind or match a site-B document. Read routes ARE site-scoped. Resolve as the one procurement-module RBAC decision already tracked for the sibling routes.
- Receipts are aggregated at SKU grain, so a PO repeating one SKU across lines cannot attribute them [src/read/projections/three_way_match.ts:171] - deferred by design, fails closed: those lines are marked `ambiguous_sku` and the match blocks rather than double-counting. A real fix needs a per-line receipt attribution key, which Story 3.4's grn_line does not carry (its line_no is the receiving document's, not the PO's).
- The match compares against the PO's CURRENT line set, not a snapshot taken at match time [src/compliance/three-way-match.ts:236] - deferred, no PO line amendment path exists in Phase 1 (Story 4.4 excluded revision), so the line set is immutable in practice. Revisit if a purchase_order.line_amended event is ever added: an old match's variance_detail would then describe lines that no longer exist.
- Payment clearance has no partial or per-invoice release [src/read/projections/three_way_match.ts:361] - deferred, matches the msme_ageing_feed precedent: every run regenerates the FULL eligible set as one payload, so an ERP consumer must dedupe against its own payment records. A per-invoice clearance event is a larger design question tied to real payment state, which does not exist anywhere in this system.

## Deferred from: code review of 4-5-goods-receipt-and-three-way-match (2026-08-06)

- Client-controlled `occurred_at` reaches statutory timestamps [src/compliance/three-way-match.ts:344 and 400] - deferred, pre-existing platform-wide event-sourcing convention: the direct POST /api/v1/events route re-stamps the actor from auth but never `occurred_at`, so a direct event can backdate or postdate the match run (`recorded_at`) and the lift (`lifted_at`). True for every event type in the system; fixing it for 4.5 alone would fork the convention. Resolve as one platform decision on server-stamping `occurred_at`.
- Three-way-match write routes perform no site-write authorization [src/api/v1/three-way-match.ts:459-497] - already logged above in the dev-story 4-5 entry; re-confirmed by this review (no new entry needed, tracked as the one procurement-module RBAC decision).
- Receipts aggregate at SKU grain (`ambiguous_sku` fail-closed) [src/read/projections/three_way_match.ts:171] - already logged above in the dev-story 4-5 entry; re-confirmed by this review.
- GRN-to-PO link has no `po_ref_ext` supplier correspondence check [src/compliance/three-way-match.ts:193-228] - deferred, structurally not implementable from the current schema: `erp_purchase_order` carries only `supplier_ref_ext` (the ERP's own code) and the governed supplier carries `gstin_ext` + `owner_party_code` - three namespaces with no direct mapping (same lesson as the Story 2.9 owner-party referential at `src/compliance/ownership.ts:221-226`). A defensible correspondence check needs a dedicated `erp_supplier_ref_ext <-> governed supplier` mapping table, which is a separate work item; until then the applier relies on the spec-mandated PO existence + status check only.

## Deferred from: dev-story 4-2-supplier-performance-scorecards (2026-08-06)

- Quality-acceptance metric source does not exist until Epic 8 Story 8.3 [src/compliance/supplier-scorecard.ts:178] - deferred by design (AC2): the `qc.lot_dispositioned` event name is reserved in the schema doc comment only, the applier has a deliberate no-op case for `metric_kind = 'quality_acceptance'`, no write route exists, and the API returns `{ state: 'no_data' }`. Epic 8 activates the applier and registers the event; until then the projection column stays empty by construction.
- Responsiveness holiday calendar is deployment configuration with an empty default [src/config/index.ts config.scorecard] - deferred: `SCORECARD_RESPONSIVENESS_HOLIDAYS` (comma-separated YYYY-MM-DD) must be populated per deployment with the statutory holiday list before the business-day counts reflect real calendars; no holiday data ships in code by design.
- Scorecard reads are procurement-module-wide, not site-scoped [src/read/projections/supplier_scorecard_metric.ts:14] - deferred, same pre-existing pattern as the 4.7 supplier-invoice fallback: the supplier registry carries no site_id, so `permittedLocationsForModuleScope` cannot narrow the scorecard read. Resolve when a supplier site_id lands (already tracked as the supplier site_id deferral).
- Scorecard metric write routes perform no site-write authorization [src/api/v1/supplier-scorecards.ts] - deferred, same module-wide procurement RBAC pattern already tracked for indents, purchase-orders, supplier-invoices, and three-way-match; resolve as that one decision.

## Deferred from: code review of 4-2-supplier-performance-scorecards (2026-08-06)

- A replayed direct event without caller-supplied idempotency_key/event_id creates duplicate domain_events and audit rows while the projection replay guard still pins one metric row [test/integration/story-4-2.test.ts:1116-1135] - deferred, pre-existing platform-wide convention: AD-16 idempotency keys are caller-carried on every direct event surface (same class as the 4.5 client-controlled occurred_at deferral). Resolve as one platform decision on caller-less direct-event dedupe, not a 4.2-only fix.

## Deferred from: code review of 5-2-bom-lifecycle-and-immutability (2026-08-09, Group 1)

- Immutability guard accepts a missing or foreign `revision_id` (no existence or bom_id cross-check); `bom_line` has no FK to `bom_revision`, so a phantom/foreign revision can receive lines and collide on `uq_bom_line_no` (unmapped 23505, raw 500) [src/compliance/bom.ts:907, 1019] - deferred, pre-existing: the Story 5.1 lock also verified nothing; confirm in Group 2 whether handlers constrain `revision_id` server-side.
- `correlation_id` passed unvalidated into a UUID column (PG 22P02 500 on non-UUID) [src/compliance/bom.ts:706] - deferred, pre-existing pattern at bom.ts:816 (applyBomDrafted); low reachability since handlers compose it server-side.

## Deferred from: code review of 5-2-bom-lifecycle-and-immutability (2026-08-09, Group 2)

- RESOLVED by 5-3-eco-workflow-and-where-used-impact (2026-08-11): Cross-revision immutability bypass - `applyBomLineAmended` now filters the `bom_line` lookup by `bom_line_id AND revision_id` (`src/compliance/bom.ts`), and the handler pre-check in `src/api/v1/boms.ts` (`amendBomLineHandler`) 404s a stale/foreign `bom_line_id` before persisting. Covered by a dedicated cross-revision test in `test/integration/story-5-3.test.ts`.
- Global idempotency-key reuse silently no-ops a lifecycle transition: `uq_idempotency` is global and `persistEvent` returns the existing event on a key hit, so reusing one key across release then hold (or across different BOMs) drops the transition with a 200 and no error [src/api/v1/boms.ts:434] - deferred, pre-existing platform-wide convention (AD-16 caller-carried keys) and the exact Task 6 pattern; the migration handler's per-kit key composition is a documented, necessary deviation.

## Resolved: code review of 5-2-bom-lifecycle-and-immutability (2026-08-09, Group 2)

- Immutability guard accepts a missing or foreign `revision_id` [src/compliance/bom.ts:907, 1019] - RESOLVED: Group 2 confirmed all three consumers (add line, amend line, release) derive `revision_id` server-side from `bom.current_revision_id`, never from the request body, and direct engineering-stream event posts are rejected (events.ts:39 accepts only stream_type 'inventory'), so the phantom/foreign-revision path is not reachable. Closing this entry.

## Deferred from: code review of 5-2-bom-lifecycle-and-immutability (2026-08-09, Group 3)

- Partial-migration failure window: `bom.sql` and `bom_revision.sql` are applied as separate implicit transactions by migrate.ts, so a process death after bom.sql commits but before bom_revision.sql applies leaves the widened `chk_bom_status` with the Story 5.1 single-value `chk_bom_revision_status`; the new appliers then write `revision_status = 'released'` and hit an unmapped 23514 (raw 500) until migrate is re-run (which heals) [src/events/migrate.ts:80] - deferred, consistent with the repo's established per-file-transaction migration pattern and its documented re-run-heals stance; new cross-file interdependency introduced by 5.2's widened vocabularies.

## Deferred from: code review of 5-5-approved-alternates-and-bom-explosion (2026-08-13)

- ECO add change lines cannot express backflush: eco_change_line has no supply_method field, so every ECO-added line silently defaults to directed_issue while story 5.5's binding decision claims backflush authorability (AC 3). Direct add-line authoring (bom_line.added via the 5.2 route) does support supply_method, so the gap is limited to the ECO-add surface. Fix requires a 5.3 schema change (add supply_method to eco_change_line and the add-insert in src/compliance/eco.ts:514-537) - deferred, pre-existing 5.3 schema, out of story 5.5 scope.

## Deferred from: code review of 5-6-cost-rollups-job-work-kit-tagging-and-erp-outbound-sync (2026-08-14)

- `getLatestCompleteRollup` and `getLatestRollup` are dead code and diverge from the live gate predicate `evaluateCostRollupCondition`, which runs its own staleness-aware query [src/read/projections/bom_cost_rollup.ts:183-211] - deferred, spec-mandated accessors the gate predicate did not adopt
- Cost rollup capture runs outside the persist transaction, so a same-revision `bom_line` amendment between capture and commit yields a silently stale snapshot; the stale guard compares only `current_revision_id` [src/compliance/bom-costing.ts:286-306] - deferred, the release gate's staleness check (created_at versus MAX bom_line.updated_at) re-validates freshness at release
- `bom.sync_conflict_raised` for an unknown-BOM conflict uses the SCIM system-actor UUID as the stream id sentinel, conflating the actor and stream namespaces [src/api/v1/erp-projections.ts:43-44] - deferred, low, no functional break
- `emitBomSyncConflictEvents` swallows every `persistEvent` rejection with a silent catch, hiding shape-assert defects in the emission path [src/api/v1/erp-projections.ts:323-335] - deferred, the commit-then-emit crash window is an accepted tradeoff
- `bom_line` is_released_structure comment mirror diverges between the canonical file and init-db.sql [read/projections/bom_line.sql] - deferred, pre-existing from Story 5.5

## Deferred from: code review of 7-1-asset-register-and-criticality-classification (2026-08-16)

- stream_type/event_type mismatch bypasses all shape validation and the direct-writes guard [src/api/v1/events.ts:119, src/events/store.ts:199-221] - deferred, pre-existing platform-wide: persistEvent never checks SUPPORTED_EVENT_TYPES membership or stream/event consistency, and every module seam gates on its own stream/event pair; a mismatched pair (e.g. stream_type 'procurement' + event_type 'asset.registered') persists unvalidated with no projection row. Story 7.1 closes the exact-case guard per the engineering precedent; resolve as one platform-level registry check, not a 7.1-only fix.
- NUL byte in text fields or the search parameter surfaces as a raw 500 (unmapped SQLSTATE) [src/events/store.ts:875-1101] - deferred, pre-existing platform mapper gap (only 23505/23514 mapped) shared by every module's handlers; resolve as one platform error-mapping decision.
- Failed registrations write no audit entry (409 DUPLICATE_ASSET, 400 INVALID_PARAMS) [src/api/v1/assets.ts:127-129] - deferred, platform convention matching the supplier precedent; duplicate-attempt traceability needs a platform-level decision.

## Deferred from: code review of 7-2-preventive-maintenance-plans-and-work-order-generation (2026-08-16, Group 1)

- Schema-drift gate cannot detect extra constraints in the CREATE body: extractCreateTable compares init-db vs canonical only, and the guarded extractDoBlock check passes unless a constraint is missing, so Task 1.8's claim that the gate enforces every constraint and index is overstated (this story's entries are complete) [test/unit/schema-drift.test.ts] - deferred, pre-existing harness weakness

## Deferred from: code review of 7-2-preventive-maintenance-plans-and-work-order-generation (2026-08-16, Group 2)

- stream_type/event_type mismatch bypasses the registry's streamType: a maintenance.* event posted with a non-maintenance stream_type skips both seams' gates and persists with no projection applied [src/events/store.ts:199-221] - deferred, pre-existing platform-wide gap; same entry already logged under the Story 7.1 review heading (this is a re-affirmation, no separate fix)
- Client-supplied future metadata.occurred_at stamps projection timestamps in the future: the envelope validator accepts any ISO-8601 timestamp with no upper bound, so completed_at/overdue_at/silent_flagged_at can be written in the future on the direct-event path [src/events/store.ts:325-332] - deferred, platform-wide envelope-validation decision affecting every module (not a 7.2-only fix)

## Deferred from: code review of 7-2-preventive-maintenance-plans-and-work-order-generation (2026-08-16, Group 3)

- TIMESTAMPTZ columns typed string while pg returns Date objects: created_at/last_reading_at/completed_at etc. are Date at runtime; every consumer today is JSON/Date.parse-safe, and asset.ts (the Story 7.1 template) types them identically - deferred, pre-existing platform-wide typing convention; resolve as one platform typing decision (setTypeParser to string, or type columns as Date) [src/read/projections/*.ts]

## Deferred from: code review of 7-2-preventive-maintenance-plans-and-work-order-generation (2026-08-16, Group 4)

- A swallowed notification failure permanently loses the escalation: emitNotification returning ok:false after the work_order_overdue event committed leaves the work order 'overdue' (excluded from the sweep's list forever) with no retry path; the notification_deliveries ledger row is the only durable trace - deferred, AD-17 by design, platform-wide notification-delivery semantics (the sweep exposes the drop via escalations_raised; a future notification-retry story owns the compensating path) [src/maintenance/pm-jobs.ts:208-221]

- 2026-08-17 manifestation: story-5-2 "migrates a qualifying kit ... effective_from should default to today" fails between ~00:00-08:30 IST when the UTC date differs from the IST date (handler stamps UTC-derived date, test asserts IST-derived date; row renders as the shifted local-midnight Date) - same pre-existing repo-wide root cause as the entry above; re-verified that the BOM path is untouched by the 7.2 review changes (git diff clean on test/integration/story-5-2.test.ts, src/api/v1/boms.ts, src/compliance/bom.ts, src/read/projections/bom.ts); passes in the daytime window

## Deferred from: code review of 7-2-preventive-maintenance-plans-and-work-order-generation (2026-08-16, Group 5)

- Same-event-type idempotency-key reuse with different content returns the original event (201/200 with the wrong resource and the new write silently dropped): replayIdOrReject compares event_type and the id field's UUID-ness, never the rest of the payload - deferred, standard idempotency semantics (a key identifies a logical write; a replay returns the stored result) and the Story 7.1 review convention; the harmful cross-event-type reuse already resolves to 409 DUPLICATE_EVENT [src/api/v1/maintenance.ts:109-127]

- 2026-08-17 manifestation (02:00-08:30 IST window): story-5-3 "does not report superseded or retired lines in the where-used impact walk" fails when the clock date crosses the fixture's date boundary; reproduces in isolation; passed at 10:14 IST the same day - same pre-existing clock/date-dependent flake class as the story-5-2 entry above; verified no bom/eco/explosion/cost file is touched by the 7.2 review diff (git diff clean on the whole src tree except maintenance/events/store/server paths)

## Deferred from: dev-story 7-3-fault-reporting-and-breakdown-work-orders (2026-08-24)

- Superseding an active SLA policy (edit or deactivate) is out of scope for Phase 1: a second POST on an occupied (criticality_class, safety_flag) key returns 409 DUPLICATE_SLA_POLICY, and there is no update route, no deactivate path, and no re-derivation of open work orders [src/compliance/maintenance-fault.ts applySlaPolicyDefined, src/api/v1/maintenance.ts createSlaPolicyBase] - deferred by the story's Binding Scope Decisions ("Superseding an active SLA policy (edit or deactivate) is NOT in scope... Log the gap"); a policy-versioning path drags in effectivity windows and re-derivation of open work orders, which no AC requires
- DOWNTIME_ALREADY_OPEN (the 23505 resolution for uq_maintenance_downtime_work_order) is not directly exercised by a test: the seam opens the window only inside breakdown_work_order_created under the fault-report FOR UPDATE lock, so a sequential or direct-event duplicate always hits FAULT_ALREADY_TRIAGED (via the report status) or uq_maintenance_work_order_fault first; the mapper wiring is covered by the compile-time contract and the sibling 23505 mapper tests [src/events/store.ts, src/compliance/maintenance-fault.ts] - deferred, backstop-only code path with no reachable sequential trigger

## Deferred from: code review of 7-3-fault-reporting-and-breakdown-work-orders.md (2026-08-24)

- The schema-drift gate compares index NAMES only (.includes(indexName)) and ignores statement ordering, so a mirror-ordering drift (indexes created before the columns they reference) is invisible to it [test/unit/schema-drift.test.ts index assertions] - deferred, pre-existing gate limitation (the story spec itself documents the gate cannot detect an EXTRA constraint added only to a CREATE body); the Group 1 CRITICAL init-db.sql ordering defect slipped through this gate; add an ordering assertion (e.g. index position vs ADD COLUMN position) when the gate is next hardened

## Deferred from: dev-story 7-4-spare-parts-cataloguing-reservation-and-critical-spares-alerts (2026-08-25)

- Amending or removing an asset parts-list line is out of scope for Phase 1: a second POST on an occupied (asset_id, sku) key returns 409 ASSET_PART_ALREADY_LISTED, and there is no update route and no delete path [read/projections/asset_parts_list.sql, src/compliance/maintenance-spares.ts applyAssetPartListed] - deferred by the story's Binding Scope Decisions; no AC requires editing a parts list, and an amendment path raises the same revision-versus-immutability question AD-4 answers for the Epic 5 BOM, which an equipment list deliberately does not inherit
- Superseding a catalogue row's min-max levels is out of scope for Phase 1: a second POST on an occupied (sku, location_id) key returns 409 SPARE_ALREADY_CATALOGUED, so changing a minimum today requires a direct data fix [src/compliance/maintenance-spares.ts applySpareCatalogued, src/api/v1/maintenance.ts createSpareBase] - deferred by the story's Binding Scope Decisions; re-levelling is an operator workflow with effectivity-window semantics that no AC specifies
- Partial issue against a reservation is out of scope: issue is all-or-nothing on the reserved quantity, and a declared quantity that diverges from the locked reservation is rejected SPARE_DERIVATION_MISMATCH rather than partially applied [src/compliance/maintenance-spares.ts applySpareIssued] - deferred by the story's Binding Scope Decisions; partial RETURN is supported because the state machine needs it to settle a reservation, partial issue has no such forcing requirement
- Damaged-return disposition and scrap routing are not modelled: every returned spare goes back to owned on-hand at the reservation's location through applyStockReceipt regardless of condition [src/compliance/maintenance-spares.ts applySpareReturned] - deferred, quality holds and disposition belong to Epic 8; until then a damaged return must be adjusted out through the Epic 2 stock-adjustment path
- Spares cost is not accumulated onto the work order: the reservation carries quantity and SKU but no valuation, and no maintenance cost ledger is written on issue [src/compliance/maintenance-spares.ts applySpareIssued] - deferred, FR-M-15 maintenance cost accumulation per asset belongs to Story 7.6
- Warranty and AMC are not checked at reservation time: a spare can be reserved against a work order for an asset still under warranty with no prompt [src/compliance/maintenance-spares.ts applySpareReserved] - deferred, FR-M-10/11 warranty check at work-order creation belongs to Story 7.7
- Offline spares-issue confirmation is not delivered: every route in this story is central-only and no PowerSync sync rule covers the four new tables [src/api/v1/maintenance.ts Story 7.4 block] - deferred, FR-M offline technician workflows belong to Story 7.8
- The where-used read is not paginated beyond a flat limit/offset over asset_parts_list joined to asset, and gives no total count, so a caller cannot tell a full page from a truncated result [src/read/projections/asset_parts_list.ts listWhereUsedBySku] - deferred, matches the pagination convention of every sibling maintenance list route; a count/total contract is a cross-cutting API change, not a Story 7.4 one
- maintenance_storekeeper is a NEW role string with no registry to validate it against: roles are free-form text in user_role_assignments, so a deployment that never provisions the role leaves every critical-spares breach alert fanning out to zero recipients while emitNotification still reports ok [src/maintenance/spares-jobs.ts runCriticalSpareBreachScan, src/notify/dispatch.ts resolveTargetUserIds] - deferred, platform-wide gap (there is no role registry for ANY role in the codebase); the Story 7.4 suite provisions the literal role and asserts the fan-out resolves to at least one user, which catches the defect in test but cannot enforce it in production

## Deferred from: code review of 7-4-spare-parts-cataloguing-reservation-and-critical-spares-alerts (2026-08-25)

- Crash between alert commit and notification emission permanently loses that day's notification [src/maintenance/spares-jobs.ts:196-210, 302-314] - deferred, pre-existing accepted crash-window tradeoff consistent with the codebase precedent (emitNotification is non-throwing and runs after the grain transaction commits); the separate write/delivery counters expose a drop only within the run that dropped it, and a same-day re-run no-ops on the same-day guard. Resolve as one platform decision on notification durability across crash windows, not a 7.4-only fix.
- No direct HTTP POST /api/v1/events bypass test for a forged maintenance.* alert under a mismatched stream_type [src/api/v1/events.ts:119-128] - deferred, ties to the pre-existing platform-wide stream/event-type registry gap already logged under Story 7.1 (persistEvent never checks SUPPORTED_EVENT_TYPES membership or stream/event consistency); the maintenance seam's alert re-derivation now rejects forged alerts on the maintenance stream, but the mismatched-stream path still skips every seam.
- Spare SKU canonicalization is case-insensitive (lowercase) while the Epic 2 item_master/stock_balance are case-sensitive [src/compliance/maintenance-spares.ts:121-123] - deferred platform decision (RESOLVED as keep-the-story-choice for 7.4): an item registered as a mixed-case SKU (e.g. 'SPR-100') cannot be catalogued by the spare module because canonicalSku lowercases before the item_master lookup, and the stock ledger matches sku case-sensitively. Aligning SKU case-sensitivity is a platform-level decision affecting item_master, stock_balance and every consumer; resolve once across the platform, not per story.

## Deferred from: dev-story 7-5-calibration-register-and-non-overridable-lockout (2026-08-25)

- Amending or voiding a recorded calibration certificate is out of scope for Phase 1: a certificate is immutable once recorded, a second POST on an occupied (instrument_record_id, lower(certificate_number)) key returns 409 CERTIFICATE_ALREADY_RECORDED, and there is no update route and no void path [read/projections/instrument_calibration_certificate.sql, src/compliance/calibration-register.ts applyCertificateRecorded] - deferred by the story's Binding Scope Decisions; a corrected certificate is recorded as a new one that supersedes the previous active row, and the superseded row is retained, so the correction path exists without an amendment surface
- Calibration due-date scheduling and workload planning are not built: calibration_interval_days is captured on instrument_register for the alert horizon and for operator reference, and nothing reads it to propose a next-due date, build a calibration calendar, or level a workload [read/projections/instrument_register.sql, src/maintenance/calibration-jobs.ts] - deferred by the story's Binding Scope Decisions; no AC requires a planning surface, and scheduling drags in the same effectivity-window semantics the Story 7.2 plan tree already owns for preventive maintenance
- Calibration is not tied to the Story 7.2 work-order tree: recording a certificate creates no maintenance_work_order row, and no work order can be raised for a re-calibration [src/compliance/calibration-register.ts, src/read/projections/maintenance_work_order.ts] - deferred by the story's Binding Scope Decisions; no AC connects the two trees, and binding them would make every certificate recording depend on an SLA policy existing for the instrument's criticality class
- Per-parameter and per-range calibration points, measurement uncertainty budgets and traceability chains are not modelled: a certificate carries validity dates, a type and an issuing laboratory, and nothing finer [read/projections/instrument_calibration_certificate.sql] - deferred, FR-Q measurement detail belongs to Epic 8; the lockout only needs to know whether a certificate is valid today
- The lockout is not extended to measurement write paths beyond qc.result_recorded: the Story 1.7 gate in src/compliance/calibration.ts still fires for exactly one event type [src/compliance/calibration.ts assertCalibrationLockout, src/events/store.ts] - deferred, weighbridge trade weighment is FR-M-14 and belongs to Story 7.6; every Epic 8 QC path that emits qc.result_recorded lands on the existing gate for free, so this is a gap only for non-QC measurement events
- Offline certificate capture is not delivered: every route in this story is central-only and no PowerSync sync rule covers the four new tables [src/api/v1/maintenance.ts Story 7.5 block] - deferred, FR-M offline technician workflows belong to Story 7.8
- Certificate document and PDF attachment storage is not provided: certificate_number and issuing_lab are the only evidence the register holds, and the scanned certificate lives outside the system [read/projections/instrument_calibration_certificate.sql] - deferred, no attachment service exists in Phase 1
- status_event_version is written as NULL on every register-driven calibration status change: the register writes instrument_calibration_statuses from INSIDE persistEvent's transaction, before the domain_events row exists and therefore before the stream's next event_version has been assigned; status_event_id is always recorded [src/read/projections/instrument_calibration.ts setCalibrationStatusFromRegister, src/events/store.ts] - deferred, the column is nullable and the event id is a complete audit link on its own; resolving it means either computing MAX(event_version)+1 a second time in the applier (a duplicate of the store's own logic, free to diverge) or moving projection application after the domain_events insert, which is a platform-wide change to persistEvent
- A maintenance.* event posted with a non-maintenance stream_type skips every maintenance seam gate, including this story's [src/events/store.ts, src/api/v1/events.ts] - deferred, pre-existing platform-wide stream/event-type registry gap already logged under Stories 7.1 and 7.4 (persistEvent never checks SUPPORTED_EVENT_TYPES membership or stream/event consistency). This gap is MORE dangerous in Story 7.5 than in any previous story: the events it would smuggle past the seam write calibration LOCKOUT status, so a forged maintenance.calibration_certificate_recorded on a foreign stream_type would unlock an instrument for measurement with no derivation check at all. The direct-events HTTP guard blocks stream_type maintenance today, which narrows but does not close the hole for any other in-process caller. Resolve as one platform decision on stream/event-type consistency, not as a 7.5-only patch
- Same-event-type idempotency-key reuse with different content returns the original event: replayIdOrReject compares event_type and the id field's UUID-ness, never the rest of the payload, so a second certificate recorded under a reused key silently returns the first certificate [src/api/v1/maintenance.ts replayIdOrReject] - deferred, standard idempotency semantics and the Story 7.1 review convention, already logged under Story 7.2; the harmful cross-event-type reuse resolves to 409 DUPLICATE_EVENT and is covered by a test in this story

## Deferred from: dev-story 7-6-statutory-examinations-cost-accumulation-and-machine-status-broadcast (2026-08-25)

- Routing above-threshold repair-vs-capitalize flagged work orders into the FR-FA capitalization queue is out of scope for Phase 1: work orders whose total_cost exceeds config.maintenance.capitalizationThreshold are flagged (capitalization_flagged = true) at closure and nothing more happens [src/compliance/maintenance-plan.ts applyWorkOrderCompleted] - deferred by the story's Binding Scope Decisions; the FR-FA capitalization queue is Phase 2 Epic 17
- Offline technician workflows and closure codes are not delivered: every route in this story is central-only and no PowerSync sync rule covers the four new tables [src/api/v1/maintenance.ts Story 7.6 block] - deferred, FR-M offline technician workflows belong to Story 7.8
- Staged pre-due statutory examination alerts (30/14/7-day style) are not built: the overdue scan flips straight to 'overdue' and emits the escalating notification, with no pre-due warning stages [src/maintenance/statutory-jobs.ts runStatutoryExaminationScan] - deferred by the story's sprint-change-proposal rescope (Binding Decision 9); adding staged alerts would be scope creep
- Statutory examinations are not tied to the Story 7.2 work-order tree: recording an examination creates no maintenance_work_order row, and no work order can be raised for a re-examination [src/compliance/maintenance-statutory.ts applyStatutoryExaminationRecorded] - deferred by the story's Binding Scope Decisions; no AC ties statutory examinations to the work-order tree
- The statutory use-lock is not extended to measurement write paths beyond weighbridge.recorded and return-to-service: the status-change handler and the weighbridge lockout gate are the only two enforcement points [src/api/v1/maintenance.ts setAssetStatusBase, src/compliance/weighbridge.ts assertWeighbridgeStampLockout] - deferred, every Epic 8 QC path lands on the existing Story 7.5 calibration gate for free
- Statutory examination document and PDF attachment storage is not provided: certificate_number_ext and device_key are the only evidence the register holds, and the scanned examination certificate lives outside the system [read/projections/statutory_examination.sql] - deferred, no attachment service exists in Phase 1
- A maintenance.* event posted with a non-maintenance stream_type skips every maintenance seam gate, including this story's [src/events/store.ts, src/api/v1/events.ts] - deferred, pre-existing platform-wide stream/event-type registry gap already logged under Stories 7.1, 7.4 and 7.5 (persistEvent never checks SUPPORTED_EVENT_TYPES membership or stream/event consistency). This gap is MORE dangerous in Story 7.6 than in any previous story: the events it would smuggle past the seam could write a compliant statutory examination status (suppressing the genuine lockout) or flip asset_operational_status with a fabricated sign-off. The direct-events HTTP guard blocks stream_type maintenance today, which narrows but does not close the hole for any other in-process caller. Resolve as one platform decision on stream/event-type consistency, not as a 7.6-only patch
- Same-event-type idempotency-key reuse with different content returns the original event: replayIdOrReject compares event_type and the id field's UUID-ness, never the rest of the payload, so a second status change under a reused key silently returns the first one [src/api/v1/maintenance.ts replayIdOrReject] - deferred, standard idempotency semantics and the Story 7.1 review convention, already logged under Stories 7.2 and 7.5; the harmful cross-event-type reuse resolves to 409 DUPLICATE_EVENT
- The statutory scan notification targets the location of the operator who ran the scan, not a physical asset location: the Story 7.1 asset register has no location column (assets are enterprise-scoped per AD-9), so the Notification Contract's '<asset location>' resolves to the scan actor's location_id [src/maintenance/statutory-jobs.ts runStatutoryExaminationScan] - deferred, adding an asset-to-location mapping is a cross-module model change not present in AD-9
- A completed work order on a weighbridge asset invalidates the stamp regardless of the work order's content: ANY completion flips a compliant weighbridge_legal_metrology examination to 'overdue' (fail-closed for trade weighment integrity) [src/compliance/maintenance-plan.ts applyWorkOrderCompleted] - deferred by Binding Decision 6; work-order-type-scoped stamp invalidation would require a work-order classification that does not exist in Phase 1

## Deferred from: code review of 7-6-statutory-examinations-cost-accumulation-and-machine-status-broadcast (2026-08-25)

- `resolveApprover` acquires a second pool connection from inside an applier that already holds an open transaction with FOR UPDATE row locks, and it reads the DOA registry outside that transaction's snapshot, so the "re-derived under lock" claim is only partly true and a saturated pool can stall every concurrent return-to-service [src/compliance/asset-operational-status.ts:217, src/api/v1/indents.ts:66] - deferred, pre-existing platform pattern shared with src/compliance/purchase-order.ts:437 and :881; resolving it means threading a PoolClient through resolveApprover and its four accessors, a cross-module change to the DOA layer
- An uppercase-but-canonical UUID in a payload fails every re-derivation comparison: the shape validators use case-insensitive UUID regexes while PostgreSQL renders `uuid` columns as lowercase text, so a valid payload rejects 409 STATUTORY_DERIVATION_MISMATCH or COST_DERIVATION_MISMATCH [src/compliance/asset-operational-status.ts:182, src/compliance/maintenance-statutory.ts:404, src/compliance/maintenance-plan.ts:628] - deferred, pre-existing platform-wide idiom across every seam since Story 7.1; resolve as one convention (normalize UUIDs to lowercase in the shape asserts) rather than a 7.6-only patch
- The 423 weighbridge lockout runs in the pre-transaction assert block, ahead of the in-transaction idempotency replay short-circuit, so a retry of an already-persisted weighment whose stamp lapsed in the meantime rejects 423 WEIGHBRIDGE_OUT_OF_STAMP instead of returning the stored event [src/events/store.ts:436, :650] - deferred, exact precedent of assertCalibrationLockout placement already accepted in Story 7.5; moving it after the replay lookup would let a malformed weighment consume an idempotency key
- `findRoleHolder` selects only the earliest-assigned active holder of a role (ORDER BY created_at ASC LIMIT 1), so when a role has several active holders only that one can sign off a return to service; any other equally authorized supervisor is rejected [src/read/projections/doa_registry.ts:302, src/compliance/asset-operational-status.ts:226] - deferred, pre-existing DOA resolution semantics shared by every approval path in the system; resolve as one DOA decision on multi-holder roles

## Deferred from: code review of 7-6-statutory-examinations-cost-accumulation-and-machine-status-broadcast, Group B (2026-08-26)

- The POST-triggered scan's `business_date` is validated for calendar form only, never for plausibility, so a typo or a bad client clock (`2999-01-01`) flips every compliant statutory examination in the register to overdue in one call - locking every asset from use per AC1 and blocking trade weighment on every weighbridge per AC2, recoverable only by physically re-examining and re-stamping each one [src/api/v1/maintenance.ts requireBusinessDate, src/maintenance/statutory-jobs.ts runStatutoryExaminationScan] - deferred, `requireBusinessDate` is the shared validator for every POST-triggered job in the codebase (Stories 7.2, 7.4, 7.5 and 7.6) and no future-date bound exists anywhere; adding one is a platform convention decision, not a 7.6-only patch. The blast radius is larger here than on any previous job, so this should be the story that forces the decision
- The scan loop is unbounded: `listOverdueExaminationsDue` takes no LIMIT and no cursor, and the loop opens one pool connection and one transaction per due row inside a single HTTP request, so a first scan after a long gap performs N sequential round trips with nothing to cap it and no way to resume after a proxy timeout [src/maintenance/statutory-jobs.ts, src/read/projections/statutory_examination.ts listOverdueExaminationsDue] - deferred, identical to the Story 7.5 calibration scan it was copied from; batching is a shared job-runner change across all four POST-triggered scans
- `APPROVAL_UNRESOLVED` is raised with 404 by the Story 7.6 handler and seam (no DOA entry governs the transaction type, per the story's Table 6) but with 409 by the shared `resolveApprover` (an entry exists and no active holder resolves), so one error code carries two HTTP statuses on the same route [src/api/v1/maintenance.ts setAssetStatusBase, src/compliance/asset-operational-status.ts, src/api/v1/indents.ts resolveApprover] - deferred, the 409 comes from shared platform code used by every approval path (indents, purchase orders, ECOs); reconciling the two is a DOA-layer decision
- The AC4 broadcast dedup evaluates `metadata->>'correlation_id'` against every `notification.created` row in `domain_events` with no supporting expression index, on every asset status change, and always returns zero rows on the non-replay path [src/api/v1/maintenance.ts setAssetStatusBase] - deferred, adding an expression index on a high-volume event table is a schema and migration decision beyond this story; the same replay-dedup shape is used by other stories
- The per-asset cost rollup ADDs into `NUMERIC(14,3)` with no ceiling, so once an asset's lifetime running total passes the column range every subsequent work-order completion on that asset fails - now as a clean 422 COST_VALUE_OUT_OF_RANGE rather than a 500, but still permanently and with no path to correct the rollup short of direct SQL [src/read/projections/maintenance_asset_cost.ts upsertMaintenanceAssetCost] - deferred, the Group A review mapped 22003 to a stable 422; widening the column or adding a rollup-reset path is a schema change the story does not call for

## Deferred from: code review of 7-6-statutory-examinations-cost-accumulation-and-machine-status-broadcast, Group C (2026-08-26)

- No RBAC negative path exists for any of the nine Story 7.6 routes: every write in the suite uses supervisor headers, and the only 403s asserted are the DOA `APPROVAL_REQUIRED` business rule raised for an actor who already holds maintenance write, so the `requireRole` wrappers are never exercised negatively and no unauthenticated 401 is tested [test/integration/story-7-6.test.ts] - deferred, this is the convention across every story suite in the repo, not a 7.6 regression; a `requireRole` matrix belongs in one shared harness rather than being re-derived per story
- The concurrency tests cannot reach the 23505 race path their titles name: `applyStatutoryExaminationRecorded` and `applyAssetOperationalStatusProjection` both take the Locking Contract's step-1 asset lock FOR UPDATE, so two parallel attempts on one asset serialize and the loser is refused by the sequential pre-check, never by the unique index. The tests still assert the stable contract (same code, same `existing_*` detail) that Task 8.7 asks for [test/integration/story-7-6.test.ts CONC tests] - deferred, the unreachability is a consequence of the Locking Contract working as designed; exercising the index mapping needs a harness that can suspend a transaction mid-applier, which does not exist
- Work orders are hand-inserted straight into `maintenance_work_order` with a phantom `plan_id`, so all nine cost and stamp-invalidation tests complete work orders the Story 7.2 generation seam never produced; any column or invariant the real generator sets is absent from the fixture [test/integration/story-7-6.test.ts insertOpenWorkOrder] - deferred, the direct-fixture pattern is the established harness convention across Stories 7.2 to 7.6; changing it means routing every cost test through PM plan definition and generation
- Ten `persistEvent(... as any)` casts appear in the direct-event forgery tests, disabling the compile-time envelope contract in exactly the tests that exist to defend it, so a renamed payload field fails at runtime with a confusing code instead of at build [test/integration/story-7-6.test.ts] - deferred, the cast is the repo-wide idiom for the direct-event path in every story suite; typing it properly means exporting per-event envelope types the store does not currently expose
- `OVERDUE_SCAN_DATE` is hardcoded to a date roughly ten months in the future, so re-stamp rows are written with `examined_on` in 2027 and `recorded_at` at wall-clock now, and `seedWeighbridgeChain` pins 2026 fixture dates against `now()` [test/integration/story-7-6.test.ts] - deferred, the same fixed-future-date scheme is used by Stories 7.2 to 7.5; a freshness check would fail all of them at once, and the fix is one shared clock convention for the integration harness
- The Story 1.9 Spine allowlist widening asserts membership only, through a `.sort()`ed `deepStrictEqual` inside a `before()` hook, so the load-bearing route ordering `src/server.ts` documents (static `/scan` before the `/:examinationId` parameter route) has no test [test/integration/story-1-9.test.ts allowedSpineRoutes] - deferred, the allowlist's shape is a Spine-wide contract; adding order assertions is a change to how every story registers its routes with that test

## Deferred from: dev-story 6-1-production-order-creation-and-release-gate (2026-08-26)

- Material staging, issue, backflush and the production WIP ledger (Story 6.2, FR-MO-04 to FR-MO-06). This story creates no stock movement and no allocation: the release gate verifies availability, it does not reserve it, and the WIP ledger does not exist yet.
- Completions, co-products, by-products, scrap declarations and completion tolerances (Story 6.3, FR-MO-07 to FR-MO-10). The completed transition is a pure state change here; the completion accounting is Story 6.3.
- Lot genealogy, the closure gate, closed-order immutability, offline execution and the central-only enforcement of release, cancel and close (Story 6.4, FR-MO-11 to FR-MO-13). Story 6.4 owns offline replication of order data; no PowerSync bucket and no edge path exist in this story.
- The consumption variance report (Story 6.4, FR-B-08). The report has no FR-MO line in this story's scope.
- Existence resolution of `source_reference_id` against the Story 2.9 `erp_sales_order` and Story 4.3 indent projections. Phase 1 records the reference on the order; it does not resolve it. The columns carry no FK and no consumer check.
- Rework orders raised from QC dispositions (Story 6.3, FR-MO-10), blocked on Epic 8. The Epic 8 QC-disposition hard prerequisite binds the Story 6.4 closure gate only, not this story.
- Kit-assembly order naming (sprint-change-proposal FR-I-09 scope note, Stories 6.1 to 6.3 collectively). The `source_reference_type` vocabulary is fixed at erp_sales_order/indent/rd_project/manual; kit naming will widen it when the FR lands.

## Deferred from: dev-story 7-7-amc-warranty-and-insurance-tracking (2026-08-27)

- No amendment, void or supersede path exists for a coverage record. `asset_coverage` is append-only in Phase 1 (Binding Decision 5) and a renewal is a NEW row with a fresh set of 90/60/30 stages, so a mistyped provider, reference or expiry date persists until the contract expires and can only be corrected by direct SQL [read/projections/asset_coverage.sql, src/compliance/maintenance-coverage.ts] - deferred, an amendment path needs an event family and a supersede grain the story does not scope, and the Story 7.5 certificate precedent uses the same renewal shape.
- No reservation-time warranty prompt is added to the Story 7.4 spare-reservation flow (Binding Decision 14). The FR-M-10/11 check is anchored to work-order creation by the epic and by the Story 7.4 deferral entry itself, so that entry stands as written - deferred, reservation-time prompting appears in no FR.
- Insurance claims administration stays out of scope (PRD Non-Goal). Policy references, coverage windows and expiry alerts are recorded; claims themselves are administered in ERP or manually - deferred by the PRD, not by this story.
- No edge or offline capture path exists for coverage recording or the warranty override, and no PowerSync sync rule replicates either (Binding Decision 12). Enforcement lands in `persistEvent`, so the future edge path inherits the chargeable-work gate unchanged [src/events/store.ts] - deferred, Story 7.8 owns offline technician workflows.
- The Story 7.3 accept handler derives its own `business_date` as a UTC `slice(0, 10)` of the wall clock, and the Story 7.7 warranty derivation consumes that same payload `business_date`, so the IST-versus-UTC near-midnight sliver is inherited unchanged rather than introduced here: a fault accepted between 18:30 and 24:00 IST evaluates warranty coverage against the previous UTC calendar date [src/api/v1/maintenance.ts acceptFaultReportBase, src/compliance/maintenance-fault.ts applyBreakdownWorkOrderCreated] - deferred, moving the accept handler onto `toIstCalendarDate` changes Story 7.3 behaviour and belongs with the platform-wide business-date convention.
- The one-override-per-work-order grain is NOT pre-checked in `recordWarrantyOverrideBase`, unlike the other pre-checks the story's Task 7.1 lists. A handler pre-check runs before `persistEvent` resolves the idempotency key, so it turned a legitimate same-key replay into a 409 and broke the AD-16 replay contract; the seam enforces the grain under the work order's lock and the `uq_maintenance_warranty_override_work_order` 23505 resolver backs it, so a genuine second override still returns 409 `WARRANTY_OVERRIDE_ALREADY_RECORDED` with the same `existing_override_id` [src/api/v1/maintenance.ts recordWarrantyOverrideBase] - recorded as a deliberate deviation, not a gap; the `raiseCalibrationEscalationBase` route resolves the same tension the same way.
- The inherited platform gaps logged under Stories 7.5 and 7.6 are re-affirmed unchanged and are NOT fixed in this story: a `maintenance.*` event carried on a mismatched `stream_type` skips every seam, so a forged coverage or override could smuggle past on a non-maintenance stream; same-event-type idempotency-key reuse returns the original event rather than a conflict; the gap between the alert commit and the notification emission loses the notification on a crash; `requireBusinessDate` validates calendar form only, so an implausible-but-valid `2999-01-01` on the coverage record or scan route is accepted; a notification aimed at a role no user holds fans out to zero recipients while reporting success; and the notification dedup convention rides an unindexed `payload->>'object_id'` scan on `domain_events` [src/events/store.ts, src/api/v1/maintenance.ts, src/maintenance/coverage-jobs.ts] - deferred, every one is a platform-wide convention decision with a blast radius far beyond Story 7.7.

## Deferred from: code review of 6-1-production-order-creation-and-release-gate (2026-08-27, Group A)

- No stream_type/event_type registry cross-check in `persistEvent`: a forged `production_order.*` event posted under a foreign stream_type (for example `stream_type` warehouse with `event_type` `production_order.released`) passes `validateEnvelope` (which checks stream_type only as a non-empty string), skips the seam's stream gate, and persists an unvalidated phantom event into `domain_events` with no projection row [src/events/store.ts:256-410, src/compliance/production-order.ts:109-113] - deferred, pre-existing platform-wide gap already logged under Stories 7.1, 7.2, 7.4 and 7.5 (deferred-work entries for stream_type/event_type mismatch); the four new production event types are new reachable instances of the same hole, and the fix is one platform-level registry check in persistEvent, not a 6.1-only patch.

## Deferred from: code review of 6-1-production-order-creation-and-release-gate (2026-08-27, Group B)

- `APPROVAL_UNRESOLVED` is raised with 404 by the release handler when no DOA entry governs `production_order.release_override`, but with 409 by the shared `resolveApprover` when an entry exists and no active holder resolves, so one error code carries two HTTP statuses on the same route [src/api/v1/production-orders.ts:507-513, src/api/v1/indents.ts:96-104] - deferred, pre-existing platform issue already logged under the 7-6 review heading (the 409 comes from shared platform code used by every approval path); reconciling the two is a DOA-layer decision, not a 6.1-only patch.

## Deferred from: code review of 6-1-production-order-creation-and-release-gate (2026-08-27, Group C)

- The release gate's `depth_truncated` path has no test anywhere in the repo: story-5-5/5-6 only assert `depth_truncated: false`, and triggering the truncation branch needs a BOM chain 21 levels deep at the default `config.bom.maxDepth` of 20 (roughly 20 BOM draft/rollup/release cycles per fixture) [src/production/release-gate.ts, test/integration/story-5-5.test.ts] - deferred, the gate is fail-closed (a truncated explosion returns `satisfied: false` and the release path rejects 409 INSUFFICIENT_STOCK, and the seam hard-fails even under an override per the Group A patch); exercising it needs a reduced test maxDepth or a dedicated deep-chain fixture, a harness/config decision beyond this story.
- The release gate's `empty_requirement_set` path has no test and is not constructible through the API: BOM creation requires at least one line (`BOM_LINE_REQUIRED`), so no released BOM can explode to zero requirement rows [src/production/release-gate.ts, src/api/v1/boms.ts:87-89] - deferred, the zero-requirement branch is defensive fail-closed and unreachable by construction.
- The delegated Table 5 error codes `COMPONENT_SKU_UNRESOLVED`, `EXPLOSION_QUANTITY_INVALID` and `BOM_EXPLOSION_CYCLE_DETECTED` have no tests and are not reachable through the API: SKUs are mandatory on items and placeholders are barred at BOM draft, order quantity is validated at order create, and cyclic BOMs are barred at draft [test/integration/story-6-1.test.ts] - deferred, each is a fail-closed branch that the upstream Story 5.x guards already prevent from being reachable; constructing them would require direct SQL fixtures that bypass the seams the tests exist to defend.
- The coverage scan loop is unbounded, exactly like its Story 7.5 and 7.6 twins: `listCoverageStagesDue` takes no cursor, and the loop opens one pool connection and one transaction per due stage inside a single HTTP request, with no way to resume after a proxy timeout [src/maintenance/coverage-jobs.ts, src/read/projections/asset_coverage.ts] - deferred, batching is a shared job-runner change across all four POST-triggered scans.

## Deferred from: code review of 7-7-amc-warranty-and-insurance-tracking (2026-08-27, Group A)

- Replay of a `maintenance.breakdown_work_order_created` event persisted before Story 7.7 re-derives `warranty_flagged` and `warranty_coverage_id` against the coverage register as it stands at replay time, not as it stood at the original occurrence. Both fields are optional on `BreakdownWorkOrderCreatedPayload` and a declared value is rejected with `WORK_ORDER_DERIVATION_MISMATCH`, so no persisted payload ever carries them and there is nothing to replay from. Reproduction: a breakdown work order created 2026-06-01 against an asset with no coverage persists `warranty_flagged = false`; a warranty covering 2026-05-01 to 2026-12-31 is recorded for that asset on 2026-07-01; replaying the original event on 2026-09-01 rebuilds the row as `warranty_flagged = true`, and `applyWorkOrderCompleted` now demands an override on a work order that historically completed without one. The same event log therefore rebuilds to two different projections [src/events/schema.ts:2288-2297, src/read/projections/maintenance_work_order.ts:162-163, src/compliance/maintenance-fault.ts applyBreakdownWorkOrderCreated] - deferred by code review decision D4, the correct fix makes persisted payloads carry the seam-derived pair and teaches the mismatch rejection to distinguish a client declaration from a write-back, which changes the declared-field contract Binding Decision 3 pins and Group B enforces across 39 tests; it is a Group B and event-contract change, not a Group A patch.
- `coverage_type` is declared on `CoverageExpiryFlaggedPayload` as a derived-and-checked field, but `asset_coverage_alert` has no `coverage_type` column, unlike `asset_id` and `expiry_date` which are both denormalised onto the alert row. `listCoverageAlerts` therefore exposes no coverage_type filter and answering "which AMCs are expiring" needs a join to `asset_coverage` that no Group A accessor provides [src/events/schema.ts:2936, read/projections/asset_coverage_alert.sql:21-32] - deferred, Task 1.2 fixes the alert column list and adding a column is a spec change, not a review patch.
- `asset_coverage.updated_at` is defined `TIMESTAMPTZ NOT NULL DEFAULT now()` but is written by no accessor and no trigger, while still being exported on `CoverageRow` and selected by `COVERAGE_COLUMNS`. Every API consumer is handed a "last amended" timestamp that can only ever equal `created_at` [read/projections/asset_coverage.sql:37, src/read/projections/asset_coverage.ts:25,61] - deferred, the column is spec-mandated by Task 1.1 and is harmless until Binding Decision 5's amendment path lands.
- The uniqueness grain folds case but not whitespace: `uq_asset_coverage_reference` keys on `lower(reference_number_ext)` with no `btrim`, and `chk_asset_coverage_reference_ext` uses `btrim(x) <> ''` which strips U+0020 only, so a tab or non-breaking space passes as non-empty. Group B's `canonicalCoverageReference` trims before write, so the database gap is defence in depth only [read/projections/asset_coverage.sql:40,45] - deferred, repo-wide precedent shares the gap (`uq_asset_tag`, `uq_instrument_register_instrument_id`, `uq_asset_meter_code`) and changing an index expression is a platform-wide migration decision.
- `lower()` is collation-dependent and nothing under `deploy/` pins `LC_COLLATE`, `LC_CTYPE` or `POSTGRES_INITDB_ARGS`, and the column carries no `COLLATE` clause, so the same event stream replays to two different register states on a C-locale host versus a UTF-8-locale host. Group B folds with JS `toLowerCase` while the index folds with SQL `lower()`, which diverge on Turkish dotted capital I (U+0130) [read/projections/asset_coverage.sql:30,45, src/compliance/maintenance-coverage.ts:103] - deferred, pinning database locale is a deployment-wide decision beyond Story 7.7.
- `reference_number_ext` is unbounded `TEXT` under a B-tree expression index, and only Group B caps it at 512 characters. A direct insert or a replay carrying a 4000-character reference raises SQLSTATE 54000 "index row size exceeds maximum" and aborts the enclosing `persistEvent` transaction as an unmapped 500 [read/projections/asset_coverage.sql:30,45] - deferred, Task 1.1 fixes the CHECK set and the reachable path is already bounded at the seam.
- A stage still unfired at the moment a coverage lapses is lost silently: the scan requires `expiry_date >= business_date`, so once the contract expires it drops out permanently, and the `(coverage_id, stage_days)` grain holds no row recording that the stage was never reached. An expired contract that was never warned about is indistinguishable from one that was [src/read/projections/asset_coverage.ts:265] - deferred, a closing or missed-stage alert is a new alert class outside AC 1, and the same shape is inherited verbatim from the Story 7.5 Staged Alert Contract.
- `asset_coverage_alert` denormalises `asset_id` and `expiry_date` at flag time with no foreign key to `asset_coverage` and no consistency guard, while `app_user` holds UPDATE on `asset_coverage`. Firing the 90-day alert and then changing the coverage expiry leaves the alert history reporting two different expiry dates for one contract with no record of which is current [read/projections/asset_coverage_alert.sql:26-27, read/projections/asset_coverage.sql:112] - deferred, projections repo-wide carry no cross-projection foreign keys and this is contingent on the UPDATE grant decision.
- `maintenance_warranty_override.warranty_coverage_id` is `UUID NOT NULL` with no `REFERENCES asset_coverage`, so replaying `warranty_override_recorded` into a database rebuilt only to the Story 7.6 tail, or against a coverage register restored from an earlier snapshot, leaves an override pointing at a coverage id that does not exist, and every lookup through `idx_maintenance_warranty_override_coverage` silently returns nothing rather than surfacing the orphan [read/projections/maintenance_warranty_override.sql:25,35] - deferred, the no-cross-projection-foreign-key convention is repo-wide.
- `chk_asset_coverage_dates` is `expiry_date > start_date`, strictly greater, so a one-day insurance cover note with `start_date = expiry_date` cannot be recorded at all and has no alternative encoding, while the equally degenerate zero-days-remaining case (expiry equal to business date) is accepted everywhere else in the story [read/projections/asset_coverage.sql:41, src/compliance/maintenance-coverage.ts:166] - deferred, the constraint is spec-mandated by Task 1.1 and widening it needs a business call on whether same-day cover is real.
- `business_date DATE` and `flagged_at TIMESTAMPTZ` on an alert row are independent columns with no CHECK relating them and `flagged_at` is envelope-supplied, so a row can carry a `flagged_at` a year ahead of its `business_date` and sort to the top of the `flagged_at DESC` alert feed [read/projections/asset_coverage_alert.sql:26-28] - deferred, envelope timestamp trust is a platform-wide convention.
- The `test/unit/schema-drift.test.ts` suite is left red at 107 pass and 1 fail on the pre-existing `gate_dwell_metric` CRLF-versus-LF mismatch that Task 1.5 names explicitly and does not fix. A permanently red drift suite means a genuine future regression inside it will not stand out from the standing failure [test/unit/schema-drift.test.ts:1598] - deferred, pre-existing and already carried forward from Stories 7.5 and 7.6.

## Deferred from: code review of 7-7-amc-warranty-and-insurance-tracking (2026-08-27, Group B)

- `applyWarrantyOverrideRecorded` holds `maintenance_work_order` FOR UPDATE on the transaction connection and then calls `resolveApprover`, which takes no client and issues two to four fresh pool checkouts (`findMatchingDoaEntry`, `findRoleHolder`, `findActiveDelegation`, `listActiveDoaEntries`). The same shape repeats in the scan, where `inTransaction` holds the `asset_coverage` row lock and `persistEvent` then runs its pre-transaction asserts on the pool. With `PGPOOL_MAX` simultaneous override posts, every connection sits inside a transaction awaiting a connection nobody can release, and the requests hang until the acquire timeout [src/compliance/maintenance-coverage.ts:544, src/api/v1/indents.ts:66-95, src/events/store.ts:440-446] - deferred, the nested-checkout-under-lock shape is platform-wide (every seam that resolves an approver or rides persistEvent's pre-transaction asserts does it) and the fix is a client-threading change across the DOA layer, not a 7.7 patch.
- Preventive and scheduled work orders are never warranty-checked: the AC 2 derivation is installed only in `applyBreakdownWorkOrderCreated`, the Story 7.2 path calls `insertWorkOrder` without `warranty_flagged` so it defaults false, and the AC 3 gate keys on `warranty_flagged === true`. A preventive service on a machine still under manufacturer warranty completes and posts its Story 7.6 cost rollup with no override, no reason code and no record that the asset was covered, while FR-M-11 is written about chargeable work rather than about breakdown work orders specifically [src/compliance/maintenance-fault.ts:582-611, src/compliance/maintenance-plan.ts:535,665] - deferred, the story pins this as intended at spec line 138 ("a preventive work order generated through the Story 7.2 path is never flagged (regression)"), but it was never recorded as a scope limit, so it is logged here to make the gap visible to anyone reading the ledger rather than the spec.
- `applyWorkOrderCompleted` locks the asset, then `statutory_examination` FOR UPDATE for the Story 7.6 stamp invalidation, then the work order, and only then rejects 403 `APPROVAL_REQUIRED` for a missing override, holding the examination lock for the whole round trip until the rollback. Retrying a doomed completion in a loop makes a concurrent weighbridge re-stamp for that asset block behind a chain of transactions guaranteed to fail [src/compliance/maintenance-plan.ts:620-626,665-677] - deferred, moving the warranty gate above the Story 7.6 lock reorders a completion path 7.6 owns and its regression suite pins.
- Review decision D5 guarantees one notification per coverage per RUN, not per business date. Both scan skip paths (the in-transaction existence check and the `DUPLICATE_COVERAGE_ALERT` catch) continue without adding the coverage to `notifiedCoverages`, so two concurrent scans over one contract 25 days from expiry can both page `maintenance_manager`: the winner notifies stage 30 with the escalation block, the loser skips 30, commits 60 and notifies again with no acknowledgment window at all [src/maintenance/coverage-jobs.ts:189,196-197,209-217] - deferred, cross-run notification dedup needs the same unindexed `payload->>'object_id'` scan already logged as a platform convention, and the single-scanner cadence the job is designed for does not hit it.
- All three Story 7.7 shape asserts raise 400 `INVALID_PAYLOAD`, a code the story's Error Code Contract never lists (its only 400-class entry is `INVALID_PARAMS`), so the same failure class returns one code through the REST handlers and another through a direct event. The sibling seams `asset-operational-status.ts`, `maintenance-statutory.ts` and `calibration-register.ts` all do the same [src/compliance/maintenance-coverage.ts:135-282] - deferred, the code follows repository precedent and the contract table is what is incomplete; completing it is a spec edit spanning every seam-bearing story.
- `business_date` is documented as a filter on the cross-asset coverage list but `coverageStatusFilter` returns early when `status` is absent and never reads it, so `GET /api/v1/maintenance/coverages?business_date=2026-13-45` returns 200 with an unfiltered register rather than the 400 every other date input in this story produces [src/api/v1/maintenance.ts:3979-4005] - deferred, the same status-coupled filter shape is inherited from the Story 7.5 twin and changing it alters a documented filter contract across both stories.
- When `status` is supplied without `business_date` the server substitutes `toIstCalendarDate(new Date())` but the response carries only `coverages`, so two identical requests either side of IST midnight return different sets with no field saying which business date was used [src/api/v1/maintenance.ts:4004,4040-4046,4078-4084] - deferred, echoing the resolved business date changes a response shape the story's integration suite pins.
- The warranty-override reason-code allow-list is compared with `Array.includes`, an exact case-sensitive and whitespace-sensitive test in both the handler and the seam, while `canonicalCoverageReference` in the same module case-folds the contract reference. The two human-entered fields on this story therefore disagree about canonicalization, and the loader's duplicate check is case-sensitive too, so `"A,a"` loads as two distinct valid codes [src/config/index.ts:326-335, src/compliance/maintenance-coverage.ts:103,532-540] - deferred, canonicalizing reason codes is a config-contract decision affecting every deployment's env file.
- The override handler checks DOA authority (403) then the reason code (422), while the seam checks the reason code (422) then DOA authority (403 or 404), so a non-approver posting an unknown reason code gets 403 through the REST route and 422 through `POST /api/v1/events` [src/api/v1/maintenance.ts:4218-4253, src/compliance/maintenance-coverage.ts:532-563] - deferred, both paths reject and aligning them means choosing which layer's ordering is canonical, a platform-wide handler-versus-seam convention question.
- `completeWorkOrderBase` runs the warranty pre-check immediately after the not-found check, ahead of any already-completed check, so a flagged and already-completed work order whose override row was removed would report 403 `APPROVAL_REQUIRED` rather than 409 `WORK_ORDER_ALREADY_COMPLETED`. The seam itself orders the checks as Task 4.4 pins them [src/api/v1/maintenance.ts:920-933, src/compliance/maintenance-plan.ts:645-677] - deferred, not reachable through the API today because a flagged work order cannot reach `completed` without an override, and the handler pre-check is a courtesy only under Binding Decision 10.

## Deferred from: code review of 7-7-amc-warranty-and-insurance-tracking (2026-08-27, Group C)

- Two parallel scans on one `business_date` - the double-notification race the row lock in `coverage-jobs.ts:158-168` exists to serialize - are never tested; the suite races overrides and coverage writes but never the scan itself. The outcome split between winner and loser is nondeterministic and the sequential same-date re-run already pins the `(coverage_id, stage_days)` grain [test/integration/story-7-7.test.ts:679-691] - deferred, revisit if the lock strategy changes.
- The `notifications_dropped > 0` reconciliation path (zero-recipient or emit-failure, the Story 7.4 lesson the suite's own comment cites) never fires; every test asserts it is 0. Exercising it means tearing down the role assignments the rest of the suite depends on [src/maintenance/coverage-jobs.ts:228-231] - deferred.
- Notification fan-out is asserted by counting `user_role_assignments` rows and trusting the scan's `notifications_delivered` counter; no test reads delivery or dispatch rows to confirm a recipient actually resolved [test/integration/story-7-7.test.ts:429-436,598-607] - deferred, the dispatch surface is outside this story's scope.
- Projection-only fixtures (`insertCoverageFixture`, `insertPreventiveWorkOrder`) insert rows with no backing domain event, invent a `plan_id` with no plan row, and create a future-start warranty the API's fail-closed gates forbid; if any seam later re-derives from events these tests pass against impossible state [test/integration/story-7-7.test.ts:223-246,293-303,965] - deferred, deliberate fixture strategy; revisit if replay re-derivation lands.
- The renewal-overlap window (a renewal recorded before lapse leaves BOTH rows raising stages, documented in `coverage-jobs.ts:120-124`) is dodged: the renewal test scans only where the original is already expired, so the only interesting case is never observed [test/integration/story-7-7.test.ts:781-806] - deferred, pre-existing; the underlying behavior is already in this ledger from Group B (D1 revert).
- Same-key idempotent replay with a DIFFERENT body silently returns the original 201; no test in any story observes it [test/integration/story-7-7.test.ts:1630-1655] - deferred, pre-existing platform-wide idempotency semantics.
- Capitalization at exactly the threshold (`>` versus `>=` against `capitalizationThreshold`) is unpinned; only threshold-plus-one and a far-below value are tested [test/integration/story-7-7.test.ts:1183-1213] - deferred, pre-existing Story 7.6 surface.
- `TODAY` is captured once at module load while handlers read the wall clock per request, so a suite run spanning UTC midnight shifts every fixture offset by one day with no assertion attributing the failure to clock drift; the file comment acknowledges it [test/integration/story-7-7.test.ts:25-28,132] - deferred, pre-existing suite convention.
- Gate persistence after warranty lapse (a flagged work order still rejecting 403 without an override after its warranty expires; the flag is immutable at creation) is only implied, never tested - pinning it requires mutating projection rows mid-test, the same anti-pattern the fixture defer records [src/compliance/maintenance-coverage.ts:543] - deferred.
- The Story 7.2 PM-generation seam path is never exercised by the preventive-work-order warranty test: the suite proves only the `warranty_flagged` column DEFAULT false via a raw projection fixture, so a regression that made the 7.2 seam start (or wrongly skip) warranty derivation would not be caught here. Resolved by user decision in the Group C review (2026-08-27): the test was re-worded to claim only what it proves rather than routed through the 7.2 surface [test/integration/story-7-7.test.ts, src/compliance/maintenance-plan.ts:535] - deferred, revisit if FR-M-11 is ever widened to preventive work.

## Deferred from: dev-story 6-2-material-staging-issue-and-wip-ledger (2026-08-28)

- Lot-controlled consumption enforcement and FEFO-directed staging are out of scope (Story 6.4, FR-MO-11): staging/issue in 6.2 accept lot-less drains exactly as the Epic 2 helpers do, and an operator-named lot is staged verbatim with no FEFO direction [src/compliance/production-material.ts applyMaterialStaged/applyMaterialIssued] - deferred by the story's Binding Decisions; lot genealogy joins 6.4.
- Completion quantity tolerances and over/short completion are out of scope (Story 6.3, FR-MO-09): confirmations in 6.2 are not capped against the order quantity - a confirmation may exceed the remaining order quantity and the backflush drains proportionally regardless [src/compliance/production-material.ts applyConfirmationRecorded] - deferred.
- Completions, co/by-product posting, and QC Hold hand-off are out of scope (Story 6.3, FR-MO-07): the confirmation event records backflush consumption only; finished-goods receipt and QC routing belong to 6.3.
- Process scrap declarations (Story 6.3, FR-MO-08) and consumption variance reporting (Story 6.4, FR-B-08) are out of scope: 6.2's explosion consumes the BOM's static scrap_percent but never records an operator scrap declaration.
- Closure gate, lot genealogy, offline execution and replay are out of scope (Story 6.4, FR-MO-11/12/13): no edge capture module and no PowerSync bucket exist for the production stream (verified - only indent/cross-dock/test capture exist); AC3's operator reporting is the synchronous error response.
- Cross-stream valuation coherence is a platform decision: production-stream issues drain stock but do NOT call applyValuationIssue, so inventory_valuation.running_average_cost is untouched by consumption (the 3.9/7.4 precedent; the valuation seam is gated to stream_type inventory + stock.received/stock.issued) [src/compliance/production-material.ts] - logged as a platform decision, not fixed here.
- Quarantine/quality-hold screening of staging source bins is out of scope: stock-class scoping is the Phase-1 guard; lot QC-hold enforcement arrives with 6.4's consumption gate.
- Rework orders from QC dispositions are out of scope (Story 6.3, blocked on Epic 8).
- The WIP ledger carries an UPDATE grant for app_user (recorded deviation): the story's Table 2 says "INSERT, SELECT only (append-only)" but the Applier Contract requires decrementing open_quantity on the source posting inside the return transaction and locking it FOR UPDATE (PostgreSQL refuses FOR UPDATE without UPDATE privilege), so AC6 and the 6.1 cancel-guard contract are impossible without it; posting rows remain append-only (never deleted, never rewritten) [read/projections/production_wip_ledger.sql].
- The staging projection carries a DELETE grant for app_user (recorded deviation, code-review decision 2026-08-28): the production_order.cancelled applier rolls staged-but-unissued stock back to `available` and clears the stage rows inside the cancel transaction, which requires DELETE on production_order_stage; nothing else in the application deletes stage rows (the UNIQUE grain stays the replay guard) [read/projections/production_order_stage.sql].

## Deferred from: code review of 6-2-material-staging-issue-and-wip-ledger (2026-08-28)

- `business_date` and stage `created_at` derive from the client-controlled `staged_at` while the requirement set is pinned to server time, so a backdated event books stock into a past business date with today's BOM revision - per-spec behavior (the Event Contract defines `business_date` as the IST date of `staged_at`), consistent with the 6.1 client-timestamp convention [src/compliance/production-material.ts:632-633] - deferred.
- `posting_value` NUMERIC(14,3) and derived `required_quantity` NUMERIC(18,6) can overflow to an unmapped 500 (SQLSTATE 22003) on pathological magnitudes (quantity up to 10^12 times a unit_cost up to 10^11); the derived-value regex admits up to 40 digits [src/read/projections/production_wip_ledger.ts:112, src/compliance/production-material.ts:133] - deferred, pre-existing.
- LIMIT/OFFSET pagination on the append-only WIP ledger can skip or duplicate rows across pages when a posting lands between fetches; no keyset or total is returned [src/read/projections/production_wip_ledger.ts:196-211] - deferred, pre-existing.
- The staging `lines[]` array is unbounded, so one event can run hundreds of descendant walks plus inserts and allocations in a single transaction (request amplification) [src/compliance/production-material.ts:516-629] - deferred, pre-existing.
- Cumulative confirmations are not bounded by the order quantity, so repeated confirmations inflate WIP and drained stock past 100% of the order (stock exhaustion is the only stop) - explicitly out of scope (Story 6.3, FR-MO-09), already logged in the dev-story 6-2 block above [src/compliance/production-material.ts:818] - deferred.

## Deferred from: dev-story 7-8-offline-technician-workflow-and-closure-codes (2026-08-28)

- Parked dependents (Binding Decision 3) are visible only on the device: when a head capture settles `STREAM_CONFLICT`, every later outbox row on the same stream is parked `needs_attention` with `parked_behind_event_id` and never posted, so the central sync-conflict queue holds only the head conflict. There is no central record of the parked rows and no device-side "discard parked" action beyond the existing sync-failure list.
- No automatic re-application of a resolved conflict (Binding Decision 6): `POST /api/v1/maintenance/sync-conflicts/:conflictId/resolve` records `discarded` or `reapplied_centrally` and nothing else; `reapplied_centrally` means the supervisor re-entered the change through the normal routes. Without a pre-filled "complete from conflicting payload" action on the supervisor screen, `discarded` is the only real button.
- Closure coding is optional (all-or-none) for preventive work orders and mandatory only for `origin = 'breakdown'` (Binding Decision 8), pending product-owner confirmation. Reversing it means editing every 7.2/7.6/7.7 preventive completion fixture; the breakdown fixtures in `story-7-4` and `story-7-7` already carry the three codes.
- The technician worklist (`GET /api/v1/edge/maintenance/worklist`) is company-wide and bounded by `limit` (default 200, max 500), not site-scoped, because work orders and the asset register carry no location (AD-9). The response always carries `total` and `truncated`; a plant with more open work orders than the limit sees "N more work orders not loaded" on the device and cannot capture against the rest until they are loaded.
- `STREAM_CONFLICT` for non-maintenance streams still settles `needs_attention` on the device with no central queue; the sync-conflict queue is maintenance-only by this story's scope, and the raise runs only for `stream_type = 'maintenance'` on the edge route.
- `in_progress` and `on_hold` work orders past their grace window are not swept to `overdue`: `listGraceExpiredWorkOrders` still selects `status = 'open'` only (Binding Decision 7 boundary, pinned by the story-7-8 status-machine test).
- The raise-commit to notification crash window: `raiseMaintenanceSyncConflict` persists the queue row, then emits the supervisor notification through the decoupled `emitNotification` (AD-17). A crash between the two leaves an open queue row with no notification; `GET /api/v1/maintenance/sync-conflicts?status=open` is the read surface that does not depend on the notification.
- The edge upload `DUPLICATE_EVENT` pre-check (`findExistingEdgeEvent`) is a SELECT outside the persist transaction; the race path is the `uq_idempotency` / `domain_events_pkey` unique index, whose 23505 mapper returns the existing row when `persistEvent` owns the transaction, which the edge handler does not re-map to 409 (a concurrent duplicate that loses the race by microseconds is returned as the original 201 body). The sequential and near-sequential paths return 409.
- A plant reconnecting after an outage emits one 409 `DUPLICATE_EVENT` per re-posted event on `POST /api/v1/edge/events`; that burst is EXPECTED and any 4xx-rate alerting on that route must exclude `DUPLICATE_EVENT`.
- The REST-path replay contract (2xx via `replayIdOrReject`, and the remaining idempotency-replay test failures across the suite) remains the open platform decision it has been since Story 2; this story fixed the EDGE contract only. The `story-1-8` "duplicate upload" test flipped to passing because of it.
- The rebase-safe set (`REBASE_SAFE_EVENT_TYPES`) is a hand-enumerated list holding exactly `maintenance.work_order_overdue`; any new job that writes on a work-order or reservation stream must be reviewed against it. "If the rebase-safe set ever grows past events no human authored, that is a bug, not a policy."
- No asset-tag list is synced to the device, so a mistyped tag on a NON-safety fault settles `needs_attention` on the tablet with no central trace; only safety-flagged faults are queued (`safety_fault_rejected`, Binding Decision 4).
- The worklist snapshot metadata (`total`, `truncated`, `fetched_at`) lives in `localStorage` on the device rather than in a `localOnly` table, so clearing site data without clearing the OPFS database drops the truncation banner until the next refresh.
- Re-affirmed inherited platform gaps, exactly as logged under 7.5/7.6/7.7: a mismatched `stream_type` skips every seam; same-event-type key reuse with a different body returns the original; unprovisioned notification roles fan out to zero; the `payload->>'object_id'` dedup scan is unindexed; `requireBusinessDate` is calendar-only. The edge route now pins `stream_type` through RBAC module resolution and rejects non-allowlisted `maintenance.*` event types, but still does not check event-type/stream consistency for other streams.

## Deferred from: code review of 7-8-offline-technician-workflow-and-closure-codes.md (2026-08-28)

- Parked dependents have no un-park path (Decision 3): rows parked `needs_attention` + `STREAM_CONFLICT` sit in `SETTLED_STATUSES` and are never re-uploaded even after the head conflict is resolved; the supervisor re-enters via normal routes per Decision 6, so the parked rows remain informational only.
- `status_updated_at` / `status_updated_by` / `status_note` are written only by `applyWorkOrderStatusUpdated`; `setWorkOrderCompleted` and `setWorkOrderOverdue` leave them untouched, so a completed or overdue row still reports the last technician transition rather than its completion.
- `idx_maintenance_work_order_closure_asset` omits `origin`, so the default breakdown-first `listRecentClosuresForAsset` filter cannot use the index (bounded by LIMIT 5, so cosmetic).
- The worklist fans out `listSpareReservations` per work order serially with no batching beyond `limit` (up to 500 round trips); the spec's Task 6.4 defines the per-work-order shape, so this is an optimization not a defect.
- `getStreamHeadVersions` returns a `Map` keyed by lowercased UUIDs; callers lowercase consistently per the L304 convention, but the contract is undocumented and a future caller using the original case would read `undefined` instead of `0`.
- Sync-conflict read surface (`GET /api/v1/maintenance/sync-conflicts`) is unscoped and returns `conflicting_payload` company-wide; whether the supervisor queue should be site-scoped or redacted is an access-model decision left open by AD-9 (company-wide reads).

## Deferred from: code review of 8-1-inspection-plans-and-qc-gate.md (2026-08-29)

- SOD recorder set read directly from domain_events instead of a read projection [src/compliance/quality.ts:1296] - CLOSED by Story 8.2 (2026-08-29): `knownResultRecorders` now reads `qc_inspection_result.recorded_by` through `listResultRecorderUserIds` (earliest `recorded_at`, then `result_id`), and the Story 8.1 SOD test records a real instrument-bound result through `POST /api/v1/qc/tasks/:taskId/results`
- Registry `streamType` is not consumed by any enforcement path; the QC stream gate uses a hardcoded `QUALITY_EVENT_TYPES` set that can drift from `SUPPORTED_EVENT_TYPES` [src/events/schema.ts:4121] - CLOSED by Story 8.2 (2026-08-29): `test/unit/quality-event-registry.test.ts` pins `QUALITY_EVENT_TYPES` to every registry entry with `streamType: 'qc'` and the central-only set to the family minus `qc.result_recorded`
- `requiresBusinessStream: true` on `qc.completion_received` also routes it through the cost_centre/project_code tagging-rule engine, which can demand fields the QC contract cannot supply [src/events/schema.ts:4129] - deferred, document or revisit when Story 6.3/9.4 producers integrate; Story 8.2 registers its five event types with an explicit `requiresBusinessStream: false` (decisions on an already-tagged task or on plan master data, AD-14)
- The seven QC DDL files lack the `ADD COLUMN IF NOT EXISTS` and drop-then-add self-heal patterns sibling projections use; Story 8.3 must drop-and-re-add `chk_qc_inspection_task_gate_status` and `chk_qc_lot_disposition_disposition` to widen the accept/reject vocabulary [read/projections/qc_lot_disposition.sql:42] - deferred to Story 8.3
- `qcGateExclusionSql` interpolates its `alias` identifier unescaped into SQL; all three callers pass the literal `'stock_balance'` today, so a future caller passing an untrusted alias would be an injection vector [src/read/projections/qc_inspection_task.ts:242] - deferred, hardcode or allowlist the alias when hardening
- Transfer passes the lot UUID into the number-keyed `stock_balance` ledger, so a lot-specified transfer cannot find its stock and fails INSUFFICIENT_STOCK [src/compliance/transfer-request.ts:261] - deferred, pre-existing Story 2.5 UUID/number mismatch; the conditional-release internal-movement path depends on it, and the Story 8.1 test masks it by seeding a UUID-keyed row. Bridge UUID to number (like pick.ts `lotNumberForUuid`) before calling applyStockAllocation/applyStockIssue
- FEFO/FIFO auto-select locks `stock_balance` rows before the lot and gate rows, inverting the mandated lot -> gate -> stock order and opening a deadlock window against dispatch/conditional-release [src/compliance/lot-serial-validation.ts:179] - deferred, deadlock risk
- A serial-pinned issue (serials but no top-level lot_id) never reaches assertQcGateAllows, so a gated serial lot surfaces INSUFFICIENT_STOCK instead of LOT_ON_HOLD [src/compliance/lot-serial-validation.ts:588] - deferred
- Cycle-count negative adjustment writes down on_hand for a named finished-goods lot with no QC-gate check [src/compliance/cycle-count.ts:1035] - deferred, out of Story 8.1 scope; document as inapplicable (physical count correction) or add a gate check
- assertQcGateAllows still returns silently when no lot reference is supplied [src/compliance/quality.ts:1621] - deferred, require a reference for specific-lot operations
- The obsolescence `last_issue_at` stamping runs on all rows at the grain with no QC predicate, so a hidden gated lot still has its clock bumped [src/read/projections/stock_balance.ts:443] - deferred, minor data-quality
- The conditional-release expiry check derives the business date from the client-supplied `occurred_at`, so an authorized actor posting a transfer/production event could backdate it past an already-expired deviation [src/compliance/quality.ts:1647] - accepted and documented in `gateBusinessDateOf` per review decision (edge backdating is a platform trust model; expiry bounds authorized movement, not an external attacker)
- Task read routes (`listQcTasksHandler`, `getQcTaskHandler`) enforce module/function RBAC but no location scope, so a single-site `qc` read grant can enumerate every site's tasks and read their conditional-release deviations [src/api/v1/quality.ts:618] - CLOSED by Story 8.2 (2026-08-29): every task-scoped read (task detail, sampling, results) checks `permittedLocationsForModuleScope(roles, 'qc', 'read')` against the task site, and the task and switching-state list routes narrow to the caller's permitted sites (403 LOCATION_ACCESS_DENIED on an out-of-scope `site_id`)
- The edge QC `CENTRAL_ONLY_OPERATION` deny rule is shadowed by the `requireRole` module wrapper, so a device uploading a `qc.*` event surfaces `MODULE_ACCESS_DENIED` instead of the mandated `CENTRAL_ONLY_OPERATION` [src/api/v1/edge.ts:238] - deferred, error-code conformance only (no bypass); Story 8.2's edge test uses a `qc`-module actor and observes CENTRAL_ONLY_OPERATION for every new central-only type
- Plan-approve and conditional-release audit rows stamp `location_id` from the arbitrary `functionMatches[0]` assignment rather than the task/lot site [src/api/v1/quality.ts:811] - PARTLY CLOSED by Story 8.2 (2026-08-29): `auditRejectedAttempt` takes an explicit `location_id` and every Story 8.2 write route stamps the task site (or the state's site); the Story 8.1 plan-approve and conditional-release call sites still use the assignment location and remain deferred

## Deferred from: dev-story 8-2-aql-sampling-and-result-capture (2026-08-29)

- Table II-A / II-B / II-C are transcribed from the standard's band structure (eleven plan diagonals on normal and reduced, ten on tightened, with the Ac 0 diagonal through G at AQL 1.0 on normal and H at AQL 1.0 on tightened) and verified against the story's fifteen pinned anchor cells; the four bottom-left corner cells the standard resolves through the supplementary letter S (n = 3150) are pinned to Ac 0 / Re 1 at letter R (n = 2000) because the story fixes a 16-letter vocabulary [src/quality/aql-tables.ts] - deferred, verify every cell against the IS 2500 (Part 1):2000 PDF (law.resource.org serves a scanned image, not extractable text) before the tables are relied on for AQLs at or below 0.015 with letter R; any disagreement is a data-only change
- The reduced-inspection Ac / Re pair sequence is transcribed as 0/1, 0/2, 1/3, 1/4, 2/5, 3/6, 5/8, 7/10, 10/13, 14/17, 21/24 (eleven diagonals, aligned with Table II-A) [src/quality/aql-tables.ts TABLE_II_C] - deferred, confirm the top-right cells of Table II-C (whether the standard prints a twelfth 30/31 diagonal) against the PDF
- Under a full_inspection plan (no AQL) the lot outcome is not_accepted on ANY nonconforming unit, since no Ac / Re exists (Ac 0 semantics of a 100 % inspection) [src/quality/switching.ts evaluateOutcome] - deferred, confirm with QC whether a null-AQL plan should instead carry an explicit acceptance rule
- Every later determination attempt for a task replays the frozen plan through the REST handler (200 with the stored plan when the task is no longer open); the seam itself rejects a second `qc.sampling_determined` under a different idempotency key with 409 QC_TASK_NOT_OPEN, so a direct-event replay is a rejection, not a replay [src/api/v1/quality.ts determineSamplingBase] - deferred, acceptable split between the route contract (AC 1) and the fail-closed seam
- The `qc.sampling_state_adjusted` authority check compares the acting role against `config.quality.qcHeadRoles` only; no DOA transaction type governs reduced-inspection authorization or resumption, so a QC Head-level role holder at ANY site with `qc` write scope on the target site can adjust the state [src/compliance/quality.ts applySamplingStateAdjusted] - deferred by Binding Scope Decision 9 (no DOA transaction type is added); revisit if the access matrix wants a DOA-governed approver
- Re-inspection, corrections and resubmitted lots are not modelled: a task moves `open` to `sampling_determined` to `inspected` exactly once, results are immutable (409 QC_RESULT_EXISTS), and a not_accepted lot stays in `qc_hold` until Story 8.3 dispositions it [read/projections/qc_inspection_result.sql, src/compliance/quality.ts applyInspectionCompleted] - deferred by the story's Out of Scope list
- The switching score counts every lot once, using the frozen plan's Ac and the switching state's severity at completion time; when several lots of one (plan, site) are open at once, the state advances in completion order, not determination order, so a lot determined under normal inspection may complete after the state has already switched to tightened (its frozen normal plan still governs its own outcome) [src/compliance/quality.ts applyInspectionCompleted] - deferred, consistent with clause 9.3's lot-sequence wording; revisit if QC wants determination-order sequencing
- Instrument-type matching between the frozen plan line (`instrument_type`) and the register asset is not enforced: any registered, calibrated instrument may record a result on any instrument-bearing line [src/compliance/quality.ts applyResultBatch] - deferred by the story's Out of Scope list (per-parameter calibration ranges and instrument-type matching are a Story 7.5 deferral)
- The `qc_inspection_result` insert is one statement per reading (up to 500 round trips per batch) [src/read/projections/qc_inspection_result.ts insertQcInspectionResults] - deferred, optimization only; the batch is inside one transaction and the unique constraint is the backstop
- The Story 1.7 synthetic `qc.result_recorded` shape is still accepted on `POST /api/v1/events` and the edge route with no task binding and no projection; it remains an audit-only event whose lockout is its sole gate [src/compliance/quality.ts assertResultRecordedShape] - deferred by Binding Scope Decision 1 (Spine Acceptance test 4 must stay green)
- `format:check` reports four files untouched by this story (src/compliance/weighbridge.ts, src/read/projections/asset.ts, src/read/projections/instrument_calibration_escalation.ts, src/read/projections/instrument_register.ts) that already fail at baseline 462668d - deferred, reformat in a housekeeping commit

## Deferred from: code review of 8-2-aql-sampling-and-result-capture.md, Group A - schema & projections (2026-08-29)

- `code_letter`/`resolved_code_letter` have no vocabulary/format constraint on `qc_sampling_plan` [read/projections/qc_sampling_plan.sql] - deferred, seam-derived write-back only (not client-writable), low exposure
- Text-hygiene inconsistency: `lot_number`, `instrument_id` lack non-empty checks that sibling free-text columns in the same files carry [read/projections/qc_sampling_plan.sql, read/projections/qc_inspection_result.sql] - deferred, cosmetic data hygiene gap consistent with pre-existing pattern elsewhere
- No index on `qc_inspection_task.sampling_id` / `inspected_by` [read/projections/qc_inspection_task.sql] - deferred, performance-only, table is small at pilot scale

## Deferred from: code review of 8-2-aql-sampling-and-result-capture.md, Group B - sampling/switching domain logic (2026-08-29)

- A pre-8.2 approved `inspection_plan_version` could carry an AQL that passed Story 8.1's looser regex-only gate but is not one of Story 8.2's 26 preferred AQL values (`PREFERRED_AQLS`); `determineSampling` throws `AQL_NOT_IN_STANDARD` permanently for any task on that plan version, with no path forward [src/quality/sampling.ts, src/compliance/quality.ts assertInspectionPlanCreatedShape] - deferred 2026-08-30 (decision resolved), low real risk: Stories 8.1 and 8.2 shipped the same day, so no real plan version is expected to carry a non-preferred AQL yet; revisit with a one-time audit query or a snap-to-nearest-preferred-AQL change if it ever surfaces
- `resolveQcResultDuplicateConflict` on a `uq_qc_inspection_result_unit` violation returns every submitted `sample_unit_no`, not the one(s) that actually collided [src/compliance/quality.ts] - deferred, diagnostic quality only, the 409 itself is correct
- A numeric characteristic with no declared `instrument_type` is still routed through the instrument-bound "result" endpoint's full asset/calibration gate [src/compliance/quality.ts applyResultBatch] - deferred, needs checking against the inspection_plan_characteristic schema for whether numeric + no-instrument-type is a legal plan-line combination; related to the instrument-type-matching deferral above
- `determineSampling`'s "AQL null but inspection_level non-null" defensive branch looks unreachable (the invariant is already enforced at plan-creation time) and is untested [src/quality/sampling.ts] - deferred, dead-code hygiene only
- `lockTaskForInspection` reads the task unlocked to get `lot_id`, locks that lot row, then re-reads the task `FOR UPDATE` without re-verifying the two `lot_id` reads match [src/compliance/quality.ts] - deferred, latent TOCTOU only; `lot_id` is immutable on a task today so there is no current exploit path

## Deferred from: code review of 8-2-aql-sampling-and-result-capture.md, Group C - AQL table data module (2026-08-30)

- Several lookup functions trust caller-supplied enum types instead of runtime-validating with the module's own `isInspectionLevel`/`isCodeLetter` guards: `codeLetterFor`'s `level` param and `singleSamplingPlan`'s `letter` param throw a raw `TypeError` on an invalid runtime value instead of a documented failure; `tighterAql` silently returns `null` (meaning "already tightest") for an unrecognized AQL rather than distinguishing invalid input from a real boundary [src/quality/aql-tables.ts] - deferred, low real risk since every current call site (Group B) pre-validates before calling in
- Test-coverage gaps in `test/unit/aql-tables.test.ts`: the monotonicity test only checks `ac`, never `re`; the long arrow-chain test (`singleSamplingPlan('R','1000','normal')`) asserts a tautology true by construction regardless of which letter the chain resolves to; no test exercises `singleSamplingPlan`'s guard-overflow safety net; `codeLetterFor`'s Table I tests never probe a lot-size band boundary (e.g. 500 vs 501) or zero/negative input - deferred, bundle into a future test-hardening pass alongside the existing table-verification deferral above
- `codeLetterFor`'s `Number.isSafeInteger` ceiling conflates "lot size too large" with "too small" (same null return) for lots above 2^53-1; no structural/type-level safeguard ties `resolved_letter` to the correct sample-size table (`SAMPLE_SIZES` vs `REDUCED_SAMPLE_SIZES`) beyond the single ternary in `singleSamplingPlan` [src/quality/aql-tables.ts] - deferred, negligible real-world relevance and no current defect (single call site)

## Deferred from: code review of 8-2-aql-sampling-and-result-capture.md, Group D - API/read layer + edge/sync (2026-08-30)

- `adjustSamplingStateBase` always returns `201`, never `200`, on an idempotency-key replay, unlike every sibling write handler in this file [src/api/v1/quality.ts] - deferred, `QcSamplingStateAdjustedPayload` has no natural per-request marker field to detect replay the way sibling routes do; a clean fix needs either a payload-field addition (reopens the already-reviewed schema) or a new replay-detection idiom (e.g. comparing `persisted.created_at` against the request's own timestamp) not used anywhere else in the codebase; data correctness is unaffected, only the HTTP status code misreports on replay
- `determineSamplingBase`'s replay short-circuit has a narrow race window: if a concurrent determination commits between this request's task fetch and its frozen-plan fetch, this request falls through and mints a second `sampling_id`, hitting `uq_qc_sampling_plan_task` (409 `QC_SAMPLING_EXISTS`) instead of returning a clean 200 replay [src/api/v1/quality.ts] - deferred, self-healing (the domain layer's lock and constraint always produce the correct data outcome), only a wrong status code under a narrow timing window
- `sample_unit_no`/`limit`/`offset` query-param regexes have no upper bound, so an extremely long digit string could produce an imprecise `Number()` value sent to Postgres [src/api/v1/quality.ts listResultsBase, listSamplingStatesBase] - deferred, this exact unbounded pattern is already used identically at two pre-existing Story 8.1 routes in the same file; fixing only the two new routes would create inconsistency with the two existing ones

## Deferred from: code review of 8-2-aql-sampling-and-result-capture.md, Group E - integration tests (2026-08-30)

- AC-proof gaps in test/integration/story-8-2.test.ts: AC1's "null level defaults to II" is never actually exercised by a null-level/non-null-AQL plan; AC3's Ac-0/1-plan switching score is only driven to 4, never to the claimed threshold of 30; the inspector-forbidden authorize_reduced 403 is never checked against audit_log unlike its resume_inspection sibling test; the "two simultaneous completions" concurrency test fires both completions against an already-completed task, proving double-completion-rejection rather than a genuine completion-vs-completion race; Story 8.1's SOD fixture only proves conditional release works in sampling_determined, never in inspected, per Task 9's explicit checklist item - deferred, bundle into a future test-hardening pass
- Missing negative-path/boundary integration tests: a genuine same-idempotency-key replay of results/observations/completion never asserts the 200 status; malformed action/blank reason on the switching-state route; empty readings[] from an authorized caller; malformed query params (sample_unit_no, limit, offset) on GET results; 404 on a missing task for 5 of 6 task-scoped routes; the 501-reading cap test doesn't assert its own error_code; the attribute-line mirror of QC_RESULT_KIND_MISMATCH; an instrument-bound attribute characteristic; an "up" arrow code-letter resolution driven end-to-end (only unit-tested today); inspection levels other than II never determined end-to-end; a real (not random-UUID) characteristic_id from a different plan for QC_CHARACTERISTIC_NOT_IN_PLAN; a "never calibrated" (zero-certificate) instrument distinct from a "lapsed" one [test/integration/story-8-2.test.ts] - deferred, bundle into the same future test-hardening pass
- Test-helper quality nits: inspectLot's multi-line branch computes the wrong required-reading count (currently dead/unreachable) and its string-based quantity parsing would silently produce NaN for a differently-formatted quantity; recordRealResult's headers parameter silently doesn't affect the sampling-determination sub-call (undocumented, likely intentional); near-duplicate instrument-provisioning fixture helpers exist in both story-8-1.test.ts and story-8-2.test.ts and could drift independently [test/integration/story-8-2.test.ts, test/integration/story-8-1.test.ts] - deferred, low value relative to cost, bundle into the same future test-hardening pass
